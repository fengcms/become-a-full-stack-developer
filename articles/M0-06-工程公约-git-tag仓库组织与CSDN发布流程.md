# 成为全栈·产品篇·工程公约：git tag、仓库组织与 CSDN 发布流程

**本文目标**：前面几篇讲了「做什么」——七端、建模、契约。这篇讲「怎么把这一切组织起来，让它能复现」。一套专栏最大的承诺是「读者跟着做能跑起来」，而让这个承诺成立的东西，就是工程公约：monorepo 单仓、每篇一个 git tag、一份 `ARTICLES.md` 对照表、以及 CSDN 发布流程。

**前置知识**：建议先读 {{LINK:M0-02}}（七端全貌）、{{LINK:M0-05}}（契约）。本文把它们的「组织形式」钉死。

---

## 一、monorepo 单仓多目录

七个子项目（M1–M7）放在**同一个 git 仓库**里，用目录区分：

```
become-a-full-stack-developer/
├── node-backend/        # M1
├── manage-frontend/     # M2
├── web-frontend/        # M3
├── app-frontend/        # M4
├── go-backend/          # M6
├── docs/                # 章程 / 路线图 / 领域模型 / 契约
├── articles/            # 本系列文章草稿
└── ARTICLES.md          # 文章 ↔ 代码 ↔ tag 对照表
```

为什么是单仓而不是七个独立仓库？因为七端**共享同一份契约**。单仓让契约和所有端处在同一条版本历史里——你打一个 tag，锁住的是「契约 + 全部端」那一刻的完整状态，而不是某个端孤立的快照。读者 checkout 一个 tag，拿到的是整个系统当时长什么样。

把这种「一份契约、多端同仓」的结构画出来：

{{IMG:06-monorepo结构图}}

你可能会问，为什么不用七个独立仓库？因为 tag 机制会垮。如果七个端各占一个 repo，那根 tag `article/M1-05` 只能锁住 M1 自己的代码，读者想看「当时整个系统长什么样」，得手动去另外六个 repo 翻各自的对应 commit——而它们根本没打对应的 tag。单仓让一根 tag 锁住全局，这才是「可复现」能成立的前提。

配套铁律是「一个 M 只讲一件实现」：每篇文章聚焦一个端/目录，不把后端和前台塞进同一篇。这样你读的时候心智干净，代码归属也清晰。

---

## 二、每篇文章一个 git tag

这是可复现性的核心。约定：

- 每篇文章发布时，打一个全局 tag，命名 `article/M1-05`、`article/M3-12`……
- 读者执行 `git checkout article/M1-05`，就能拿到**那篇文章发布时刻**的完整代码状态——契约、后端、前端，全部对齐。
- 我们的工程产出指标（P2）就这一条：读者能 checkout 到任意一篇文章对应的代码状态并跑起来。

举个例子，你读到 M3-12 那篇讲会员中心，执行 `git checkout article/M3-12`，你不仅拿到 `web-frontend/` 当时的代码，还能看到那时 `node-backend/` 的契约实现、`docs/` 里的契约版本——三者天然对齐。如果是半年后，后端早已被 M6 的 Go 重写改过，但你 checkout 的仍是「M3 时刻」的 Node 版。文字和代码，永远指向同一点。

为什么非要 tag？因为代码是流动的。你今天写的 M1 后端，三个月后 M6 用 Go 重写时早变了。没有 tag，读者读到 M1-05 那篇想照着跑，拿到的却是半年后的代码，对不上号。**tag 是文章和代码之间的一根锚绳**——它让「文字描述的那个世界」和「实际可运行的那个世界」始终指向同一点。

把「打 tag → checkout → 拿到当时完整代码」的链路画出来：

{{IMG:06-tag流程}}

---

## 三、根目录 `ARTICLES.md` 对照表

仓库根维护一份 `ARTICLES.md`，把三件事钉在一起：

| 文章标题 | 对应 tag | 代码位置 / 链接 |
|---|---|---|
| M0-01 为什么前端要走向全栈 | `article/M0-01` | `articles/M0-01-...md` |
| M1-05 … | `article/M1-05` | `node-backend/...` |

它的价值是**双向跳转**：从文章能跳到当时的代码，从代码（tag）能跳回讲了它的那篇文章。没有它，文章和代码会逐渐脱节——这是我们在风险表里明确列过的坑。

> 你正在读的 `articles/` 目录，是写作期的临时草稿归属。随着 M0-06 落地，正式绑定机制以本文为准：文章发布后，标题与 CSDN 链接回填进 `ARTICLES.md`，代码状态由对应 tag 锁定。

设想没有它：你三个月前读了 M1-05，今天想回看当时用的实体字段，却翻到 M1-05 引用了 `Article.status`，而你当前的代码里 status 已经加了新状态。到底哪版才是文章说的？对照表 + tag 把这个疑问彻底消灭。

---

## 四、CSDN 发布流程

写作和代码之外，还有「怎么发出去」：

- **主阵地是 CSDN**（`blog.csdn.net/fungleo`）。自建站只作成果展示，不分发。
- **单篇约 2500 中文字（±300）**。够把一个技术决策或一步实现讲透，不注水；**每篇只讲一件事**，对应一个 tag。
- **质量项但内容优先**：配图 2–4 张、文末 ≥2 条站内往期互链、排版有多样性——这些是 CSDN 的基本质量分工程项，会做，但**内容为王、密度优先**，绝不为了凑分注水。
- **发布后回填**：文章发到 CSDN 后，把链接回填进 `ARTICLES.md` 和本文的延伸阅读区，闭环。

强调一句：这些质量项是「锦上添花」，不是「本末倒置」。一篇 800 字的水文，配再多糖图也救不回阅读完成率；一篇 2500 字的硬货，哪怕只有两张图，读者也愿意读完。所以我的优先级永远是：先把一件事讲透，再谈排版。

这套流程的意义，是把「写一篇技术博客」升级成「交付一个可复现的教学产品」。读者不是来看热闹，是来跟着做出来的。

把这条「写作 → 打 tag → 发 CSDN → 回填」的闭环画成环：

{{IMG:06-发布流程}}

给读者的实操建议——这套公约不是写给我自己看的，是给你用的：你 clone 仓库后，读到某篇想照着跑，直接 `git checkout article/Mx-yy`，就能拿到那篇发布时刻的完整代码，不用手动对齐版本。如果某篇引用了 `Article.status`，而你本地是当前版本对不上，记得 checkout 对应 tag，才看得到文章说的那个世界。

反过来，如果违约会怎样？单仓拆成七个 repo，tag 锁不住全局；不打 tag，半年后代码变了你照着旧文跑对不上；不维护 `ARTICLES.md`，文章和代码迟早脱节；不回填 CSDN 链接，新读者找不到入口——这四条任意一条失守，「可复现」的承诺就垮了。所以公约看着琐碎，每条都是为「你跟着做能跑起来」这个承诺兜底。

---

## 五、为什么这套公约值得费劲

一句话：**它让「文章是产品」这句话落地，而不是口号。**

没有 monorepo，契约和端会各走各的版本；没有 tag，代码流动后读者对不上；没有 `ARTICLES.md`，文章和代码迟早脱节；没有 CSDN 流程，再好的内容也触达不到人。四样东西单独看都小，合起来是专栏可信度的地基——和契约一样，是「值得提前投入、不能返工」的那类东西。

顺带说，这套公约不只服务于读者，也服务于我自己。130 篇的长周期里，我会忘掉半年前某个端长什么样——tag 和对照表让我随时能回到任意一篇的「当时」，保证后续文章不前后矛盾。它既是给读者的承诺，也是给作者的安全带。

说到底，公约保护的是「信任」：读者相信跟着做能跑起来，作者相信半年后文章和代码还对得上。技术教程最怕的，就是「照着写了一半发现对不上、作者自己也忘了当时长啥样」。这套公约把这种风险压到最低——你任何时候 checkout 一个 tag，拿到的都是文章描述的那一刻，不掺水、不漂移。对一个跨年度、130 篇的长线专栏，这比任何炫技都重要：它让「文章是产品」不是一句口号，而是一个可验证的交付物。

你买的不只是几十篇文章，而是一份「随时能跑、随时能溯」的工程资产。

这也是为什么我把公约放在 M0 而不是某个端里讲——它是地基的地基。

---

## 小结

- 七端放同一个 git 仓库（monorepo 单仓多目录），共享契约，一个 tag 锁全局状态。
- 每篇文章一个全局 tag（`article/Mx-yy`），读者 checkout 即拿到发布时刻完整代码——这是「可复现」的硬承诺。
- 根 `ARTICLES.md` 把「文章 ↔ tag ↔ 代码」钉在一起，支持双向跳转，杜绝文章代码脱节。
- CSDN 主阵地，单篇约 2500 字、每篇一事、内容密度优先；发布后回填链接闭环。
- 四样东西合起来让「文章是产品」落地：可复现、可追溯、可触达。

## 延伸阅读

- {{LINK:M0-02}}《用一个真实系统串起全栈》——本文的 monorepo 结构，对应那篇的七端规划。
- {{LINK:M0-07}}《本系列怎么读》——下一篇，也是 M0 收官，给你两类读者的阅读路径。
- （地基文档）[00-项目章程](./00-项目章程.md)——本文所有公约的权威出处（仓库组织 R14、tag 机制、CSDN 主阵地）。

## 订阅这个专栏

如果你也想跟着一个真实系统，从「调接口的人」走到「设计系统的人」，欢迎订阅我的《成为全栈开发工程师》专栏。后续每篇都会带着可运行的代码和完整的设计取舍走下来，欢迎在评论区讨论、指正。

{{IMG:CTA-订阅专栏}}
