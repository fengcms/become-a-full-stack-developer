# M1 后端 · 批次 B3.5 代码审阅报告

> 审阅者：总把控（Backend Architect 视角）｜方法学：不采信自陈，逐文件回磁盘取证 + 独立复跑门禁
> 审阅对象：commit `43c51c3`（feat(node-backend): 完成B3.5批次：文章标签关联回填+过滤精确化+代码拆分）+ 开发者交付 `docs/node-backend/B3.5-NOTES.md`
> 规格基线：`docs/prd/m1-tasks/03.5-article-tags-backfill.md`（上一轮我写正式任务包）

## 一、裁定

**✅ 通过，放行 B4（评论，5 端点，按 `04-comments.md`）。**

无 P0 / 无 P1 / 无 P2。仅 2 项 P3 非阻塞观察（均不阻放行）。这是自 B1 以来第三批"零缺陷闭环"的批次——同一套"死表→生效""B2 P3 子串误匹配""B3 守卫失效"三个真缺口全部被钉死，且测试断言精确到行为级。

## 二、门禁独立复验（全绿，不采信自陈）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 类型 | `pnpm typecheck` | ✅ 0 error |
| Lint | `pnpm lint` | ✅ 57 文件 0 problem |
| 测试 | `pnpm test` | ✅ **78 passed**（13 文件，B3.5 新增 10） |
| 契约结构门 | `openapi_spec_validator` | ✅ `docs/api/openapi.v1.yaml: OK` |
| 契约语义门 | 未跑（契约未改动，git 确认 `openapi.v1.yaml` 不在 HEAD 改动列表） | — |

> 注：三道代码门禁**分三次独立调用**跑（吸取 B2 复批被沙箱 OOM 杀掉的教训）。契约自 B1 的 `a00377d` 后未被触碰，本次 HEAD 改动清单含 15 文件、无 yaml，故语义门维持历史零回归基线。

## 三、逐文件取证（关键修复确证为真，非假修复）

### ① article_tags 写同步（创建/更新/软删）— 真落地
- `src/lib/article-tags.ts:15` `resolveTagIds`：按 `slug==name` 解析，**不存在的标签名直接跳过**（不越权建 catalog，符合禁止项）。
- `src/lib/article-tags.ts:38` `syncArticleTags`：先 `DELETE` 旧关联、再按新标签 `onConflictDoNothing` 覆盖插，**幂等**；写语句 `.run()`。
- `src/lib/article-mutation.ts:142` `createArticleRow`：插入后 `if (input.tags?.length) syncArticleTags(created.id, ...)`。
- `src/lib/article-mutation.ts:179` `updateArticleRow`：`if (input.tags !== undefined) syncArticleTags(...)` —— 省略则沿用、提供则覆盖，与任务包语义一致。
- `src/routes/articles-write.ts:107` 软删：先 `DELETE FROM article_tags WHERE article_id=?` 再软删文章，junction 与文章生命周期一致。

### ② 存量回填 — 真实可用、幂等、复用核心
- `src/lib/article-backfill.ts:32` `backfillArticleTags(db?)`：抽可复用核心（脚本与单测共用），`parseTags(row.tags)` → `resolveTagIds` → `INSERT ... ON CONFLICT DO NOTHING`，**幂等**。
- `scripts/backfill-article-tags.ts`：独立脚本，`DB_FILE === ':memory:'` 显式告警；底部附 **D1 等价 SQL**（json_each 展开 + JOIN tags），部署侧可操作。

### ③ 列表标签过滤切 JOIN（关闭 B2 P3）— 真精确
- `src/lib/article.ts:173-184`：`q.tag` 解析为 `tagRow`，无对应 catalog → 直接返回空列表；命中则 `conds.push(eq(articleTags.tagId, tagRow.id))` + `innerJoin(articleTags, articleId=articles.id)`。
- **计数正确性论证**：`uniq_article_tag` 唯一约束保证每 (文章,标签) 至多 1 行 → JOIN 不产生行乘法 → `count(*)` 计的是文章数而非关联数（`:227-231` 非 keyword 分支 / `:222-225` keyword 分支均先加 JOIN 再加 where）。**无重复计数风险**。
- `src/lib/pagination.ts:14-18`：**所有排序列改为 `articles.` 限定 + 末位 `articles.id DESC`** —— 根除 JOIN 后 `created_at`/`id` 歧义导致的 `ambiguous column` 500。这是回归安全的改法（白名单列全部带基表前缀）。

### ④ articles.ts 拆分（还 200 行欠债）— 真拆分 + 边界守约
- `src/routes/articles-read.ts`（**123 行**）、`src/routes/articles-write.ts`（**147 行**）均 ≤200，同挂 `/api/v1/articles`（app.ts:37-38）。
- 共享写逻辑下沉 `lib/article-mutation.ts` / `lib/article-tags.ts`，路由层薄委托。
- 公开读/登录写职责分离，对外行为零变化（13 条 articles 既有测试全绿未改）。

### ⑤ B3 P2-1 修复 — 在独立 commit 收尾（符合我上一轮"单独小 commit"要求）
- commit `2cee68b`（fix(category): 修复分类移动深度校验与buildTree环防御问题）。
- `src/routes/categories-write.ts:133-136`：`depthOf(parent) + subtreeHeight(id) > MAX_CATEGORY_DEPTH` —— **被移动节点的整棵子树高度**已纳入，正是我上一轮要求的修复点；`wouldCreateCycle` 环检测同在。

### ⑥ tags.ts 删除守卫 — 真缺口闭环
- `src/routes/tags.ts:98-106`：删除前查 `article_tags WHERE tag_id=?`，非空 → `3002` 拒删。**此前因死表恒空导致守卫失效**（我 B3 审阅指出的真缺陷），现 junction 非空后真正生效。

## 四、测试断言质量（门禁盲区的精准补位）

`test/routes/article-tag-sync.test.ts` 10 例**断言精确到行为级**，直接钉死我前两轮指出的真缺口：
- `filter_a`：`?tag=js` 仅命中 tag=js 文章，`?tag=json` 不反向命中（且 `?tag=js` 否定 `json` 被包含）——**直接关闭 B2 P3 子串误匹配**。
- `guard_a`：被引用标签删除 → `409/3002`；清空关联后 → `200` —— **闭环 B3 死表守卫失效**。
- `bf_a`：清空 article_tags 模拟死表 → 回填从 `articles.tags` 重建 → 幂等（含 `zzz` 不存在标签跳过）—— **闭环死表缺口**。

## 五、P3 非阻塞观察（建议，不要求改）

1. **`src/lib/article.ts` 252 行，略超 200 软上限**：已按项目纪律注释说明特殊情况（四类紧密领域逻辑）。原 200 行铁律的债主——`articles.ts` 路由 358 行——已由 read/write 拆分清零；本文件为 **lib 层**且例外已文档化，裁定为合规。仅提醒：若后续再涨，优先把 `queryArticles` 移入独立 `article-query.ts`。
2. **回填脚本是"部署后人工跑一次"**：NOTES 已显式记录此选择，D1 SQL 也附了。提醒在**部署 checklist 里加一条"首次发布后执行 `pnpm backfill` 或 wrangler D1 SQL"**，否则线上 articleCount 在存量文章被新文章覆盖前仍偏 0（新文章已实时同步，仅历史数据依赖回填）。这是运维动作，非代码缺陷。

## 六、credit（这次地道的点）

- 把"死表回填"拆成**可复用核心 `backfillArticleTags(db)`** + 脚本壳，单测直接复用核心，避免脚本无法单测的通病。
- 索引 `uniq_article_tag` 在两处（schema 声明 + migrate raw SQL）保持一致，`.onConflictDoNothing()` 复用而非 catch 兜底正常路径。
- `parseTags` 提升为导出、回填与运行时共用单源，无逻辑分叉。
- 删除守卫、过滤精确化、深度子树修复三处**我前两轮指出的真缺口，全部不漏、不假修复**。
- B3 P2-1 严格按"独立小 commit"收尾，批次边界干净。

## 七、下一棒

B4 = 评论（5 端点），严格按 `docs/prd/m1-tasks/04-comments.md`，**不碰 articles.ts**。开发 AI 完工后交回复文档，我按同样打法独立复验（含"评论不越权改动文章写逻辑"的边界核对）。
