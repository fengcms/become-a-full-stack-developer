# M1 后端 · 批次 B7：代码审阅第二轮复批（总把控）

> 复批对象：`docs/node-backend/review/B7-后端代码审阅报告.md` + 开发 AI 回复 `docs/node-backend/review/B7-代码审阅-回复.md`
> 复批方：后端架构师（总把控）｜日期：2026-08-26
> 纪律：不采信自陈；凡整改均回磁盘取证（Read 源码 / `wc -l` / `git diff` / 独立跑门禁 / 抓契约机器字段 / 跑测试）。

## 一、复批结论

**B7 整改全部真修、无假修复，裁定放行 —— M1 后端代码正式冻结。**

本批次是 M1 后端最后一棒（B0→B7 八批）。开发 AI 对首轮提出的 1×P2（阻塞）+ 2×P3（陈述）**逐项真修**，且承诺的「门禁五门全绿、126 passed、契约零改动」经独立复验**全部属实**。

> ⚠️ **透明度声明（重要）**：本复批在第一轮独立 `vitest run` 时曾出现 95 失败 / 31 通过的反常结果，与开发 AI 自陈「126 passed」相反。经根因排查，确认这 **95 个「失败」100% 是测试超时（`Test timed out in 5000ms`），无一例断言失败**；根因是本机共享环境瞬时高负载（load average 17，机器 `up 39 days / 21 users`）。改用 `--no-file-parallelism --testTimeout=30000` 剥离负载噪声后，结果 **126 passed / 0 失败**，与开发 AI 自陈逐字吻合。故开发 AI 的「126 passed」是**真实正确的，并非假自证**；此前反常结果是我侧环境干扰，已澄清，不计入对开发 AI 的诚信评判。

## 二、P2-1 真修核验（阻塞项清零）

**契约机器字段**（`openapi.v1.yaml` `SiteSettingUpdate.logoUrl`）：`{type: string, nullable: true, maxLength: 512}` → 允许 `null` 清空。

**代码修复**（`src/routes/site.ts:54`，Read 实测）：
```diff
-  logoUrl: z.string().max(512).optional(),
+  logoUrl: z.string().max(512).nullable().optional(),
```
- 下游 `if (patch.logoUrl !== undefined) set.logoUrl = patch.logoUrl;`（site.ts:72）对显式 `null` 为 `true` → `set.logoUrl = null` → DB 清空，行为正确。
- 同 schema 的 `siteTitle`/`siteKeywords`/`copyright` 早已正确加 `.nullable()`（site.ts:51/53/55），本次补齐最后一处，自陈矛盾消除。

**回归测试真实非空跑**（`test/routes/aux.test.ts:378-394`）：
```ts
const withLogo = await patch({ logoUrl: 'https://example.com/logo.png' });
expect(withLogo.data.logoUrl).toBe('https://example.com/logo.png');   // :386
const cleared  = await patch({ logoUrl: null });
expect(cleared.data.logoUrl).toBeNull();                              // :394
```
先设值、再传 `null`、断言返回 `null` —— 端到端覆盖清空路径，非空跑。

## 三、P3-1 / P3-2 真修核验（陈述修正）

- **P3-1**（`src/lib/related.ts:6-7`，Read 实测）：注释改为「标签取 `articles.tags` 去规范化 JSON 列（B2 创建文章即填充，与 `article_tags` 关联表回填状态无关）」；`docs/node-backend/B7-NOTES.md` §二.3 同步修正。`related` 读 denormalized 列、逻辑本就正确，仅事实陈述有误，已校正。
- **P3-2**（`docs/node-backend/B7-NOTES.md` §一）：`getAdminSiteSettings` → `adminGetSiteSettings`（对齐契约 3124 行 operationId）。代码不绑定 operationId 字符串，无功能影响。

## 四、独立复验证据

| 门禁 | 结果 | 说明 |
|---|---|---|
| `tsc --noEmit` | ✅ 0 error | 独立复跑 |
| `biome check .` | ✅ **93 文件** 0 error / 0 warning | 回复记 92 为笔误，实质 0 成立 |
| `vitest run` | ✅ **126 passed**（17 文件） | 标准 5s 超时下前次因负载误报 95 失败；高超时串行复跑 **126/126 全绿**，证伪环境干扰 |
| 契约结构门 `openapi-spec-validator` | ✅ `openapi.v1.yaml: OK` | 字节级未改 |
| 契约语义门 `check_contract.py` | ✅ 全部通过（33 OK） | 字节级未改 |

- **改动集**：commit `0d498a7`（fix(site-settings): 补全 logoUrl 入参 nullable 支持并加清空测试）。仅含 `src/routes/site.ts`(79) / `src/lib/related.ts`(118) / `test/routes/aux.test.ts` / `docs/*`；`openapi.v1.yaml` **不在改动集**（契约零回归）。
- **行数诚实**：site 79 / related 118，与自报逐字吻合。

## 五、非阻塞边角（不阻放行）

1. P3-3/P3-4/P3-5（MemberProfile.articles 可选未返回、toc 锚点边界、related 同 publishedAt 无 tie-breaker）开发 AI 选择留作已知可选/后续，与首轮「暂不阻塞」裁定一致，本次未改，可接受。
2. 开发 AI 回复门禁表中 `biome 92 文件` 与本复批实测 `93 文件` 差 1 —— 纯计数笔误，0 issue 实质成立，不计入诚信问题。
3. 首轮复批提及的「FK `[unhandled] SqliteError: FOREIGN KEY constraint failed`」日志，经排查是测试超时中断时未完成的异步 DB 操作抛出的**副产品噪声**，随超时消除而消失，并非真实数据缺陷或回归信号。

## 六、M1 后端代码冻结裁定

B7 是 M1 后端收尾批次。自 B0 起八批（B0 代码框架 + B1 认证、B2 文章、B3 分类标签、B3.5 标签回填、B4 评论、B5 用户上传、B6 收藏历史点赞通知、B7 辅助站点）全部通过总把控独立复验、**零假修复**。

**裁定：M1 后端代码（node-backend）即日起冻结**，进入「写 M1 后端文章」阶段（由总把控执笔，按 `docs/prd/M1-后端实现计划.md` 的节奏产出）。

交付物：
- `docs/node-backend/review/B7-代码审阅-第二轮复审批复.md`（本报告）
- `docs/node-backend/review/REVIEW-LOG.md`（追加「B7 第二轮复批」思考节，含环境误报根因复盘）
- `.workbuddy/memory/2026-08-26.md`（追加复批记录）
