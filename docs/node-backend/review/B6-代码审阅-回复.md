# M1 后端 · 批次 B6：收藏 / 历史 / 点赞 / 通知 — 代码审阅回复

> 回复方：开发 AI｜日期：2026-08-26
> 对应审阅：`docs/node-backend/review/B6-后端代码审阅报告.md`
> 裁定回顾：B6 通过，放行 B7；建议先清零 **P2-1**（likeArticle 并发 500 + like_count 漂移）。

## 一、总览

| 项 | 结果 |
|---|---|
| 复审裁定 | 采纳：P2-1 已清零，P3-1 已同批修复；P3-2 并入 P2-1；P3-3 / P3-4 / P3-5 非阻塞、无代码改动 |
| 改动文件 | `src/routes/likes.ts`、`src/routes/history.ts`、`test/routes/interactions.test.ts` |
| 契约 | ❌ 未改动（冻结 v1.11.0，沿用零回归基线） |
| 门禁 | ✅ tsc 0 / biome 0（80 文件）/ vitest **117 passed** / 契约双门 OK |

## 二、P2-1（已清零）：likeArticle 并发 500 + like_count 漂移

### 根因
`likeArticle`（契约 `x-idempotent:true`，行 3331）原实现为「先查 `if(!existing)` → 裸 `insert` → 应用层读 `article.likeCount` 再 `+1`」：
- **并发双发**（`POST /articles/{id}/like` 重叠在途）：两个请求都先读到"无记录"，第二个 `insert` 撞 `uniq_like` 唯一约束 → `SQLITE_CONSTRAINT_UNIQUE` → 全局错误处理器 → **500**，违背 `x-idempotent` 机器字段承诺的 200。
- **计数漂移**：`like_count` 用「读值再 +1」的应用层 read-modify-write，并发下可重复 +1（一次真实点赞变 +2）；`unlikeArticle` 同理（虽已 `Math.max(0,…)` 防负，但读值仍陈旧）。

同批 `addFavorite` 已用 `onConflictDoNothing`（真幂等），`likes` 漏用属同批内不一致。

### 修复（`src/routes/likes.ts`）
对齐 `favorites` 写法，将幂等与计数一致都下沉到 DB 层：
```ts
// likeArticle
const res = await db
  .insert(likes)
  .values({ userId, articleId, createdAt: new Date() })
  .onConflictDoNothing()
  .run();
if (res.changes > 0) {
  await db
    .update(articles)
    .set({ likeCount: sql`like_count + 1` })
    .where(eq(articles.id, articleId))
    .run();
}

// unlikeArticle（对称，原子下限夹 0）
if (existing) {
  await db.delete(likes).where(eq(likes.id, existing.id)).run();
  await db
    .update(articles)
    .set({ likeCount: sql`CASE WHEN like_count > 0 THEN like_count - 1 ELSE 0 END` })
    .where(eq(articles.id, articleId))
    .run();
}
```
- `onConflictDoNothing()` 保证并发双发不再撞约束 500，`res.changes > 0` 才 +1，计数与行数严格一致。
- `sql\`like_count + 1\`` / `CASE WHEN … THEN … - 1 ELSE 0 END` 原子增减，彻底消除读改写竞态。

### 证据
- 新增回归测试「并发双发 POST /like 不 500（x-idempotent 真幂等，P2-1 修复）」：用 `Promise.all` 同时起飞两个 `POST /like`，断言两者均 200、`likeCount` 仍为 1、DB 仅 1 条 like 行。该测试在旧实现下第二个请求必 500（可复现），新实现稳定通过。
- 稳定性：连跑 3 次均 ✅。
- 全量 `vitest run`：**117 passed**（含 B6 行为级 9 例 + 存量 108）。

## 三、P3-1（已同批修复）：reportReadingProgress upsert 竞态

### 根因
`history.ts:reportReadingProgress` 原「先查 `existing` → 再 `insert`」，与 likeArticle 同源 read-then-insert；该端点契约未标 `x-idempotent`，故非契约违反，但并发双发同 `(user,article)` 会撞 `uniq_view_history` → 500。

### 修复（`src/routes/history.ts`）
改为 DB 层 upsert（`onConflictDoUpdate`），冲突时仅更新 `lastReadAt`，`progress` 仅在请求显式携带时才覆盖（保留原"progress 仅携带时覆盖"语义）：
```ts
const onConflictSet: { lastReadAt: Date; progress?: number | null } = { lastReadAt: now };
if (progress !== undefined) onConflictSet.progress = progress;
await db
  .insert(viewHistory)
  .values({ userId, articleId, lastReadAt: now, progress: progress ?? null })
  .onConflictDoUpdate({
    target: [viewHistory.userId, viewHistory.articleId],
    set: onConflictSet,
  })
  .run();
```
- 冲突目标 `uniq_view_history(userId, articleId)` 与 schema 唯一索引一致。
- 行为级测试「上报 upsert 不重复，仅更新 lastReadAt」原 `progress:10 → 80` 仍得 80，✅ 全绿。

## 四、P3-2 / P3-3 / P3-4 / P3-5（非阻塞，已登记）

- **P3-2（like_count 应用层维护漂移）**：已并入 P2-1 修复（原子 `±1`），不再有漂移。
- **P3-3（`GET /me/likes` 契约内部不一致）**：代码严格按契约返回裸数组、仍用 limit/offset 截断——正确。属契约文档问题，按审阅建议留**契约维护批次**统一处理（呼应 B5 复批 N 项提醒）。本批不碰契约。
- **P3-4（`schema.ts` 336 行 > 200 软上限）**：该文件是"全部表单一事实源"，B2–B5 各批均增量加表、历次审阅未列 P 级；为避免跨文件 import 大面积重构风险，本批保持单文件，沿用既往先例。如总把控判定需拆，可单独立项 `schema/*.ts`。
- **P3-5（NOTES §六 自陈与契约不符，benign）**：`B6-NOTES.md` 原称 clearMyHistory / removeHistoryItem 也标 `x-idempotent`，但契约实测仅 `addFavorite`/`removeFavorite`/`likeArticle`/`unlikeArticle` 标 `x-idempotent:true`；`clearMyHistory`/`removeHistoryItem` 未标（DELETE 天然幂等、代码行为正确）。本回复已据实测契约纠正，**代码行为未被带偏**；自陈偏差将在契约维护批次一并核对。

## 五、门禁独立复验（全绿）

| 门禁 | 结果 |
|---|---|
| `tsc --noEmit` | ✅ 0 error |
| `biome check .` | ✅ 0 error / 0 warning（**80** 文件，+1 为本回复新增的并发测试） |
| `vitest run` | ✅ **117 passed**（16 文件） |
| 契约结构门 `openapi-spec-validator` | ✅ `openapi.v1.yaml: OK` |
| 契约语义门 `check_contract.py` | ✅ 全部通过（未改动契约，零回归） |
| 契约是否被改 | ✅ `git diff` 不含 `openapi.v1.yaml` |

## 六、改动文件清单

| 文件 | 改动 |
|---|---|
| `src/routes/likes.ts` | `likeArticle` 改 `onConflictDoNothing` + 原子 `like_count + 1`；`unlikeArticle` 改原子 `CASE WHEN … - 1 ELSE 0`；同步 `sql` 导入与文件头注释 |
| `src/routes/history.ts` | `reportReadingProgress` 改 `insert … onConflictDoUpdate`（DB 层 upsert） |
| `test/routes/interactions.test.ts` | 新增并发双发回归测试（P2-1），共 9 例 B6 行为级 |

## 七、下一步

B6 全部实质性问题（P2-1 / P3-1 / P3-2）已清零，门禁全绿、契约零回归。按裁定**放行 B7（辅助接口 / 站点，按 `07-*.md` 任务包）**。P3-3 / P3-5 两处契约侧登记项，建议随契约维护批次统一处理，不在本批次代码中动契约。
