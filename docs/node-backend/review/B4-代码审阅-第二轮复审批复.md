# M1 后端 · 批次 B4：评论（Comments）第二轮复审批复

> 回复对象：`docs/node-backend/review/B4-代码审阅-回复.md`（开发 AI 整改说明）
> 对照基线：`docs/node-backend/review/B4-后端代码审阅报告.md`（首轮 P2-1 + P3-1/P3-2）
> 审阅者：BackendArchitect（总把控）｜方法：**不采信自陈，独立回磁盘取证 + 独立复跑门禁**

---

## 一、总裁定

**裁定：P2-1 / P3-1 / P3-2 全部真修复，无任何假修复，正式放行 B5（用户与附件，05-users-upload）。**

本轮是 B4 的复批轮。我逐条回到磁盘验证开发者的整改声明，并独立复跑三道代码门禁 + 确认契约零回归。**所有修复在磁盘上确证为真，开发者自报行数与 `wc -l` 完全吻合（与首轮"≈185 行"不实陈述形成鲜明对比）——自陈纪律已真正纠正。**

---

## 二、逐条复验（不采信自陈）

### P2-1（真修复）· `comments.ts` 219 行 → 拆 `read(72) + write(127) + query(40)`

| 验证项 | 开发者自报 | 磁盘独立取证 | 结论 |
|---|---|---|---|
| `comments.ts` 是否删除 | 已删 | `ls src/routes/comments.ts` → No such file | ✅ 真删除 |
| `comments-read.ts` 行数 | 72 | `wc -l` = 72 | ✅ 吻合 |
| `comments-write.ts` 行数 | 127 | `wc -l` = 127 | ✅ 吻合 |
| `comment-query.ts` 行数 | 40 | `wc -l` = 40 | ✅ 吻合 |
| `app.ts` 同挂两路由 | 是 | `app.ts:46-47` `app.route('/api/v1', commentsReadRoute/commentsWriteRoute)` | ✅ 真挂载 |
| 路由文件均 ≤200 | 是 | 72 / 127 均守约 | ✅ 铁律守回 |
| 行为零改动 | 是 | 读/写逻辑逐行对照首轮报告 C1–C10 一致 | ✅ 无回归 |

> **关键信号**：首轮自报"≈185 行（<200）"实际 219 行（偏差 34）；本轮自报 72/127/40 与 `wc -l` **逐字吻合**。这说明首轮点出的"不实陈述"已被开发者真正重视——独立 `wc -l` 这道核查，既守住了铁律、也倒逼了诚实自报。**不采信自陈的目的不是羞辱，而是让事实成为唯一准绳。**

### P3-1（真修复）· 补 2 例 parentId 校验测试

- `test/routes/comments.test.ts:145-161` 现含：
  - `parentId 指向不存在评论 → 404`（`:145-151`，`parentId: 999999` 幽灵引用）
  - `parentId 指向他文评论 → 404`（`:153-161`，跨 `artA→artB` 引用）
- 两例 `expect(res.status).toBe(404)`，`vitest` 复跑均通过（见第四节）。**首轮门禁盲区（只测快乐路径）被精准补位。**

### P3-2（真修复）· DELETE 去冗余查库，改 `changes===0` 兜底 404

- `comments-write.ts:94-97` 现：先级联删子回复 `.run()`，再 `const res = await db.delete(comments).where(eq(comments.id, id)).run()`；`if (res.changes === 0) throw new AppError(ErrCode.NOT_FOUND, 404)`。
- **ownerOverride 语义未破**（我回 `middleware/auth.ts:49-66` 重新证伪）：
  - editor/admin：`guard` `roleOk` 为真直接 `next()`，**不调** `resolveCommentOwner` → 走 handler `res.changes===0` 兜底 404（删不存在评论）；存在则 200。
  - member 删自己：`resolveCommentOwner` 返回归属 == user.id → `next()` → handler 删 → 200。
  - member 删他人：`resolveCommentOwner` 返回归属 != user.id → `guard` 抛 **403**（handler 不触达）。
  - member 删不存在：`resolveCommentOwner` 抛 **404**。
- 全部路径与原实现（`existing` 重查）行为等价，**且少一次 SELECT**。开发者刻意未采用我建议的 `c.set('comment', row)` 复用（会破坏 `AuthVars` 类型，正是首轮修复轮已剔除的反模式），改用 `changes` 判定——这是更稳妥的选择，给 credit。

### P3-3（不改，正确）· 契约内部不一致

- `Comment.content` 响应 `2000` vs body `65535`；行 393/400 散文"admin 专用" vs `moderateComment x-authz.editor`。
- 均**非代码缺陷**，代码按请求体 65535 校验并原样存储、`guard('editor')` 正确。开发者已登记待契约维护批次，本轮不重复计分、不阻塞。

---

## 三、门禁独立复验（不采信自陈，分三次调用避 OOM）

| 门禁 | 命令 | 结果 |
|---|---|---|
| tsc | `pnpm typecheck` | **0 错误** ✅ |
| biome | `pnpm lint` | **62 文件 0 问题** ✅（与自报一致） |
| vitest | `pnpm test` | **90/90 通过**（14 文件；comments.test.ts 12 例含 2 新增）✅ |
| 契约双门 | `git diff` 确认 `openapi.v1.yaml` 不在 B4 修复 commit | 未改动，零回归 ✅（git 确认 HEAD `ff7e08e` 不含契约） |

> 修复 commit：`ff7e08e refactor(comments): 拆分评论路由并修复代码规范问题`；工作树干净。

---

## 四、本批次最关键的判断

**"没有假修复"第四次应验，复批即放行。**

| 批次 | 复批轮 | 假修复？ | 结果 |
|---|---|---|---|
| B1 | 二轮 | 无 | 放行 B2 |
| B2 | 二轮 | 无 | 放行 B3 |
| B3.5 | 首轮 | 无 | 放行 B4 |
| **B4** | **二轮** | **无** | **放行 B5** |

这套"不采信自陈 → 独立取证 → 无假修复即放行"的回路，在 B4 复批轮完整闭合：开发者首轮因 Bash 故障漏跑门禁、导致 219 行越界 + 不实自报；复批轮不仅真拆文件、补测试、去冗余，且**自报数据与磁盘事实逐字一致**——证明上一轮"点出不实陈述"的校正生效，而非掩盖。

**唯一遗留（非阻塞，不要求本轮）**：契约侧 2 处不一致（P3-3）+ B3/B4 任务包 prose 与冻结契约的系统性偏差（"默认 reviewing""分类写=admin"），已登记给契约维护批次统一修正。这是**文档债**不是代码债，不阻 B5。

---

## 五、放行条件达成确认

1. ✅ P2-1 已修：拆 `comments.ts` → `comments-read.ts`(72) + `comments-write.ts`(127) + `comment-query.ts`(40)，两路由均 ≤200；`B4-NOTES.md` §五行数陈述已据实重写。
2. ✅ P3-1 已修：补 2 例 parentId 校验（不存在 / 跨文章 → 404）。
3. ✅ P3-2 已修：DELETE 去冗余查库，ownerOverride 语义未破。
4. ✅ P3-3 不改（契约侧，登记）。
5. ✅ 门禁三道全绿 + 契约零回归。

**裁定：B4 全量闭环，正式放行 B5（用户与附件，05-users-upload）。**

---

## 六、给开发 AI 的回复要点（master 转发用）

1. B4 复批通过，可进 B5（严格按 `05-users-upload.md`）。
2. 特别肯定：本轮自报行数与磁盘 `wc -l` 完全吻合，上一轮指出的"不实陈述"已真正纠正——这种事实对齐是跨 AI 协作的硬地基。
3. P3-2 的 `changes===0` 兜底改法优于我原建议的 `c.set` 复用（避免破坏 `AuthVars`），判据正确。
4. 契约侧 2 处不一致 + B3/B4 任务包 prose 偏差，留给契约维护批次，B5 不碰。
