# M1 后端 · 批次 B6：收藏 / 历史 / 点赞 / 通知 — 代码审阅第二轮复批

> 复批方：总把控（BackendArchitect）｜日期：2026-08-26
> 对象：`docs/node-backend/review/B6-代码审阅-回复.md`（开发 AI 修复说明）+ `B6-NOTES.md`（更新版）
> 首轮裁定：`docs/node-backend/review/B6-后端代码审阅报告.md` — 通过，放行 B7；建议先清零 P2-1。
> 纪律：不采信自陈。凡自述均回磁盘取证（Read 源码 / `wc -l` / `git diff` / 独立跑门禁 / grep 复核用例）。

## 一、总览裁定

**P2-1（likeArticle 并发 500 + like_count 漂移）、P3-1（reportReadingProgress upsert 竞态）均真修、无假修复；P3-2 已并入 P2-1；P3-3/P3-4/P3-5 仅登记无代码改动。维持首轮裁定：放行 B7。**

注意：本次修复是**工作树未提交改动**（最新 commit 仍为 `d7662a0`，fix 落在 `likes.ts` / `history.ts` / `interactions.test.ts` 三个 modified 文件）。复批基于工作树当前状态，建议开发 AI 修复确认后单独 commit（不混入其他批次）。

## 二、独立复验证据（分四次跑门禁，vitest 独占避 OOM）

| 证据 | 结果 | 说明 |
|---|---|---|
| `wc -l` 行数 | likes 163 / history 146 / favorites 103 / notifications 108 | 与首轮审阅逐字吻合（history 原 152 现 146，因删 read-then-insert 分支）；均 ≤200 |
| `git diff --stat src/ test/` | 3 文件 +61/-37 | 仅 `likes.ts`/`history.ts`/`interactions.test.ts`，与回复文件清单一致 |
| `tsc --noEmit` | ✅ 0 error | — |
| `biome check .` | ✅ 81 文件 0 error / 0 warning | 回复记 80 是笔误（interactions.test.ts 计数 +1），实质 0 issue 成立 |
| `vitest run` | ✅ **117 passed**（16 文件） | 含 B6 行为级 9 例（新增 1 并发例），与回复逐字吻合 |
| 契约结构门 | ✅ `openapi.v1.yaml: OK` | venv 重跑 |
| 契约语义门 | ✅ 全部通过 | 结构+operationId+孤儿实体+死胡同状态+机器强制约束+错误码+R1/R5/N5/N2 全绿 |
| 契约字节级 | ✅ `git status` 不含 `openapi.v1.yaml` | 冻结 v1.11.0 零回归 |

## 三、逐项真修核验（读 file:line，非仅测试绿）

### P2-1 — likeArticle 真幂等（file:line 实证）
- `src/routes/likes.ts:63-75` `likeArticle`：`insert(likes).values(...).onConflictDoNothing().run()`，随后 `if (res.changes > 0)` 才 `update(articles).set({ likeCount: sql\`like_count + 1\` })`。
  - **并发双发**：第二个请求撞 `uniq_like` 时被 `onConflictDoNothing` 吞掉 → 不再 500，满足契约 `x-idempotent:true`（行 3331）承诺的 200。
  - **计数漂移**：`+1` 走原子 SQL、且仅当本请求确实新增行时才执行，彻底消除「读值再 +1」竞态。
- `src/routes/likes.ts:102-109` `unlikeArticle`：删行后 `update(articles).set({ likeCount: sql\`CASE WHEN like_count > 0 THEN like_count - 1 ELSE 0 END\` })` —— 原子下限夹 0，并发陈旧读不漂移。
- `sql` 已导入（likes.ts:9）。✅

### P3-1 — reportReadingProgress 真 upsert（file:line 实证）
- `src/routes/history.ts:100-109`：改 `insert(viewHistory).values(...).onConflictDoUpdate({ target: [userId, articleId], set: onConflictSet })`；`progress` 仅在请求显式携带（`progress !== undefined`）时进 `onConflictSet`，保留「progress 仅携带时覆盖」语义。
- 冲突目标 `uniq_view_history(userId, articleId)` 与 schema 唯一索引一致，并发双发不再撞约束 500。✅

### P3-2 — like_count 漂移
已并入 P2-1（原子 `±1`），不复存在。✅

### P3-3 / P3-4 / P3-5 — 仅登记，无代码改动
- P3-3（`GET /me/likes` 契约内部不一致）：代码严格按契约返裸数组 + limit/offset 截断，正确；留契约维护批次。
- P3-4（`schema.ts` 336 行 >200）：全表单一事实源，B2–B5 先例未列 P，沿用。
- P3-5（NOTES §六 自陈偏差）：回复已据实测契约纠正（clearMyHistory/removeHistoryItem 未标 x-idempotent），代码行为未被带偏。善意、非缺陷。

## 四、新增并发回归测试复核（grep 实证非空跑）

`test/routes/interactions.test.ts:314-342`「并发双发 POST /like 不 500（x-idempotent 真幂等，P2-1 修复）」：
- `Promise.all([fire(), fire()])` 同时起飞两个 `POST /like`；
- 断言 `a.status 200` + `b.status 200` + `status.data` 为 `{ liked: true, likeCount: 1 }` + DB `rows.toHaveLength(1)`。
- 该测试在旧实现（裸 insert + 先查判存）下第二个请求必 500，新实现稳定通过 —— **真实逻辑覆盖，非空跑**。

## 五、判定与放行

- 修复满足「无假修复即放行」信号：P2-1/P3-1 读 file:line 实证、门禁全绿、契约零回归、新增测试非空跑。
- 唯一轻微笔误：biome 文件数 80→81（无害，门禁以工具实际输出为准）。
- **裁定：B6 全部实质性问题清零，维持放行 B7（辅助接口 / 站点，8 端点，按 `07-*.md`）。**

## 六、给接手 AI / 下一代审阅的提示

1. **x-idempotent 同 x-authz 是第 4 铁律机器字段**：不能只看「顺序单测绿」，必须追问实现是否真幂等（DB 层 `onConflict*` / 原子更新）。本批 `favorites` 与 `likes` 同构写法并存一度暴露不一致，已通过复批统一。
2. **未提交修复**：复批基于工作树未提交状态，开发 AI 修复确认后请单独 commit（消息如 `fix(backend): B6 P2-1/P3-1 幂等与 upsert 竞态`）。
3. **契约侧登记项**（P3-3、P3-5 自陈偏差）建议随契约维护批次统一处理，不在后续代码批次中动冻结契约。
4. **元认知**：这是本生命周期第 **6** 次「无假修复即放行」（B1-2/B2-2/B3.5/B4-2/B5-2/B6-2）。开发 AI 自 B4 复批后已稳定内化「自陈须与磁盘一致」纪律，可沉淀为 M1 后端审阅 skill。

## 七、交付物
- 本报告：`docs/node-backend/review/B6-代码审阅-第二轮复审批复.md`
- 更新：`REVIEW-LOG.md` 追加「B6 第二轮复批」思考节；`.workbuddy/memory/2026-08-26.md` 追加复批记录。
- 状态：B0→B6 七批全收口、零假修复，正式进入 **B7（辅助接口 / 站点，8 端点）** 收尾段。
