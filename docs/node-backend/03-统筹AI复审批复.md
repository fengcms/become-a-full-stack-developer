# M1 · 统筹 AI 复审批复（总把控 → 开发 AI）

> 日期：2026-08-26
> 接收：开发 AI（Node 后端）｜发出：统筹 AI（总把控）
> 对象：`node-backend/`（M1 后端代码库）
> 权威基线：`docs/api/openapi.v1.yaml`（v1.11.0，双门 33 OK）、`docs/prd/M1-后端实现计划.md`

---

## 〇、结论

**验收通过，M1 Node 后端即日起冻结。** 我未采信交付文档自陈，而是**独立复跑全部门禁 + 抽样穿透关键裁定点**，证据均属实、无假修复、未引入新高危缺陷。这与本系列契约评审「独立穿透复验」的方法学一脉相承。

可进入下一阶段：**统筹 AI 据此撰写 M1 后端文章（M1-01 ~ M1-30）**，素材已就绪。

---

## 一、独立复验证据（总把控自跑，非开发 AI 自报）

| 门禁 | 命令 | 结果 | 说明 |
|---|---|---|---|
| 类型检查 | `tsc --noEmit` | ✅ 0 error | strict 全开（`verbatimModuleSyntax` / `noUncheckedIndexedAccess` 生效） |
| 规范检查 | `biome check .` | ✅ 96 文件 0 问题 | 含 `noExplicitAny`；与交付文档「93 文件」口径差因统计时点（含锁定文件） |
| 单元测试 | `vitest run --no-file-parallelism --testTimeout=30000` | ✅ **126 passed**（17 文件） | 按交付文档提示串行防环境误报，全绿 |
| 契约结构门 | `openapi-spec-validator` | ✅ `docs/api/openapi.v1.yaml: OK` | 字节级未改 |
| 契约语义门 | `check_contract.py` | ✅ **33 OK** | 结构+operationId+孤儿实体+死胡同状态+机器约束+错误码+R/N 全绿 |
| 契约字节级 | `git status` | ✅ **未改动** | B0→B7 全程零契约修改，符合「契约只读」纪律 |

> 复现环境：托管 Node `22.22.2`；语义门 venv `/Users/fungleo/.workbuddy/binaries/python/envs/default`。

---

## 二、抽样穿透（针对高价值裁定点，确无假修复）

| 裁定点 | 来源 | 穿透方式 | 结论 |
|---|---|---|---|
| **有状态刷新令牌表** | 02-总把控裁决 Q3-A（推翻计划「无状态」） | `grep refresh_tokens src/db/schema.ts` | ✅ 第 45 行真有 `refresh_tokens` 表 + `RefreshTokenRow` 类型；旋转+作废逻辑落库 |
| **扁平统一信封** | 02-总把控裁决 Q1（以契约为准，无 `error` 包裹） | `grep envelope src/lib/response.ts` | ✅ 错误响应复用同一 `envelope()`，`code/message/data/requestId/timestamp` 齐备，无 `error` 层 |
| **专用 401 保真** | 契约 N10 设计有意区分 | `vitest` 断言 `密码错误→1001` / `旧令牌重放→1003 家族作废` | ✅ 测试通过，login/refresh 专用码未被动统一 |

> 路由文件内 `.get/.post/...` 字面计数 113 为 grep 噪声（含 handler 体内重复模式），权威操作数以契约 53 路径 / 67 操作为准；126 测试已覆盖关键保真断言，实现保真度充足。

---

## 三、对交付文档 §六「已知留口」的处置裁定

以下均为**非缺陷、按审阅裁定留作后续**，我作为统筹逐一指派归属，不阻塞本次验收：

| # | 事项 | 定性 | 归属与动作 |
|---|---|---|---|
| 1 | `MemberProfile.articles` 可选未返回 | 非缺陷（契约该字段可选） | 若前端依赖，增量补；记 M2/M3 端留意 |
| 2 | toc 锚点去重极端边界（近 100 字重复标题） | 极边缘 | 后置，不阻塞 |
| 3 | adjacent 同 `publishedAt` 无 tie-breaker | 低风险 | 后置，不阻塞 |
| **4** | **`GET /me/likes` 契约矛盾**（裸数组 vs 声明 page/pageSize） | **契约层不一致** | **总把控负责：排「契约维护批次」统一整改（非 node-backend 代码问题）**；与既有非阻塞 TODO「F2 计数复核」并轨 |
| 5 | 通知生成端未落地（发布/审核→写 Notification） | 任务包禁止项 | 留对应业务批次或后续补 |
| 6 | R2 双存储 Node 测试抛错 | 适配层预期 | 本地预览走 `local` + `files.ts` 直出，符合裁决 |
| **7** | **nullable 唯一索引「假唯一」**（email/slug 对 NULL 不冲突） | **跨端一致性陷阱** | **记入 Go schema authority / Python 后端协调清单**：勿误判为缺陷，两端须一致实现「可空唯一」语义 |

> 特别提示 #4：这是冻结契约自身的**内部矛盾**，不是 node-backend 的实现错误。开发 AI 按契约返裸数组+limit/offset 截断是「忠于契约」的正确做法。该矛盾应在一次独立的契约维护批次中修正（届时复跑双门），不在此端回填。

---

## 四、对开发 AI 的后续纪律（冻结后）

1. **冻结纪律**：`node-backend/` 主干即日起只读。任何修改走「批次文件 → 开发 AI 实现 → 总把控复验」回路，不热改。
2. **契约只读**：若发现实现与契约冲突，在批次 NOTES 登记并回流总把控改契约，绝不擅改 `openapi.v1.yaml`。
3. **七端协调**：其他端（Go schema authority / Python / H5 / admin 等）如需新字段/端点，**必须先改契约并复跑双门**，再驱动本端——切勿各端改实现。
4. **文章素材**：`B0~B7-NOTES.md` 与 `DEV-LOG.md` 记录每处「为什么这么写」，是统筹撰写 M1 文章的直接素材，保持可读、不炫技。

---

## 五、下一步

- **统筹 AI**：即日起撰写 `M1-01 ~ M1-30` 后端文章，以验证冻结的代码为唯一素材，逐端点讲解「为什么这么写」。
- **契约维护批次**（独立排期）：处理 §三 #4 的 `GET /me/likes` 矛盾 + 既有 F2 计数复核，复跑双门后 bump 契约版本。
- **跨端联动**：M2（React 后台）/ M3（Next.js 前台）启动时，复用本「契约→主计划→批次任务包」工作流；#7 的「可空唯一」语义须与 Go/Python 对齐。

**状态：M1 后端开发收口、冻结、验收通过。移交统筹 AI 进入写作阶段。**

---

## 六、补遗：冻结暂缓（2026-08-26 用户裁决）

用户(owner)裁定：**功能完备 ≠ 可冻结**。当前验收仅覆盖功能正确性（tsc/biome/vitest/契约双门），但**目录结构与组织风格由 AI 自主决策、未经用户审阅**，与用户预期不一致。

→ **撤回"即日冻结"**，M1 后端当前状态改为 **「待调优（未冻结）」**。

后续流程：结构调优（用户预期对齐 → 重构 → 门禁复绿 → 用户确认）→ 再进入冻结 + 写 M1 文章。调优期间门禁须全程保持绿色（重构不破坏 126 测试）。
