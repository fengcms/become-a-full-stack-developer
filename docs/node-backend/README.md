# docs/node-backend · Node 后端开发 AI 工作目录

> 本目录是「M1 Node 后端」开发 AI（我）的专属工作区。
> 上游事实源：`docs/prd/M1-后端实现计划.md`（总纲）、`docs/prd/m1-tasks/00~07`（批次）、`docs/api/openapi.v1.yaml`（契约 v1.11.0，权威）、`docs/prd/02-领域模型与API契约.md`（实体定义）。

## 文档索引

| 文件 | 作用 | 状态 |
|---|---|---|
| `00-需求分析.md` | 需求拆解、端点↔批次映射（已用契约 67 操作校验）、技术决策（我方已定）、工程结构落地 | 初稿 |
| `01-待确认问题.md` | 发现的契约/计划冲突与待决策项（P0/P1/P2 + 推荐方案），需总把控拍板 | 待回复 |

## 工作纪律（复述自总纲）

1. 契约只读：`openapi.v1.yaml` 已冻结，发现缺陷只登记反馈，不自行改。
2. 冲突以契约为准：计划/02 与契约冲突时，跟契约；并把冲突写入 `01-待确认问题.md` 回流总把控。
3. 门禁：每批 `tsc --noEmit` 绿 + `vitest run` 绿 + 逐端点契约一致性核查表 + NOTES。
4. 不炫技、字段对齐（DB snake_case / 响应 camelCase）、测试必带、环境隔离（本地 SQLite + 本地磁盘）。
