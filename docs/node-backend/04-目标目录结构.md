# M1 后端 · 目标目录结构（结构调优规范）

> 版本：v1.0 · 2026-08-26
> 决策：采用**分层式（水平分层）**方案（routes / services / shared / types / db / middleware / config）。
> 定位：本文件是结构调优的唯一事实源，重构 PR 须与此一致；任何结构改动先改本文件再改代码。
> 纪律：重构全程保持 `tsc --noEmit` 0 error、`biome check` 0 问题、`vitest` **133 passed**（当前实测值，原 126 为调优期基线，后续增测后更新）、契约双门 **33 OK** 全绿。

---

## 1. 目标结构

```
src/
├── config/                      # 不变：env 等
├── db/                          # 不变：client(getDb)/schema/migrate
│   ├── client.ts                # getDb()/setDb()/createLocalDb()/createD1Db() —— 适配层，不动
│   ├── schema.ts
│   └── migrate.ts
├── middleware/                  # 不变：auth/cors/error/validate
├── routes/                      # 薄路由：校验 → 调恰好一个 service → 返回（文件按资源分，保持现状）
│   ├── articles-read.ts
│   ├── articles-write.ts
│   ├── articles-admin.ts
│   ├── comments-read.ts
│   ├── comments-write.ts
│   ├── categories-read.ts
│   ├── categories-write.ts
│   ├── users.ts / users-admin.ts
│   ├── auth.ts / me.ts / members.ts
│   ├── likes.ts / favorites.ts / history.ts / notifications.ts
│   ├── tags.ts / site.ts / upload.ts / files.ts / aux.ts / health.ts
│   └── index 聚合在 app.ts / worker.ts（不变）
├── services/                    # 领域服务：业务规则 + 所有 DB 查询（可调用 getDb/shared/其它 service/types）
│   ├── article.ts               # queryArticles / 状态机 / 序列化（原 lib/article.ts）
│   ├── article-mutation.ts      # create/update（原 lib/article-mutation.ts）
│   ├── article-tags.ts          # 标签关联（原 lib/article-tags.ts）
│   ├── article-backfill.ts      # 回填脚本逻辑（原 lib/article-backfill.ts）
│   ├── attachment.ts            # 附件（原 lib/attachment.ts）
│   ├── category.ts              # 分类树（原 lib/category.ts）
│   ├── comment.ts               # 评论 + 状态机（原 lib/comment.ts）
│   ├── comment-query.ts         # 评论查询（原 lib/comment-query.ts）
│   ├── refresh.ts               # 刷新令牌（含 DB，原 lib/refresh.ts）
│   ├── related.ts               # 相关文章（原 lib/related.ts）
│   ├── search.ts                # 会员搜索（原 lib/search.ts）
│   ├── stats.ts                 # 站点统计（原 lib/stats.ts）
│   ├── tag.ts                   # 标签（原 lib/tag.ts）
│   └── user.ts                  # 用户（原 lib/user.ts）
├── shared/                      # 基础设施/纯工具：无领域概念、不依赖 services（可被 routes/services/types 引用）
│   ├── codes.ts                 # 错误码表（原 lib/codes.ts）
│   ├── errors.ts                # AppError（原 lib/http-error.ts）
│   ├── db-error.ts              # 唯一约束判定（原 lib/db-error.ts）
│   ├── auth.ts                  # jwt + Role + AccessToken（原 lib/jwt.ts）
│   ├── pagination.ts            # 分页/排序（原 lib/pagination.ts）
│   ├── password.ts              # 密码哈希（原 lib/password.ts）
│   ├── response.ts              # 响应信封 ok/paginate（原 lib/response.ts）
│   ├── slug.ts                  # slug 工具（原 lib/slug.ts）
│   ├── storage.ts               # 存储驱动（原 lib/storage.ts）
│   └── toc.ts                   # 目录提取（原 lib/toc.ts）
└── types/                       # 仅跨模块共享类型（被 ≥2 方引用）；领域专属类型就近留在 service 文件
    ├── auth.ts                  # Role / AccessToken / AuthVars（Hono env 绑定，现散在 middleware/auth.ts + jwt.ts）
    └── common.ts                # ErrorCode / BizErrorCode / Pagination / 响应信封类型
```

---

## 2. 分层职责铁律

| 层 | 允许 | 禁止 |
|----|------|------|
| **routes/** | 输入校验（委托 `middleware/validate` + zod）；调用**恰好一个** service 函数；用 `shared/response` 格式化返回 | 出现 `.select/.insert/.update/.delete`、`getDb()`、业务规则、手工序列化 |
| **services/** | 领域逻辑 + 所有 DB 查询；调用 `getDb()`、`shared/*`、其它 service、`types/*` | 直接拼 HTTP 响应（返回领域对象，交给 routes 格式化） |
| **shared/** | 跨领域基础设施/纯工具；可被任意层引用 | 引入领域概念、依赖 `services/*`、直接查库（db-error 仅做异常判定不查） |
| **types/** | 仅放被 ≥2 方引用的共享类型 | 放领域专属类型（如 `ArticleStatus` 留在 `services/article.ts`） |
| **db/ middleware/ config/** | 不变 | —— |

---

## 3. 当前 lib → 新结构逐文件映射

| 当前文件 | 目标位置 | 归类依据 |
|----------|----------|----------|
| lib/article.ts | services/article.ts | 领域：文章查询/序列化/状态机 |
| lib/article-mutation.ts | services/article-mutation.ts | 领域：增改 |
| lib/article-tags.ts | services/article-tags.ts | 领域：标签关联 |
| lib/article-backfill.ts | services/article-backfill.ts | 领域：回填脚本逻辑 |
| lib/attachment.ts | services/attachment.ts | 领域：附件 |
| lib/category.ts | services/category.ts | 领域：分类树 |
| lib/comment.ts | services/comment.ts | 领域：评论 + 状态机 |
| lib/comment-query.ts | services/comment-query.ts | 领域：评论查询 |
| lib/refresh.ts | services/refresh.ts | 领域：刷新令牌（含 DB） |
| lib/related.ts | services/related.ts | 领域：相关文章 |
| lib/search.ts | services/search.ts | 领域：会员搜索 |
| lib/stats.ts | services/stats.ts | 领域：站点统计 |
| lib/tag.ts | services/tag.ts | 领域：标签 |
| lib/user.ts | services/user.ts | 领域：用户 |
| lib/codes.ts | shared/codes.ts | 基础设施：错误码表 |
| lib/http-error.ts | shared/errors.ts | 基础设施：AppError |
| lib/db-error.ts | shared/db-error.ts | 基础设施：唯一约束判定 |
| lib/jwt.ts | shared/auth.ts | 基础设施：JWT + Role + AccessToken |
| lib/pagination.ts | shared/pagination.ts | 基础设施：分页/排序 |
| lib/password.ts | shared/password.ts | 基础设施：密码哈希 |
| lib/response.ts | shared/response.ts | 基础设施：响应信封 |
| lib/slug.ts | shared/slug.ts | 基础设施：slug 工具 |
| lib/storage.ts | shared/storage.ts | 基础设施：存储驱动 |
| lib/toc.ts | shared/toc.ts | 基础设施：目录提取 |

> 注：`AuthVars`（Hono `c.get('user')` 的 env 绑定类型）现定义在 `middleware/auth.ts`，重构时上提到 `types/auth.ts`，`middleware/auth.ts` 改为从 `types/auth.ts` 引入。

---

## 4. 迁移原则与批次

- **零行为变更**：仅移动文件 + 调整 import 路径，不改任何逻辑；外部行为（HTTP 契约）不变 → 126 测试与契约双门天然保持绿。
- **按资源逐个下沉**：先把 routes 中的裸 DB 查询/业务逻辑下沉到对应 `services/*` 文件，routes 退化为薄路由。建议批次：
  1. **试点批（小资源）**：`likes` / `comments` / `favorites` / `history` —— 体量小、风险低，先跑通流程与门禁复绿。
  2. **主体批**：`articles-*` / `categories-*` / `users*` / `auth` / `tags` / `site` / `upload` / `members` / `notifications` / `search` / `related` / `stats`。
  3. **收尾批**：`lib/` 整体迁空并删除；`shared/` 更名收口；`types/` 抽取共享类型。
- **不动**：`db/`、`middleware/`、`config/`、`app.ts`/`worker.ts` 聚合方式。

---

## 5. 质量门（每批结束必跑）

```bash
pnpm typecheck      # tsc --noEmit 0 error
pnpm lint           # biome check . 0 问题
pnpm test           # vitest run → 133 passed（当前实测值）
# 契约双门（venv default）
python docs/api/check_contract.py   # 33 OK
openapi-spec-validator docs/api/openapi.v1.yaml  # OK
```

---

## 6. 风险与回滚

- 风险等级：**低**（纯目录重排，无逻辑改动）。
- 回滚：全程 git 可 diff；单批失败即 `git checkout` 该批，不影响其它批。
- 禁止：重构期不得借机"顺手"改逻辑/契约；如发现需改逻辑，单独提 issue，不在结构调优批内夹带。
