# API 契约专项审阅 · 终评结案报告

> 处置对象：磁盘真实 `docs/api/openapi.v1.yaml`（契约 **1.11.0**）+ `docs/prd/00/01/02/README`（文档 **v1.14**）
> 审阅者：**Backend Architect**（后端架构师）
> 方法：不采信回复自陈，独立重跑双门 + 自写穿透脚本（独立于作者 `check_contract.py`）逐条核验 N10 / N9-2 / 重复组件 / ErrorCode 枚举 / login 专用 401 / 残留 02 引用

---

## 0. 结论（一句话）

针对第四轮回复的独立复验**全部通过、无假修复、无新引入高危缺陷**。决定七端一致性的核心约束（授权求值 N1 / 401·403 线协议 N7 / ownerOverride 表征 N8 / 状态机 N9-2 / 错误码 F1 / 字段约束 N2 / 限流粒度 N5）已全部机器化且可机器校验。**评审正式终结，契约作为 M1 动手前的冻结基线。**

---

## 1. 独立核验证据（穿透脚本逐条）

| 声称修复点 | 核验方式 | 结果 |
|---|---|---|
| **N10**：46 端点 401 统一引用共享 `Unauthorized` | 遍历 46 个 x-authz 端点 responses.401 的 `$ref` | ✅ 46/46 引用 `#/components/responses/Unauthorized`；**0 内联残留、0 缺失** |
| login/refresh 专用 401 有意保留内联 | 查公开端点 401 code 集合 | ✅ `POST /auth/login`={1001,1005}、`POST /auth/refresh`={1002,1003,1004,1005}，均在 `ErrorCode` 枚举内，确为公开端点（不计入 46），与 N10 不冲突 |
| 重复 `Unauthorized` 组件已删 | 列 `components.responses` 同名 key | ✅ 现仅 1 个 `Unauthorized`（双 examples 确为 `token_missing:1004` / `token_invalid:1002`） |
| **N9-2**：`Article.status.x-allowed-transitions` 覆盖 §2.3 六边 | 解析 dict 列表 → `from->to` 集合比对 §2.3 | ✅ 6 条边完全吻合（缺口 0、多余 0） |
| `ArticleUpdate` 不含 status（避免绕过状态机） | 查 `ArticleUpdate.properties` | ✅ 无 `status` 字段，状态只经专用端点变更，与 x-authz 形成干净闭环 |
| 双门 33 OK 属实 | 重跑 + 数 OK 行 | ✅ 结构门 OK；语义门 33 条 OK（53 路径/67 操作/45 schema/46 x-authz/21 公开 429/13 枚举值） |
| `ErrorCode` 枚举 13 值 | 解析枚举 | ✅ `[0,1001..1005,2001,3001..3003,4001,5000,5001]`，非零 12 个，与回复"13 值错误码"一致 |
| 残留 02 引用 23 处 | 全文正则 | ✅ 与四审一致；行为约束溯源仅 §2.2/§3.3 两处（已登记非阻塞 TODO），**无授权/状态核心行为外置** |

---

## 2. 历次评审缺陷族闭环（F1 一脉彻底斩断）

```
F1  错误码只在散文        → v1.7 机器化 ErrorCode 枚举（一审清零）
R1/N1 授权求值只在散文+外置 → v1.9 第4铁律自包含 + x-authz（二审/三审清零）
N7  401 线协议不全         → v1.10 46 端点挂 401（三审清零）
N10 401 code 不统一        → v1.11 全 $ref 共享组件 + 语义门守护（四审清零）
N9-2 状态机只在散文        → v1.11 x-allowed-transitions 机器化（四审清零）
```

五层约束（错误码 / 授权求值 / 线协议 / 状态机 / 字段约束）现已**全部在契约内自包含且可机器校验**，不再依赖"读 02 + 自觉"。这正是"七端共享唯一地基"承诺在机器层面的兑现。

---

## 3. 冻结基线下的非阻塞 TODO（不阻 M1 动手，登记即可）

1. **§2.2 树环检测 / §3.3 阅读去重**：行为约束刻意留契约外，由其对应后端篇目（M1 分类 / M3 阅读量）在 PRD 层落地。与"文章是产品、代码是素材、契约只定接口边界"铁律一致。
2. **Comment 状态机未机器化（可选一致性观察，非缺陷）**：`Comment.status`（approved/rejected/reviewing）未配 `x-allowed-transitions`，N9-2 范围本就限定 Article §2.3；评论状态机与 §2.2/§3.3 同源，可一并归入 TODO 1 的 PRD 层处理，不构成权威模糊。
3. **F2 应急集计数复核**（33/35，三处引用对齐）——历次顺延。
4. **独立终审**：M1 动手前由非作者跑穿透式核验（呼应 N6 方法学预警，规避"作者自验盲区"）。
5. **OAuth 白名单**：`redirect` 白名单在 M3-09 显式声明（契约已留 501 占位）。
6. **M6-09**：Go 后端一致性校验增补"授权行为"断言（而非仅"声明一致"）。

---

## 4. 终审判定

**评审终结，契约可冻结为 M1 动手前基线（v1.11.0 / 文档 v1.14）。**

- 决定七端一致性的五层核心约束均已机器化且经双门（33 OK）硬校验。
- 第四轮回复**属实、无假修复、未引入新高危缺陷**，无需开启第五轮评审。
- 残留项均为已登记的非阻塞 TODO，按 §3 处理即可。

---

*附：本次独立核验所用穿透脚本核心逻辑——直接 `yaml.safe_load` 遍历 `paths` 提取每个操作的 `responses.401/403` 的 `$ref` 与 `code` 值集合、解析 `x-allowed-transitions` 的 `from->to` 列表、枚举 `components.responses` 同名 key，完全绕开作者脚本的断言实现，确保"声称已修复"由第三方逻辑确证。*
