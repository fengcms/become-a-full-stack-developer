# M2 React / Vite 管理后台 · 第五批文章审阅记录

> 审阅日期：2026-09-14  
> 审阅范围：M2-15～M2-18  
> 当前结论：四篇文章已完成，可进入发布准备。

## 一、文章与事实边界

| 编号 | 核心命题 | 状态 |
| --- | --- | --- |
| M2-15 | 异步 UI 必须表达任务数、防重入与部分失败 | 草稿中 |
| M2-16 | 管理能力与个人能力可以共享 SPA，但需分开建模 | 草稿中 |
| M2-17 | 全局配置需要基线、后果预览和缓存传播 | 草稿中 |
| M2-18 | 看板应从指标导向具体工作 | 草稿中 |

- M2-15 已对照 UploadScope、批量选择和失败明细，确认计数、查询作用域、顺序执行及失败项保留准确。
- M2-16 明确 `/profile` 仍嵌套在 RequireConsole 中，当前实际访问者是 editor/admin，没有沿用源码注释中“member 即可访问”的不准确结论。
- M2-17 已核对六个配置字段、表单基线、后台缓存直接更新和公开配置失效关系。
- M2-18 已核对待审查询 pageSize=1、近期文章排序、近期评论缺少服务端 sort 的限制，以及各区块独立错误态。

## 二、发布信息复审

- 四篇均包含 6 个 Tag、250 字以内简介、封面与正文配图提示词。
- 四篇均包含固定的“成为全栈专栏订阅”宣传图片。
- M2-11～14 遗漏的宣传图片也已补齐，并将该项加入后续自动检查。
- Markdown 围栏和发布辅助区标记完整，未发布内链继续保留占位符。

## 三、发布前仍需完成

- 生成并上传最终配图，替换正文图片占位符。
- 按发布顺序回填 M2 内链。
- 删除发布辅助区，并在 `ARTICLES.md` 回填 tag、CSDN 地址与发布状态。
