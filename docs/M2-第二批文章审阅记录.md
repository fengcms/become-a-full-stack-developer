# M2 React / Vite 管理后台 · 第二批文章审阅记录

> 审阅日期：2026-09-14  
> 审阅范围：M2-05～M2-07  
> 当前结论：三篇文章已完成，可进入 owner 审阅与发布准备。

## 一、文章与论证边界

| 编号 | 核心命题 | 纯汉字数 | 全文字符数 | 状态 |
| --- | --- | ---: | ---: | --- |
| M2-05 | 状态管理首先要判断所有权和生命周期 | 2174 | 5485 | 草稿中 |
| M2-06 | Query 的核心价值是描述远端事实的身份与失效关系 | 2444 | 5472 | 草稿中 |
| M2-07 | 登录、恢复、刷新、重放和退出必须构成会话闭环 | 2386 | 7150 | 草稿中 |

统计不包含发布辅助区。纯汉字数不计标点、英文和代码；全文字符数包含正文中的 Markdown 与代码。

三篇的职责已经分开：M2-05 回答状态放在哪里，M2-06 回答远端状态怎样保持同步，M2-07 回答认证会话怎样跨越页面刷新和令牌过期。相邻文章通过内链衔接，没有重复展开同一套实现。

## 二、技术事实复审

- M2-05 对照 `store/auth.ts`、`store/ui.ts`、`lib/queryClient.ts`、`useMe.ts` 与表单实现，确认 Query、Zustand、React Hook Form、URL 和组件状态的职责描述符合当前代码。
- M2-05 明确区分认证 user 会话快照与 `/me/profile` 远端资料；两者只接受同一次服务端成功结果，避免各自演化成两个真相源。
- M2-06 对照查询键工厂与文章、评论、站点、个人资料 hooks，确认 `invalidateQueries`、`setQueryData`、30 秒默认 staleTime、公开设置 10 分钟 staleTime 和 mutation 禁止自动重试的描述准确。
- M2-06 保留“不乐观插入评论回复”的真实案例：回复可能被后端判为 `rejected`，必须等待服务器状态后再提示和失效列表。
- M2-07 对照认证 store、请求核心、会话副作用、根组件和路由守卫，确认内存令牌、Cookie 启动恢复、boot 三态、单一刷新 Promise、一次重放和强制退出的闭环一致。
- M2-07 引用并发刷新测试作为证据：三个并发 401 共享一次 `/auth/refresh`，刷新成功后分别重放。

## 三、文风与发布信息复审

- 三篇均从真实故障或返工场景进入，再给出归因、实现、取舍和适用边界。
- 代码片段前后均承担解释任务，没有把源码按文件顺序改写成 API 说明书。
- 每篇包含 6 个 Tag；简介分别为 163、148、161 个字符，均不超过 250 字。
- M2-05、M2-06 各准备 2 张配图提示词，M2-07 准备 3 张；每张均注明插入位置、比例、节点文字和视觉约束。
- 已发布的 M1-12 使用真实 CSDN 链接；未发布的 M2 文章继续保留链接占位符。
- Markdown 代码围栏数量均为偶数，发布辅助区起止标记完整。

## 四、发布前仍需完成

- 根据提示词生成并上传最终配图，替换正文图片占位符。
- 按发布顺序逐篇回填已经上线的 M2 内链。
- 发布前再次对照当时源码，确认配置数字、Cookie 部署前提和错误码策略没有改变。
- 删除发布辅助区，并在 `ARTICLES.md` 回填 tag、CSDN 地址和发布状态。
