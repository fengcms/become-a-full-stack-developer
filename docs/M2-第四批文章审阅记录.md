# M2 React / Vite 管理后台 · 第四批文章审阅记录

> 审阅日期：2026-09-14  
> 审阅范围：M2-11～M2-14  
> 当前结论：四篇文章已完成，可进入 owner 审阅与发布准备。

## 一、文章与论证边界

| 编号 | 核心命题 | 纯汉字数 | 全文字符数 | 状态 |
| --- | --- | ---: | ---: | --- |
| M2-11 | 界面动作必须忠实表达文章状态机 | 2383 | 4860 | 草稿中 |
| M2-12 | 前端权限应以能力为判据并与后端安全边界分工 | 2245 | 4478 | 草稿中 |
| M2-13 | 审核界面应同时提供动作、上下文与失败恢复 | 2243 | 3906 | 草稿中 |
| M2-14 | 分类与标签界面要把关系约束转成操作条件 | 2228 | 4423 | 草稿中 |

统计不包含发布辅助区。四篇分别处理状态迁移、权限映射、审核决策和关系数据，没有将业务页面写成 CRUD 操作手册。

## 二、技术事实复审

- M2-11 对照文章编辑 hook、表单页与文章 mutations，确认普通保存保留原状态，发布与下架显式分离，新建成功转入编辑地址，成功响应统一重建页面基线。
- M2-11 明确当前管理后台只允许 editor/admin 进入，member 投稿入口尚不属于这个应用，避免为标题中的“投稿”虚构现有界面。
- M2-12 对照 `permission.ts`、`Can.tsx`、菜单和路由守卫，确认能力名称、角色等级、owner override 和三层入口描述准确；自锁保护被明确归为业务护栏。
- M2-13 对照评论审核、代回复和 Query hooks，确认三态语义、200 字理由、2000 字回复、敏感词拒绝保留输入及不做乐观插入符合实现。
- M2-14 对照分类树纯函数、递归节点、表单和标签列表，确认最大 4 级、父级反推、子树排除、祖先保留搜索、slug 正则和 articleCount 删除限制准确。
- M2-14 明确契约没有标签合并端点，也没有分类 articleCount，因此没有声称当前前端具备这两项能力。

## 三、文风与发布信息复审

- 四篇均从真实误操作或信息缺失进入，包含反例、实现证据、取舍和适用边界。
- 每篇包含 6 个 Tag；简介分别为 126、129、147、148 个字符，均不超过 250 字。
- M2-11～13 各有 2 张配图提示词，M2-14 有 3 张；均注明插入位置、比例与中文节点。
- M1-14、M1-15、M1-16、M1-25、M1-27 使用 `ARTICLES.md` 中的真实发布链接。
- Markdown 代码围栏、发布辅助区起止标记和文件索引均已检查。

## 四、发布前仍需完成

- 生成并上传最终配图，替换正文图片占位符。
- 按顺序发布并回填 M2 内链。
- 发布前重新核对角色能力、状态按钮、评论限制和分类最大深度。
- 删除发布辅助区，在 `ARTICLES.md` 回填 tag、CSDN 地址和发布状态。
