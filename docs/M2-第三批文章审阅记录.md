# M2 React / Vite 管理后台 · 第三批文章审阅记录

> 审阅日期：2026-09-14  
> 审阅范围：M2-08～M2-10  
> 当前结论：三篇文章已完成，可进入 owner 审阅与发布准备。

## 一、文章与论证边界

| 编号 | 核心命题 | 纯汉字数 | 全文字符数 | 状态 |
| --- | --- | ---: | ---: | --- |
| M2-08 | 列表查询状态应该由 URL 承载 | 2209 | 6564 | 草稿中 |
| M2-09 | 表单可靠性建立在明确的服务端基线与工作副本之上 | 2257 | 5926 | 草稿中 |
| M2-10 | Markdown 编辑器集成的核心难点是异步状态一致性 | 2374 | 6407 | 草稿中 |

统计不包含发布辅助区。纯汉字数不计标点、英文和代码；全文字符数包含正文中的 Markdown 与代码。

三篇的职责已经分开：M2-08 处理可恢复的列表查询上下文，M2-09 处理表单基线、校验和离开保护，M2-10 处理编辑器内部的异步上传、光标、布局与主题边界。

## 二、技术事实复审

- M2-08 对照 `useTableQuery.ts`、`useKeywordFilter.ts`、`useBatchSelection.ts`、文章列表和分页器，确认 URL 参数校验、筛选回第 1 页、300ms 防抖、返回位置、总页数夹正和当前查询范围选择的描述准确。
- M2-08 使用 M1-17 的真实标题与 CSDN 链接，不再沿用旧规划中的示例链接。
- M2-09 对照 `useArticleEditor.ts` 与 `UnsavedChanges.tsx`，确认一次回填、保存后重建基线、新建转编辑、错误面板定位、同步 ref 防重复提交和两层离开保护均与实现一致。
- M2-10 对照 `MarkdownEditor.tsx`、`imageUpload.ts`、`useCompactEditor.ts` 与 `UploadScope.tsx`，确认 latest ref、Promise 队列、逐图失败隔离、10MB 校验、640px 断点和上传计数器的描述准确。
- M2-10 使用 M1-18 的真实文章标题及 CSDN 地址，并明确前端校验不能替代后端文件校验。

## 三、文风与发布信息复审

- 三篇均从真实的工作中断或数据覆盖问题切入，并依次给出现象、根因、实现、取舍和验证方法。
- 代码片段围绕一个明确判断展开，没有按源码文件顺序逐段翻译。
- 每篇包含 6 个 Tag；简介分别为 132、142、155 个字符，均不超过 250 字。
- M2-08、M2-09 各准备 2 张配图提示词，M2-10 准备 3 张；图片节点文字、比例与插入位置均已给出。
- Markdown 代码围栏数量均为偶数，发布辅助区起止标记完整。

## 四、发布前仍需完成

- 根据提示词生成并上传最终配图，替换正文图片占位符。
- 按发布顺序回填已经上线的 M2 内链。
- 发布前重新核对 300ms 防抖、10MB 图片限制、640px 断点等可能随实现调整的数字。
- 删除发布辅助区，并在 `ARTICLES.md` 回填 tag、CSDN 地址和发布状态。
