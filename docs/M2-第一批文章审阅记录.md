# M2 React / Vite 管理后台 · 第一批文章审阅记录

> 审阅日期：2026-09-14  
> 审阅范围：M2-01～M2-04  
> 当前结论：四篇文章均已达到交付 owner 校准文风的草稿标准。

## 一、首批文章

| 编号 | 核心命题 | 正文汉字数 | 状态 |
| --- | --- | ---: | --- |
| M2-01 | 工程底座要让错误尽早暴露，并为后续模块建立边界 | 2234 | 草稿中 |
| M2-02 | 后台骨架由布局、数据路由、菜单和分层守卫共同构成 | 2152 | 草稿中 |
| M2-03 | 请求层应统一处理响应信封、业务错误和并发 401 刷新 | 2124 | 草稿中 |
| M2-04 | OpenAPI 负责生成契约类型，人工请求函数保留应用语义 | 2253 | 草稿中 |

这里按 Unicode 汉字统计，不包含发布辅助区、标点、英文与代码。四篇全文各约 4700～5400 个字符，正文篇幅和论证密度符合首批草稿要求。

## 二、技术事实复审

- M2-01 使用当前项目真实版本和配置：React 19.2.8、Vite 8.2.2、TypeScript 6.0.2、Biome 2.5.11，并引用实际的代理、路径别名和质量命令。
- M2-02 按当前路由实现描述 `RequireAuth`、`RequireGuest`、`RequireConsole` 和 `RequireRole`。普通 `member` 会被 `RequireConsole` 拦截，账号中心只在 editor/admin 共用的后台主壳中开放。
- M2-03 对照 `lib/request/` 与刷新测试，确认统一信封、业务错误、单一刷新 Promise、请求重放和强制退出的描述与实现一致。
- M2-04 对照 `types/api.gen.ts`、`api/*` 和 `gen:types`，确认项目只生成 TypeScript 类型，请求函数仍由人工维护；分页字段示例与当前契约一致。
- 四篇引用的 M0/M1 已发布文章均使用真实链接；尚未发布的 M2 内链保留 `{{LINK:M2-xx}}` 占位符。

## 三、文风复审

- 每篇从项目中的具体矛盾进入，没有从概念定义或依赖清单开篇。
- 论证顺序保持为：现象与失败方式 → 原因 → 取舍 → 实现证据 → 边界与代价。
- 保留第一人称经验判断和少量“各位看官”等既有表达，同时控制口头禅密度。
- 代码段均承担论证任务，并在前后说明为什么这样写以及它不能解决什么。
- 四篇各自只证明一个主要命题，相邻文章之间通过职责边界和内链衔接，避免重复解释同一段实现。

## 四、发布辅助信息复审

每篇文末均包含一段可整体删除的发布辅助区，使用统一注释标记包围，其中包括：

- 6 个文章 Tag；
- 250 字以内的文章简介，当前分别为 147、136、147、142 字；
- 建议分类、封面短标题；
- 封面图与正文图的中文 AI 提示词；
- 图片插入位置、比例和图中文字建议；
- 发布前链接、图片、代码和排版核对项。

## 五、发布前仍需完成

- 本轮只提供配图提示词，尚未生成最终图片。
- 发布某篇文章前，应把已经上线的 M2 占位链接替换为真实地址；尚未发布的后续文章可暂时保留为纯文本预告。
- `react-frontend-v1.0` 里程碑 tag 尚未创建，应等 M2 全部文章完成且管理后台代码冻结后再创建。
- owner 完成首批阅读后，应将反馈同步应用到 M2-01～04，再开始 B 批次，避免同一类文风偏差扩散到余下十八篇。
