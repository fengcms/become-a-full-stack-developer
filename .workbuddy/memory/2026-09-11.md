# 2026-09-11 工作日志

## 发布维护 · M1-23/24 同步 + 25/26 内链

**SOP-A 镜像**
- M1-23 部署上线 → `164815866` → 🟢
- M1-24 一套后端双部署 → `164816647` → 🟢
- 至此 M1-01~24 全部已发布

**回填（5 处）**
- `M1-02` L83 `{{LINK:M1-24}}` → 一套后端双部署
- `M1-07` L125 `{{LINK:M1-23}}` → 部署上线
- `M0-03` L71 `{{LINK:M1-24}}` → 一套后端双部署
- `M1-22` L177 `{{LINK:M1-23}}` → 部署上线
- `M1-23` L148 `{{LINK:M1-24}}` → 一套后端双部署

**25/26 无注入项**
- `M1-25` L233 唯一占位 `{{LINK:M1-26}}`（今天发，保留）
- `M1-26` L161 唯一占位 `{{LINK:M1-27}}`（未发，保留）
- 连续第五轮该情况，确认为写作风格（后续文章对已发文章多用纯文本引用），非漏注

**证据**
- grep `{{LINK:M1-01~24}}` → 0 残留，零漏注
- ARTICLES.md 第 47/48 行 🟢，49/50 行保持 🟡

**CSDN 副作用**：本轮改了 5 篇已发布文章 `M1-02`、`M1-07`、`M0-03`、`M1-22`、`M1-23`，需 blog AI 重新发布才生效。

## 效率改进（本轮验证有效）

1. **先 Glob 再 Grep**：按编号猜文件名曾导致假阴性（9-10 日志教训）。本轮先 Glob 拿真实文件名再查占位，一次命中。
2. **old_string 只取 `{{LINK:Mx-yy}}`**：中文正文引号经 Read 输出会被规范化，整句匹配易失败；只匹配占位符本身最稳，且天然唯一。
3. **不同文件的 Edit 合并到同一条消息批量发**：本轮 5 处回填一次性提交，无写竞争、无丢失。同文件仍必须串行。
4. 全流程压缩到约 14 次调用，未再触发网关中断。

---

## 第二轮 · M1-25/26 同步 + 27/28 内链

**SOP-A 镜像**
- M1-25 分类树 → `164973360` → 🟢
- M1-26 阅读量防刷 → `164973883` → 🟢
- 至此 M1-01~26 全部已发布

**回填（3 处）**
- `M1-21` L123 `{{LINK:M1-26}}` → 阅读量防刷
- `M1-24` L160 `{{LINK:M1-25}}` → 分类树
- `M1-25` L290 `{{LINK:M1-26}}` → 阅读量防刷

**27/28 无注入项**
- `M1-27` L161 唯一占位 `{{LINK:M1-28}}`（今天发，保留）
- `M1-28` L178 唯一占位 `{{LINK:M1-29}}`（未发，保留）
- 连续第六轮该情况，写作风格使然，非漏注

**证据**：grep `{{LINK:M1-01~26}}` → 0 残留；ARTICLES.md 第 49/50 行 🟢，51/52 行 🟡。

**CSDN 副作用**：改了 3 篇已发布文章 `M1-21`、`M1-24`、`M1-25`，需 blog AI 重新发布才生效。

**效率**：沿用上午的改进流程（先 Glob 再 Grep、old_string 只取占位符、不同文件批量提交），11 次调用完成，未中断。已稳定复现，可作为后续固定套路。
