# 2026-09-16 工作日志

## 里程碑：M1 全 31 篇发布完毕，M2 开篇

**SOP-A 镜像（4 篇）**
- M1-29 点赞系统 → `165292499` → 🟢
- M1-30 通知系统 → `165293002` → 🟢
- M1-31 数据建模手艺 → `165445806` → 🟢
- M2-01 Vite + React + TypeScript → `165447601` → 🟢

M1-01~31 **全部已发布**，M1 模块收官。M2（React 管理后台）已开第一篇。

**回填（9 处）**
- `M1-31` L26/L28/L36 `{{LINK:M1-29}}` ×3 → 点赞系统（replace_all 一次搞定）
- `M1-30` L146 `{{LINK:M1-29}}` → 点赞系统；L194 `{{LINK:M1-31}}` → 数据建模手艺
- `M1-29` L198 `{{LINK:M1-30}}` → 通知系统
- `M1-28` L239 `{{LINK:M1-29}}` → 点赞系统
- `M1-21` L130 `{{LINK:M1-29}}` → 点赞系统
- `M2-02` L20/L186 `{{LINK:M2-01}}` ×2 → URL 165447601（replace_all）
- `M2-19` L86 `{{LINK:M2-01}}` → URL 165447601

**M2-02/03 结论**
- `M2-02`：2 处 `{{LINK:M2-01}}` 已注入（M2-01 本轮发布）→ 就绪
- `M2-03`：唯一占位 `{{LINK:M2-07}}`（未发布，保留）→ 无注入项

**证据**：grep 覆盖 M1-01~31 + M2-01 → 0 残留；ARTICLES.md 第 53/54/55/61 行 🟢。

## ⚠️ 发现疑似坏链（待 owner 定夺，未擅自改）

`M1-31` 正文有两处**手工 markdown 链接**（非占位符）：
`[阅读量：时间桶去重与原子计数](https://blog.csdn.net/fungleo/article/details/164493891)`

- 该 ID `164493891` **不在** `materials/csdn-已发布链接.md` 索引中全仓 grep 不到
- M1-26「阅读量防刷」的真实 URL 是 `164973883`
- 链接文案「阅读量：时间桶去重与原子计数」也与索引标题「阅读量防刷：去重、冷却与计数写分离」不一致
- 判断：疑似写作阶段凭空生成的 URL。M1-31 已发布，坏链在线上生效
- 已向 owner 报告，等确认后是否改为 164973883

## 待办
- ~~ARTICLES.md 第 57 行小节标题仍为 `## M2 · React 管理后台（待写作）`~~ → 已改「草稿完成，待发布」（owner 本轮回明确要求"全盘更新落后文档"，属授权范围）

## 11:40 文档进度对齐（owner 要求：避免文档落后于进度）

M2 共 22 篇草稿（M2-01~22）已写好，但多份文档状态标记落后，本轮回统一校准：

- **ARTICLES.md:57** `## M2 · React 管理后台（待写作）` → `草稿完成，待发布`（22 行 M2 已登记，形态正常）
- **README.md 进度表**：M0/M1 行此前已更新；补 `M2–M8 | 未开始` → `M2 React 管理后台 | 文章草稿就绪（22 篇：M2-01 已发布，M2-02~22 待发布）；M3–M8 未开始`
- **docs/prd/README.md**：
  - 第 03 行 `M1 PRD ⬜ 未开始` → `✅ 已完成（后端冻结 node-backend-v1.0，31 篇成文）`
  - 第 04 行引用的 `M2-前端开发准备简报.md`（不存在，死链）→ 改为真实存在的 `M2-文章创作规划.md` / `M2-文章创作纲要.md`
- **M3/M4–M7 PRD 的「未开始」保留**（确属未启动，非落后）
- M2 各规划/NOTES/审阅记录文档的状态头已抽查，均反映真实进度，无"待写作/未开始"残留

校验：三份主文档 grep 「待写作/未开始」再无异常（除 M3–M8 合理未开始）。

## 经验固化
- **同文件多处相同占位符用 `replace_all`**：M1-31 的 3 处、M2-02 的 2 处，一次替换完成，避免串行 3 次
- **M2 系列占位形态不同**：`[标题]({{LINK:M2-01}})` —— 链接文案已写好，占位符在 URL 位置，替换时只填 URL、不加 markdown 链接结构。与 M1 的 `{{LINK:Mx-yy}}` 裸占位形态不同，需按形态分别处理
- **Grep 支持 `(a|b)` 交替，Glob 不支持**：Glob 只能用字符类 `[...]` 和 `*`
- 索引标题检索用 `管理后台篇`、`Node 后端篇·(关键词)` 定位稳定

## 分类页子分类文章聚合（级联 category，方案 B 落地，SeniorDeveloper）

- 来源：`docs/web-frontend/分类页子分类文章聚合-方案分析.md`（用户确认可行后按方案 B 实现）。
- 改动：① `docs/api/openapi.v1.yaml:142` FilterCategory 描述「不含子分类」→「包含该分类自身及其所有后代子分类（级联）」（仅改描述文本，结构不变）。② 复跑双门：语义门 `check_contract.py` **33 OK / 0 FAIL**，结构门 `openapi_spec_validator` SPEC VALID，yaml 仅此一行变更。③ `src/services/article.ts`：`queryArticles` 的 category 由 `eq(categorySlug, q.category)` 改为 `resolveCategorySlugsWithDescendants(slug)` → `inArray(categorySlug, slugs)`；分类不存在返回空列表。新增 helper 应用层循环展开（深度≤4，分类树小不引递归 CTE）。import 补 `inArray` + `categories`。
- 影响：`categorySlug` 仅出现在 article.ts 服务层，前台/后台/admin 全部共用 `queryArticles`，一处改动全链路生效；叶子分类无后代，行为不变，自然覆盖。
- 测试：新增 `test/routes/articles-category-cascade.test.ts`（3 用例：父分类返子分类文章 / 叶子不变 / 不存在 slug 返空），145 全绿（原 142 + 3）。
- 门禁：typecheck 0 / biome（article.ts 已格式化工件；`src/services/article-mutation.ts` 有 1 处既有 format 错误，非本次引入，未动）/ test 145 passed / 双门全绿。
- 部署：`npx wrangler deploy` 成功 → 线上 Version `0053d1f6-950f-443e-8a08-fc2d54b6446c`，域名 `api-befull.kao9.com`。本机无 wrangler，用 `npx --yes wrangler@latest`（CF OAuth web@fengcms.com 仍有效，workers write）。
- 线上验证：分类树 frontend→react 存在，但 `/articles` 全量 `total=0`（D1 当前无已发布文章），故级联无法用现有数据观察；逻辑由集成测试 + 成功部署佐证。未自动 commit（用户自管）。
- 备注：本机 shell 有 HTTP_PROXY，`npx wrangler` 会提示走代理；localhost curl 仍需 `--noproxy '*'`。

## 18:54 补修 lint 格式错误 + 打 tag（owner 指令「补修那个 lint 错误并打 tag」）

- 修复 `src/services/article-mutation.ts:181` 既有 biome 格式错误（长行换行，仅格式、无行为影响）；`biome check --write` 修复后全量 `biome check .` 118 文件 0 错。
- 门禁复跑全绿：typecheck 0 / lint 0(118 文件) / test 145 passed / 双门 33 OK+0 FAIL / yaml 仅第 142 行描述变更。
- 提交范围：只 stage 后端相关 4 文件（openapi.v1.yaml + article.ts + article-mutation.ts + 新增测试），排除 web-frontend 与 memory（工作区混有未提交前端改动，未带入后端 tag）。commit `32d86d9`。
- null 可选字段修复（`.nullish`）已于更早 commit `5d7a964` 提交（本次工作区 article.ts 仅含级联改动）。
- 打 **annotated tag `node-backend-v1.0.3`** 并 `git push origin node-backend-v1.0.3` 推送（新 tag 成功）。tag 指向 `32d86d9`，覆盖 v1.0.1 + v1.0.2(null 修复) + 级联 + lint。
- 注：lint 修复属纯格式，线上部署 Version `0053d1f6` 行为等价，无需重部署；tag 为源码/发布态标记。
