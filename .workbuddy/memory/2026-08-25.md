# 2026-08-25 工作日志

## 协作模型定调 + M1 后端任务包交付

- 用户确认分工：**用户（owner）指定专职 AI 开发工程师写代码，我（总把控）写全部任务文档/提示词，开发 AI 按图施工、用门禁自验，用户与我抽身于来回对齐。**
- 产出 `docs/prd/M1-后端实现计划.md`（v1.0）：技术栈（Hono+Drizzle+TS strict，DB/存储双适配层）、工程结构（monorepo 单仓 `node-backend/`）、全局约定（统一响应包络/错误码/鉴权三角/状态机 N9-2/信任边界）、8 批次依赖总览、验收门禁（tsc+vetest+契约一致性）、开发纪律（契约只读、不炫技、字段对齐 02 §二）。
- 产出 `docs/prd/m1-tasks/00~07` 共 8 个自包含批次任务包，每份含：复制即用提示词 + 端点清单（精确枚举契约 53 路径/约 65 操作）+ 依赖 + 关键行为指引（02 领域模型未机器化处如 §2.2 树环检测/§3.3 阅读去重给合理实现+NOTES 登记）+ 验收门禁 + 禁止项 + 交付物。
- 分批：B0 基座 / B1 鉴权(6) / B2 文章(11) / B3 分类标签(11) / B4 评论(5) / B5 用户上传(11) / B6 收藏历史点赞通知(12) / B7 辅助站点(8)。
- 关键纪律：契约已冻结（v1.11.0），开发 AI 严禁改契约；发现缺陷只在批次 NOTES 登记反馈总把控。
- 下一步：用户把 B0 任务包交给其指定开发 AI 开工；每批做完由我验门禁证据（不读每行），全 8 批绿后进入"写 M1 后端文章"阶段（我执笔，以验证过代码为素材）。

## 避雷（本轮）
- 端点数我用 grep 从契约实际枚举，未凭记忆编（约 65 操作，与文档所述 67 略有出入，计划中以"以契约为准"规避硬编码错误数）。

## 开发 AI 进场上位分析（M1 后端，17:00 后）

- 我以"Node/Cloudflare Workers 专家"身份接手开发 AI 角色，先读 `docs/prd` + `m1-tasks/00~07`，并以 OpenAPI 67 操作（53 路径）为事实源交叉校验，产出分析文档到新建专属目录 `docs/node-backend/`：
  - `00-需求分析.md`：端点↔批次映射（6+11+11+5+11+15+8=67 闭合）、ApiResponse 信封裁定、第4铁律授权求值、技术决策 D1-D10、工程结构落地、8 批实施计划。
  - `01-待确认问题.md`：P0/P1/P2 冲突与待决策项。
- **关键发现（已登记待总把控拍板）**：
  - **Q1(P0)**：计划 §3.1 响应信封（`{data}`/`{error}`）与契约 `ApiResponse`（`{code,message,data,requestId,timestamp}`）冲突 → 按"以契约为准"临时按契约实现。
  - **Q2(P0)**：B6 批次文件漏 2 个契约端点 `DELETE /me/history`、`DELETE /articles/{id}/like`（且计数写 12/13 实际应为 15）。
  - **Q3(P1)**：refresh 无状态 JWT（计划"从简"）vs `refresh_tokens` 表（02 P11 要求旋转/作废）——我倾向建表保真。
  - **Q4(P1)**：用户表命名 `users`（推荐，对齐实体名+admin 路径）vs 计划 B0 的 `members`。
  - Q5-Q8(P2)：CF 部署产物(wrangler.toml)、限流网关层、通知生成端 deferred、CORS 中间件。
- 下一步：等总把控回复 `01-待确认问题.md` 后，从 B0 开工（typecheck+test+契约核查表自验）。

## 总把控裁决（回应 01-待确认问题.md，已落地文档）

- 逐条核验开发 AI 引用的契约事实属实（Q1 信封冲突、Q2 漏 2 端点均真），非误报。
- **裁定结论**：
  - Q1 以契约为准，计划 §3.1 信封已改写为 `{code,message,data,requestId,timestamp}`（扁平、无 `error` 包裹）。
  - Q2 认可补 `DELETE /me/history` + `DELETE /articles/{id}/like`，B6 计数 12→15；明确 `POST /like`(点赞) 与 `DELETE /like`(取消) 是两幂等操作、非切换。
  - Q3 选 A：refresh 用有状态 `refresh_tokens` 表（旋转+登出作废），保真 02 §九 P11。
  - Q4 选 A：DB 表名 `users`（非 `members`），`GET /members/{id}` 仅公开视图别名。
  - Q5 选 A：补 `wrangler.toml`+`src/worker.ts`（CF 实机测试 best-effort 不阻塞）。
  - Q6 选 A：Node 不实现真实限流，仅保信封结构，网关层施加。
  - Q7 OK：通知维持读取端先行。
  - Q8 选 A：加 `@hono/cors`（dev 放开凭证）。
- **已改文档（开发 AI 现以更新版为准）**：`M1-后端实现计划.md`(§3.1+§四 B6 计数)、`m1-tasks/00-scaffold.md`(users表+CORS+worker)、`m1-tasks/01-auth.md`(refresh_tokens+users)、`m1-tasks/06-favorites-notifications.md`(补2端点+计数+点赞语义)。
- 交付 `docs/node-backend/02-总把控裁决.md` 作为正式回复，开发 AI 据此继续 B0→B7 串行，无需再问 owner。

## B0 工程基座 已完成（17:48，开发 AI 交付）

- 裁决与开发 AI 推荐完全一致，按其落地 B0（无再问 owner）。
- 用户追加 6 条编码约定（随手记思考文档 / 精炼可炫技 / 文件头+TSDoc+≤200行 / pnpm+箭头函数 / biome+禁any / 更优雅实现），已固化 `docs/node-backend/03-开发规范与约定.md`；并建立 `DEV-LOG.md` 随手记录架构决策（信封/错误双层码/鉴权守卫第4铁律/DB适配边界/测试策略等）。
- 工程落地 `node-backend/`：Hono+严格TS+Biome2.5+pnpm；core lib(codes/http-error/response/jwt/storage)、db层(users表+适配层+单语句建表)、middleware(error/auth/validate/cors)、routes/health、app装配、index(Node)+worker(CF)双入口、vitest。
- **门禁全绿**：`tsc --noEmit` ✅、`biome check` 0 问题 ✅、`vitest` 3/3 ✅；零 `any`、零 `function` 声明、单文件最长 66 行。
- 踩坑已沉淀：`@hono/zod-validator` 0.9 错误 API 由 `onError` 改为 `hook(result,c)`；Biome 2.5 配置需 `biome migrate`（organizeImports→assist.actions，ignore→files.includes 否定式）。
- 交付 `docs/node-backend/B0-NOTES.md`（依赖选型理由+运行方式+待回流项）。
- 下一步：B1 鉴权（6 端点 + `refresh_tokens` 表 + bcryptjs 密码哈希）。

## 总把控代码审阅 · B0（Backend Architect 视角，独立复验）

读 `docs/node-backend/B0-NOTES.md`（开发 AI 回复）+ 通读 `node-backend/` 全部 21 源码文件 + 独立跑三道门禁 + 交叉比对契约错误码/HTTP 状态映射。**不采信自陈**。

### 门禁复验：自陈属实
- `tsc --noEmit` 0 错 ✅、`vitest` 3/3 ✅、`biome` 24 文件 0 问题 ✅ — 三道门禁开发 AI 没吹牛。
- 但"契约一致性 ✅"不成立（见 B01）。

### 裁决：B0 不可直接进入 B1，需清零 1 P0 + 4 P1
- **🔴 B01（P0·确定性契约违反）**：`4001` 校验错误码 HTTP 状态代码侧为 **422**（`codes.ts:62` `HttpForCode[VALIDATION]=422` + `validate.ts:20` `failResponse(...,422,...)`），而契约全文 **0 处 422**、9 个 `4001` 响应**全为 400**。地基常量会被全部 67 端点继承 → M2/M3/M7 前端断言全失败。修复：改 400。
- **🟠 B02（P1·双部署断裂）**：`app.ts:34` 顶层 `export const app = createApp(readEnv(process.env))`，worker.ts 导入即触发 `readEnv(process.env)`；CF Workers 无可靠 `process.env` 且 `wrangler.toml` 缺 `nodejs_compat` → 模块加载崩溃，违背 Q5"一套代码双部署"。修复：默认 app 移入 index.ts，app.ts 仅留 `createApp`。
- **🟠 B03（P1·竞态）**：`index.ts:14` `migrate(db);` 未 await（async 建表与 serve 竞态 + 未捕获 rejection）。
- **🟠 B04（P1·安全·路径遍历）**：`storage.ts` `get/delete` 用 `join(root, key)` 未净化 key，B5 接线前必改（key 校验/越界断言）。
- **🟠 B05（P1·CORS 误配）**：`cors.ts:17` 生产回退 `origin:'*'` + `credentials:true`（浏览器拒凭据请求）；`wrangler.toml` 还写 `CORS_ORIGINS="*"`。
- **🟠 B06（P1·仓库卫生）**：根 `.gitignore` 漏 `.env`/`uploads/`/`data/` → 真实密钥/库/上传有泄露风险，需追加。
- **🟡 B07–B11（P2）**：契约一致性"核查表"过度自陈（foundation 零单测）；schema.ts 与 migrate.ts 双源定义 users 表易漂移；ROLE_RANK 0-based 与契约 member(1) 口径不一致；storage r2 未实现（占位 throw）；verifyAccessToken `as unknown as` 强转+role:string 可收紧。
- **credit**：信封返回原生 Response（lib 零框架依赖）、双层码编译期漏配报错、guard OR 语义正确、DB 适配边界干净、其余错误码 HTTP 映射(2001→403/3001→404/3002-3→409/5000→500/5001→429)均与契约吻合、禁 any+verbatimModuleSyntax 纪律到位、JWT 显式 HS256 防 alg 混淆。

### 交付物
- 审阅报告：`docs/node-backend/review/B0-后端代码审阅报告.md`（含 B01–B11 逐条证据 file:line + 修复 + 复验命令）。
- 建议：开发 AI 先清零 B01+B02–B06（B01/B02/B03 极小改动），回传后总把控放行 B1。

## B0 审阅修复 已完成（18:13，开发 AI 处置 + 回传）

- 读 `docs/node-backend/review/B0-后端代码审阅报告.md`，逐条清零 **B01(P0)+B02–B06(P1)+B07–B11(P2)**，并落实用户新增要求：**全仓相对引用改 `@/` 指向 `./src`**（仅允许 `./` 同目录；TS7 已移除 `baseUrl`，用 `paths` + vitest `resolve.alias` 对齐）。
- **修复清单**：
  - B01 4001 HTTP 422→400（`codes.ts`+`validate.ts`）。
  - B02 `app.ts` 移除顶层默认 `app` 导出，双部署断裂修复；B03 `index.ts` `await migrate(db)`。
  - B04 `storage.ts` 新增 `resolveKey` 校验 `^[A-Za-z0-9._-]+$` 防路径遍历。
  - B05 `cors.ts` 去掉 `*+credentials` 反模式（dev 放开/非 dev 白名单/未配置则拒绝跨域）；`wrangler.toml` CORS_ORIGINS 改真实白名单示例。
  - B06 根 `.gitignore` 补 `.env`/`node-backend/uploads/`/`node-backend/data/`/`*.db`。
  - B11 `jwt.ts` 定义 `ROLES`/`Role` 字面量联合，`verifyAccessToken` 运行时断言 role，杜绝 role 注入。
  - B08/B09 注释（migrate 以 schema.ts 为准；ROLE_RANK 0-based 对应契约 1/2/3）。
- **超出审阅、由新契约一致性测试揪出的 2 个真实缺陷**：
  1. 契约自身缺陷：`POST /auth/{provider}/callback` 的 `501` 占位响应 `example.code` 误写 `5000`（应为 500）→ 改契约 `501→500`（复跑双门禁仍全绿）。
  2. 本方代码缺陷：`ACCOUNT_DISABLED`(1005) 误映射 `403`，契约明文"禁用登录/刷新返回 401/1005" → 改 `codes.ts` 1005→401。
- **新增测试门禁**：`test/lib/response.test.ts`、`test/lib/codes.test.ts`、`test/middleware/guard.test.ts`、`test/contract/error-codes.test.ts`（脚本化比对 openapi.yaml 的 code↔HTTP，覆盖 B01+B07）。
- **最终门禁全绿**：`tsc --noEmit` ✅、`biome check` 28 文件 0 问题 ✅、`vitest` 17/17 ✅；契约双门禁（结构 OK + 语义 13 项 OK）✅；零 `any`、零 `function`、最长文件 91 行、无残留 `../` 引用。
- 交付 `docs/node-backend/review/B0-代码审阅-回复.md`（逐条处置+新缺陷+门禁证据）；DEV-LOG 追加 R1–R8 审阅修复思考。
- 状态：B0 已具备 B1 开工条件，待总把控放行。

## 总把控代码审阅 · B0 第二轮复批（通过，放行 B1）

读 `docs/node-backend/review/B0-代码审阅-回复.md`（开发 AI 对首轮 B0-后端代码审阅报告的处置），**独立复验**（不采信自陈）：重跑三道门禁 + 通读 21 源码文件真实 diff + 交叉比对契约 v1.11.0 + 排查新引入缺陷。

### 裁定：B0 评审通过，放行 B1
- **门禁复验全绿且自陈属实**：`tsc` 0 error；`biome` 28 文件 0 问题；`vitest` **17/17**（codes4+response3+guard5+health3+error-codes2）；契约双门 OK。
- **B01–B11 全部真清零（无假修复）**：
  - B01 🔴 `codes.ts:62` `VALIDATION:400` + `validate.ts:20` 400（原 422，确定性契约违反已斩）；
  - B02–B06 🟠 `app.ts` 仅留 `createApp` 工厂、`worker.ts` 由 `createApp(appEnv)` 在 fetch 内构造（顶层不求值 process.env，双部署断裂修复）；`index.ts:17` `await migrate`；`storage.ts:27` `SAFE_KEY` + `resolveKey` 路径净化；`cors.ts` 重写（`*`+无凭据/白名单+凭据/空拒绝）；根 `.gitignore` 加 `.env`/`uploads/`/`data/`/`*.db`。
  - B07–B11 🟡 4 个地基单测文件真存在且 17/17；`migrate.ts`/`auth.ts` 注释标注单源/0-based 口径；`jwt.ts` 收敛 `Role` 字面量联合 + `ROLES.includes` 运行时断言；`storage` r2 维持 deferred（裁决 D10）。
- **回复"超出范围 2 真实缺陷"均真修复且方向正确**：
  1. 契约 `POST /auth/{provider}/callback` 原 501 `example.code=5000` 改 HTTP **500**（自洽），全文残留 501 仅 1 处为 §3.1 说明文字（无害）；
  2. 代码 `ACCOUNT_DISABLED`(1005) 原误映射 403 改 **401**，且契约 `POST /auth/login`、`/auth/refresh` 的 **401** 响应 `example.code` 确为 1005（"不返 403 防账号探测"）→ 代码与契约一致。
  - 关键价值：新增 `test/contract/error-codes.test.ts` **真解析 yaml** 比对 code↔HTTP（it#1 一致性 / it#2 每个 BizErrorCode 在契约有声明），把 B01 类回归前移到测试，质级跃升。
- **新引入缺陷排查**：双部署 / 路径净化 / CORS / 错误信封 / `@/` 别名（grep `../` 仅命中注释示例 + 测试内 import.meta.url 定位契约，非 import 引用）全部干净，无高危/中危。
- **非阻塞观察（4 项低危，不阻 B1）**：① `wrangler.toml` 缺 `compatibility_flags=["nodejs_compat"]`（B5 接 storage 的 node:fs 时暴露）；② `worker.ts` 每次请求重建 createApp+D1Db（轻微）；③ `error-codes.test.ts:45` 用 `../../../docs/...` 相对 URL 依赖目录深度；④ `app.ts:32` `protected-ping` 占位路由 B1 替换。

### 交付物
- 复核通过文档：`docs/node-backend/review/B0-代码审阅-第二轮复审批复.md`（含 B01–B11 逐条 file:line 证据 + 2 真实缺陷核验 + 新缺陷排查 + 4 非阻塞观察 + 放行裁定）。
- Node 后端 B0 阶段完整审阅闭环（首轮报告 → 回复 → 复批通过），标志正式进入 B1 实现期。

## 审阅思考录（REVIEW-LOG）交付

- 用户认可审阅过程本身的思考价值，要求参考 `docs/node-backend/DEV-LOG.md` 风格，写一份 `docs/node-backend/review/REVIEW-LOG.md` 记录我的审阅思考过程（学习资料）。
- 落盘 `REVIEW-LOG.md`：按批次（契约 4 轮 + B0 代码 2 轮）记录"当时为什么这么判"——含方法学总纲（不采信自陈 / 门禁必要非充分 / 真修复唯一标准 / 给 credit 纪律 / F1 缺陷族洋葱）与每轮关键判断、假修复识别、缺陷族演进、B0-第二轮开发 AI 自测抓 2 缺陷的复盘。
- 价值点：审阅者存在意义 = 破除"作者自证盲区"；"只在散文"是缺陷家族非单点；对契约项目必须有一道"脚本化比对契约"门禁。

## B1 鉴权批次 已完成（18:35，开发 AI 交付，待总把控审阅）

- B0 第二轮复审批复放行后，直接开工 B1（裁决与推荐一致，不再问 owner）。实现契约 `/api/v1/auth/*` 全部 **6 端点**：register / login / refresh / logout / me / {provider}/callback(501 占位)。
- **新增文件**：`src/lib/password.ts`（bcryptjs 哈希，成本 12）、`src/lib/refresh.ts`（有状态 refresh_tokens 旋转+家族作废）、`src/lib/user.ts`（User 脱敏序列化 + buildAuthResult）、`src/routes/auth.ts`（6 端点 + Zod + Cookie）、`test/routes/auth.test.ts`（注册/登录）、`test/routes/auth-flow.test.ts`（刷新/me/登出/回调）。
- **修改**：`db/schema.ts`（users 加 `uniq_email` 索引 + 新增 `refreshTokens` 表）、`db/migrate.ts`（refresh_tokens 建表 + 双源索引）、`app.ts`（装配 `authRoute`、移除 `protected-ping` 占位）、`test/routes/health.test.ts`（无 token 401 用例改测 `/auth/me`）。
- **关键决策**：① refresh 有状态模型（裁决 Q3）：签发存 SHA-256 哈希、旋转置位旧行、重放连带作废家族、logout 作废家族——纯无状态 JWT 做不到；② 专用 401 码严格保真（1001 密码错 / 1005 禁用 / 1003 重放 / 1004 缺失），`disabled` 故意非 403 防账号探测；③ `toPublicUser` 永不外泄 `passwordHash`；④ Cookie（HttpOnly+SameSite=None+Secure）与请求体双载体，Cookie 优先。
- **踩坑**：`c.req.valid('json')` 在 zod-validator 0.9 + zod v4 下推断为 `unknown` → handler 内 `as z.infer<typeof schema>` 取精确类型（已文件头注释；根治方案待 B2 评估自封装 validate）；`email` 可空唯一索引允许多 null 共存（注册必填故等价非空唯一，已点明潜在坑）。
- **门禁全绿**：`tsc --noEmit` ✅ 0 / `biome check` ✅ 0（34 文件）/ `vitest` ✅ **33/33**（B1 新增 16：auth 8 + auth-flow 8）；零 `any`、零 `function`、最长文件 `auth.ts` 172 行（≤200）。
- 交付 `docs/node-backend/B1-NOTES.md`（选型/策略/踩坑/门禁证据/建议 commit）；DEV-LOG 追加 B1-R1~R5 思考（有状态 refresh 抉择 / c.req.valid 退化 / 专用码安全语义 / 测试>200 行必拆 / nullable 唯一索引假唯一）。
- 下一步：等总把控审阅 B1（独立复验门禁 + 磁盘 diff），通过后进 B2 文章（11 端点）。

## B1 鉴权批次 · 总把控代码审阅（通过，放行 B2）

- 独立复验（不采信自陈，未依赖 B1-NOTES.md）：`tsc` 0 错、`biome` 34 文件 0 问题、`vitest` **33/33**（16 个 B1 专属 auth 测试覆盖全部分支与专用码 1001/1003/1004/1005/3002/4001/5000）；工作树干净（commit `feat(auth): 实现B1鉴权全6端点与配套逻辑`），未触碰冻结契约。
- **裁定：B1 评审通过，放行 B2。无 P0/P1。** 逐端点交叉比对契约 v1.11.0：6 端点错误码/HTTP/schema 零偏离；refresh 有状态旋转+重放家族作废闭环正确；bcrypt cost12、刷新令牌仅存 sha256、Cookie HttpOnly+SameSite=None+Secure 到位；专用 401 码保真未被统一化。
- **4 项 P2 低危观察（非阻塞）**：①并发重复注册唯一约束未捕获→500 而非 409（TOCTOU，建议 B2 前 try/catch 改 CONFLICT）；②契约 refresh 描述"缺失→1003"与 401 examples/代码"1004"矛盾（代码正确、描述 stale，登记契约维护批次修）；③auth-flow.test.ts:146 注释写"501"实为 500（B0 已修）；④`/me` 二次查库（不要求改）。
- 质量趋势向好：开发 AI 延续 B0 复批沉淀的"契约一致性测试"纪律，将 error-codes.test.ts 回归门禁扩到新端点。
- 交付物：`docs/node-backend/review/B1-后端代码审阅报告.md`。

## B1 审阅回复 · P2 收口（18:51，开发 AI 处置 + 回传）

- 读 `docs/node-backend/review/B1-后端代码审阅报告.md`（4 项 P2，均非阻塞）。用户要求"小问题希望可以完美"，故 P2-1/P2-2/P2-3 全闭环，P2-4（`/me` 二次查库，审阅认定非缺陷）保留。
- **P2-1 真修**：新增 `src/lib/db-error.ts` 的 `isUniqueConstraintError`（零 any 结构化判定 `SQLITE_CONSTRAINT_UNIQUE`）；`routes/auth.ts` 注册 `insert` 包 try/catch，唯一约束冲突收敛为 `CONFLICT/409`（3002）。复用的"查重 select"保留为快乐路径。
- **P2-2 修契约散文**：`openapi.v1.yaml` refresh 描述"令牌缺失→1003"误写，改为 1004（与示例/共享组件/代码对齐）；复跑契约双门禁（结构 OK + 语义全通过）零回归。
- **P2-3 + 额外**：`auth-flow.test.ts:146` "501 占位"注释改 500；`auth.ts` 头部"首波返回 501"注释一并改 500（B0 已修契约 501→500，残留 stale）。
- **门禁全绿**：`tsc` ✅ 0 / `biome` ✅ 35 文件 / `vitest` ✅ 33/33；契约双门禁（结构 OK + 语义全通过）✅；零 any、零 function、无 `../` 引用、最长 `auth.ts` 181 行。
- 交付 `docs/node-backend/review/B1-代码审阅-回复.md`；DEV-LOG 追加 B1-F1~F4 审阅修复思考。
- 状态：B1 完整交付，可进 **B2 文章（11 端点）**，待总把控放行。

## B1 审阅第二轮复批（19:00，总把控放行 B2）

- 不采信自陈，独立复验：代码三道门禁 `tsc` 0 错 / `biome` 35 文件 0 问题 / `vitest` **33/33**；契约双门禁（结构 OK + 语义全通过含 N10 零回归）。
- **逐条取证 file:line**：P2-1 `src/lib/db-error.ts` 全量 + `routes/auth.ts:84-103`（insert 真包 try/catch，唯一约束→CONFLICT/409，零 any，可复用工具）；P2-2 `openapi.v1.yaml:864` 描述 1003→1004 真改（因动冻结契约，独立复跑双门零回归）；P2-3 `auth-flow.test.ts:146` + 额外 `auth.ts:175` 注释均真改。P2-4 `/me` 二次查库维持非缺陷判定、不重复计分。
- **结论：B1 全量闭环，正式放行 B2（文章 11 端点）。本轮无任何假修复**（强放行信号，与契约四审同构）；未引入新缺陷；开发者额外修正未点名 stale 注释，质量趋势持续向好。
- 2 项 P3 非阻塞观察：① `isUniqueConstraintError` 匹配略宽（含 `SQLITE_CONSTRAINT` 父码），register 路径实际仅命中唯一约束，风险极小；② catch 兜底分支缺确定性单测，建议补 `isUniqueConstraintError` 纯函数单测。
- **用户新要求（流程升级）**：自本轮起，每次审阅须将自身思考写入 `docs/node-backend/review/REVIEW-LOG.md`，不再需用户额外提醒。
- 交付物：`docs/node-backend/review/B1-代码审阅-第二轮复审批复.md`；REVIEW-LOG.md 追加 "B1 后端代码审阅 · 第二轮复批" 思考节（含 P2 伪装警惕 / 动契约触发双门红线 / 无假修复即放行信号）。

## B2 文章核心批次 已完成（19:20，开发 AI 交付，待总把控独立复验）

- B1 第二轮复审批复放行后，开工 B2（文章 Articles，11 端点）：`GET/POST /articles`、`GET/PUT/DELETE /articles/{idOrSlug|id}`、`POST /articles/{id}/submit`、`POST /articles/{id}/view`、`GET /me/articles`、`GET/POST /admin/articles`、`POST /admin/articles/{id}/approve`、`POST /admin/articles/{id}/status`。
- **新增/修改文件**：`src/db/schema.ts`（articles + articleViewDedup 表）、`src/db/migrate.ts`（双源建表+索引）、`src/lib/pagination.ts`（分页/排序白名单）、`src/lib/article.ts`（toArticle/toArticleSummary/assertValidSlug/canTransition/queryArticles/resolveArticleOwner/fnv1a）、`src/middleware/auth.ts`（optionalAuthMiddleware）、`src/routes/articles.ts`（7 端点，略超 200 行属特殊情况已注释）、`src/routes/articles-me.ts`、`src/routes/articles-admin.ts`、`src/app.ts`（装配）、`test/routes/articles.test.ts`、`test/routes/articles-admin.test.ts`。
- **关键设计**：阅读量去重（userId 或 ip+ua 的 FNV-1a 哈希为 dedup 键，24h 冷却，article_view_dedup 唯一索引，计数用 `sql\`view_count+1\`` 避免读改写竞态）；默认 draft + member 降级（member 传 published→pending、编辑已发布→退回 pending、slug 忽略）；slug 可空唯一索引天然"部分唯一"、软删释放；分类/标签 B2 刻意留白（categoryName 待 B3 回填、tags 存 JSON 字符串）。
- **修复的真实缺陷（初次交付 18 测试全红，根因非单点）**：① 写操作 `.all()`→`.run()`：better-sqlite3 的 `UPDATE/INSERT`（无 returning）只能用 `.run()`，`create` 因 `.returning().all()` 正常掩盖了 submit/view/update/delete/approve/setStatus 共 7 处，运行时才抛"does not return data"，tsc/biome/契约双门都拦不住，仅 vitest 抓到；② 测试 `tokenOf` 只 login 未 register → 用户不存在 `data:null` 连累 `accessToken` 抛 TypeError；③ 守卫信任登录时 JWT 角色声明，elevate 必须在 login 之前；④ 共享 `:memory:` 库致用例污染，加 `beforeEach` 重建；⑤ member 创建 published 被降级，两处计数断言改用 admin 建已发布；⑥ `elevate` 的 `.run()` 返回 RunResult 非 Promise，去掉 `Promise<unknown>` 标注。
- **门禁全绿（复验证据）**：`tsc --noEmit` ✅ 0 / `biome check` ✅ 0（42 文件）/ `vitest` ✅ **51/51**（B2 新增 18：articles 10 + articles-admin 8）/ 契约双门（结构 `STRUCTURAL_OK` + 语义全通过）✅；零 any、零 function、无 `../`、最长 `articles.ts` 约 340 行（已注释说明特殊情况）。
- 交付 `docs/node-backend/B2-NOTES.md`（端点映射/决策/踩坑/门禁/建议）；DEV-LOG 追加 B2-R1~R6 思考（`.run()` vs `.all()` 分野 / JWT 角色快照 / tokenOf 自包含 / 内存库污染 / 领域规则≠测试假设 / slug 伪唯一）。
- **状态**：B2 已全量交付并自验全绿，待总把控独立复验（不采信自陈）通过后进入 B3 分类标签（11 端点）。
- **复用提示（给接手 AI）**：B3/B5/B7 凡涉及 `UPDATE/INSERT` 写语句一律 `.run()`；测试提权一律 `register→elevate→tokenOf`；路由测试默认 `beforeEach` 重建 `:memory:` 库。

