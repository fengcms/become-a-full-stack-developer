# 2026-08-31 工作日志

## 准备「发布维护 Agent」启动物料（M0 全发完、M1 起接管）
- 用户确认：等产品册（M0）全发完后，委派独立 AI 做内容链接维护——此计划已在 `docs/发布维护-agent-岗位说明书.md` 与 `docs/链接与发布协作约定.md` 约定。
- 核验现状：M0 8/8 全 🟢；M1 31 篇已写完（`articles/M1-01`~`M1-31`）但未发布；`ARTICLES.md` 的 M1 区块仍是占位行「（待 M1 开工后逐篇登记）」，31 行未建；全仓 `{{LINK}}` 占位 65 处全在 M1；仅 `articles/M0-03` 残留 1 个未注入占位。
- 产出：
  1. 新建 `docs/发布维护-agent-启动提示词.md`——自包含、可复制粘贴启动；含身份/边界、必读启示文档清单、当前交接状态、SOP-0（建 31 行 + M0 内链收尾）、常态 SOP-A/SOP-B、铁律、数据来源。
  2. 更新 `docs/发布维护-agent-岗位说明书.md` §9 交接状态 → 2026-08-31 真实状态（原 §9 为 2026-08-27 旧快照，已过期）。
- 启示文档（新 AI 必读）即上述两份既有文档 + `ARTICLES.md`。
- 后续：用户粘贴启动提示词到新会话即激活；首个动作 SOP-0 建 M1 行，之后等 owner 发一篇维护一篇（SOP-A），M1 全发完跑 SOP-B 统一回填内链。

## 发布维护 Agent · SOP-0 执行（激活后首任务）
- 已读启示文档：岗位说明书、链接与发布协作约定、ARTICLES.md（按启动提示词 §二 顺序）。
- **建 M1 行（SOP-0）**：`ARTICLES.md` 的 M1 区块占位行已替换为 31 行（M1-01~M1-31），列格式对齐 M0（`代码 / 草稿位置` 列填 `articles/M1-xx-*.md`），状态全部 `🟡 草稿中`、tag 与 CSDN 链接列均为 `—`。
- **M0-03 内链收尾（SOP-B 局部）**：`articles/M0-03` line 71 的 `{{LINK:M1-24}}` 指向 M1-24（未发布）→ 按 SOP-B 第 3 步**保留占位、不注入**，未改写正文。
- **grep 证据**：
  - `成为全栈·Node 后端篇·` 行数 = 31；`🟡 草稿中` 出现 32 次（31 行 + 顶部图例 1）。
  - 全仓 `{{LINK}}` token 总数 = 65（M0-03:1 + M1-01~30 共 64，M1-31:0），与交接状态吻合。
  - `ARTICLES.md` 含 `blog.csdn.net` 计 8 次（仅 M0 区块），M1 行无 URL 误填；M1 行 tag 单元格无里程碑 tag。
  - 全仓无裸 `{{IMG:}}` 残留于 M0 已发布正文；无 per-article tag。
- 现状：M0 8/8 🟢；M1 31 行就位（🟡 待发）。进入常态——owner 每发一篇通知→跑 SOP-A；M1 全 31 篇发完→跑 SOP-B 一次性回填 65 处 `{{LINK}}`。
- 备注：岗位说明书 §9「当前交接状态」仍写"M1 区块仍是占位行…SOP-0 首任务"，该句现已过时，建议由统筹 AI 更新（发布维护 Agent 不越界改 doc）。

## 发布维护 Agent · M1-01/02 SOP-A + M1-03/04 内链注入（owner 现场指令）
- **SOP-A（M1-01/02 发布后维护）**：索引已同步，URL 取自 `/Users/fungleo/Documents/Blogs/materials/csdn-已发布链接.md`：
  - M1-01 → 164186950；M1-02 → 164187017。
  - `ARTICLES.md` 对应两行翻 🟢 + 镜像 URL，tag 列保持 `—`（未同步里程碑 tag）。M1-01/02 本地草稿无裸 `{{IMG}}`，图片核验无异常。
- **owner 现场指令**：M1-03/04 即将发布，要求把其中指向已发布篇（M0 全 8 篇 + M1-01/02）的 `{{LINK}}` 占位提前注入真实链接（03/04 为未发布草稿，属草稿内链注入，不动已发布正文，符合边界；此偏离默认「里程碑末统一回填」但属 owner 最新裁定，按启动提示词 §六 以 owner 裁定为准）。
- **注入结果**：
  - M1-03：注入 6 处 → M1-01、M1-02(×2)、M0-02、M0-05、M0-04；保留 `{{LINK:M1-04}}`（未发布）。
  - M1-04：注入 1 处 → M0-04；保留 `{{LINK:M1-16}}`/`M1-05`/`M1-06`（未发布）。
  - 全仓 `{{LINK}}` token 65 → 58。
- **待 owner 发布 M1-03 时提醒**：M1-03 line 52 有裸配图占位 `{{IMG:03-分层架构图}}`（非标准 `![...]()` 格式），发布前需由作者/blog AI 替换为真实图片 URL（文末「配图提示词」段有发布前处理说明）。M1-04 正文无裸 IMG 占位。
- 显示标题约定：M0 沿用已 established 短名（用一个真实系统串起全栈/领域建模/契约先行）；M1 用 ARTICLES.md 标题去掉「成为全栈·Node 后端篇·」前缀的核心部分。

## 发布维护 Agent · M1-03/04 SOP-A + 已发布文章内链维护 + M1-05/06 草稿注入（owner 指令）
- **SOP-A（M1-03/04 发布后维护）**：索引已同步（标题用全角冒号、无 `M1-03` 字面量，需按标题关键词检索）。URL：M1-03 → 164209137；M1-04 → 164209279。`ARTICLES.md` 两行翻 🟢 + 镜像 URL，tag 列保持 `—`。M1-03/04 本地源均无裸 `{{IMG}}`（发布时已由作者/blog AI 替换真实图）。
- **owner 指令（本次关键偏离）**：要求「维护已发布的文章中的内链」+「M1-05/06 内链准备发布」。前者 = 编辑已发布正文回填占位，偏离默认铁律「单篇发布时不改已发正文、内链只走 SOP-B 统一回填」——但属 owner 最新裁定，按启动提示词 §六 以 owner 裁定为准执行。⚠️ 副作用：本地源已正确，但 CSDN 线上 M1-01/02/03 仍需**重新发布（更新）** 才能把新链接推上线。
- **Part A 已发布文章注入**（指向已发布篇的占位）：
  - M1-01：注入 `{{LINK:M1-03}}`(→164209137)、`{{LINK:M1-02}}`(→164187017)；保留 M1-21/M1-20（未发布）。
  - M1-02：注入 `{{LINK:M1-03}}`×2(→164209137)；保留 M1-08×2/M1-24（未发布）。
  - M1-03：注入 `{{LINK:M1-04}}`(→164209279)；占位已清零。
  - （M0-01~08 无指向已发布 M1 的可解占位；M1-04 占位均指向未发布篇，均不动。）
- **Part B/C M1-05/06 草稿注入**：
  - M1-05：注入 `{{LINK:M0-04}}`(→164139553)、`{{LINK:M0-03}}`(→164121738)；保留 M1-17/M1-19/M1-06×2。
  - M1-06：注入 `{{LINK:M0-05}}`(→164140515)、`{{LINK:M0-03}}`(→164121738)；保留 M1-07。
- **grep 证据**：全仓 `{{LINK}}` token 58 → **49**（本批注入 9 处）；M1-03 残留 0、M1-05 残留 4、M1-06 残留 1、M1-01 残留 2、M1-02 残留 3（全为未发布篇，正确保留）；M1-03/04/05/06 均无裸 `{{IMG}}`。
- 链接文本约定：M0 用短名（领域建模/技术选型不是投票/契约先行），M1 用 ARTICLES.md 标题去前缀（分层架构-Controller-Service-Repository的边界 / 数据库选型-关系型还是文档型）。

## BUG 分析 · 后台创建文章 summary/coverImage/slug 校验失败（code 4001）
- 现象：/articles/new 提交空可选字段，后端报 4001，三字段 `expected string, received null`；`categoryId:null` 反而通过。
- 三方证据（契约真相源）：① 契约 ArticleCreate(summary/coverImage/slug 均 `nullable: true`，required 仅 title/content)；② 前端 ArticleFormPage 空串转 null 发出、api.gen.ts 生成类型 `summary?: string|null` 类型正确（build 绿）；③ 后端 node-backend/src/routes/articles-write.ts 的 createArticleSchema 用 `.optional()`（仅放行 undefined，拒 null），漏写 `.nullable()`，且同 schema 内 categoryId 已用 `.optional().nullable()` → 后端自身不一致，与现象严丝合缝。
- 结论：**后端 BUG（违背冻结契约），前端实现正确**。根因 = Zod `.optional()` 与 OpenAPI `nullable:true` 语义不对齐。
- 报告：`docs/bugs/BUG-文章创建-null校验.md`（现象/三方证据/结论/建议 diff/验证/误解澄清/关联）。建议修复（待后端 owner 确认）：三字段改 `.nullish()`（=optional+nullable）；updateArticleSchema 经 extend 自动继承。
- 所有权：根因在 M1 冻结后端仓库，走增量维护流程，前端 AI 不擅自改。
- 关联：与 M2 四轮审阅 R5(/me/likes 裸数组) 同类「契约↔实现偏差回流」，建议在门禁加反向断言防回归。

### SeniorDeveloper 复核结论（2026-08-31，本会话）
- **报告判定正确**（不采信自陈，已三处独立核实）：① 契约 yaml:515-540 三字段 `nullable:true`+required[title,content]；② 后端 `articles-write.ts:30-39` 的 `summary`/`coverImage`/`slug` 确为 `.optional()`（拒 null）、`categoryId` 为 `.optional().nullable()`（放 null）—— 与现象严丝合缝；③ 错误码 `codes.ts:21,45` VALIDATION=4001「参数校验失败」与报告响应一致。
- **处理方案**：纯后端实现 bug，改 `articles-write.ts` 三字段 `.optional()`→`.nullish()`（coverImage 保留 `.or(z.literal(''))`）；`updateArticleSchema` 经 `extend` 自动继承，无需再改。
- **不改变契约**：契约 `nullable:true` 本就正确（真相源），问题是实现未对齐；修复是"实现回归到契约"，`yaml git diff` 应保持 0。与分类 PUT 修复不同（那次是行为变更需补契约说明）。
- **下游 null 安全已核实**：`article-mutation.ts:22-27` 入参接口已声明 `| null`；`assertSlugAvailable`(:88-93)`if(!slug) return` 已对 null 作"不设 slug"处理 → 补 `.nullable()` 不会在 DB 层引入新崩溃。
- 流程：增量维护 fix → 门禁(tsc/biome/vitest + 契约双门 + yaml diff 0) → commit → bump patch tag(如 node-backend-v1.0.2)。可补一条回归测试「三字段传 null 创建成功」。待 owner 确认开工。

### 修复已落地 + 线上部署验证（2026-08-31 续）
- **代码改动**：`articles-write.ts:32,34,37` 三字段 `.optional()`→`.nullish()`；`test/routes/articles.test.ts` +1 回归用例「summary/coverImage/slug 传 null 创建成功」。`updateArticleSchema` 经 extend 自动继承，无需改。
- **六门门禁全绿**：tsc0 / biome0(117文件) / vitest **142 passed**(原141+1) / openapi_spec_validator OK / check_contract.py 语义门全过(33 OK 维持) / **yaml git diff 0**(契约零变更，证实"实现回归契约")。
- **线上部署**：本环境 `wrangler` 已登录 OAuth Token(web@fengcms.com, workers write 权限)，直接 `wrangler deploy` 成功 → Version `96e3ab8a-b7a6-47f1-911e-2ed052a76f70`，R2_BUCKET 绑定正确，STORAGE_DRIVER=r2，自定义域名 api-befull.kao9.com。
- **线上复现验证**：用 bug 报告原始 payload(title:222,content:111,summary/coverImage/slug/categoryId:null,tags:[],status:draft) POST /api/v1/articles → 返回 `code:0` + 文章创建成功(id:1, slug:null…)，**不再 4001**。BUG 关闭。
- 待 owner 自管：git commit 增量 + bump patch tag(node-backend-v1.0.2)。AI 不自动提交（用户纪律）。

## 后端接口 E2E 契约符合性验证（真实数据，32 用例）
- 脚本：manage-frontend/scripts/e2e-contract-check.mjs（v3，凭证走 env，测试数据带 E2E 前缀 + 自动清理）。目标：https://api-befull.kao9.com（admin 凭证用户给）。
- 结果：**32/32 PASS，零后端契约偏离**。覆盖 A 探针/B 内容域/C 治理域/D 资产·私域·统计/E 权限反向(纯 member) 五域。
- 两大修复点回归通过：① 文章 null 校验已 200（验证 user 之前报的 BUG 已真修好）；② R4 公开 GET /site/settings 已 200（不再 5000）。
- 两个设计观察（非 BUG，记 owner 复核）：① 删被引用分类返回 200、引用文章 categoryId 置 null（软外键，契约未强制拒）；② member→editor 提升后 /auth/me 角色即时同步（JWT 从 DB 取角色）。
- 脚本踩坑：前两轮失败全是我契约误读（分类/标签/文章更新是 PUT 非 PATCH、收藏 POST /me/favorites 带 articleId、历史 POST /me/history 且响应是 h.article.id 嵌套、公开 /articles 只返 published 故查询用 /admin/articles、回调漏 r 参数）→ 已据契约修正，重跑全绿。
- 残留：2 个 member 账号（e2emember*/e2emem2*+时间戳）因契约无 DELETE /users 无法经 API 清理，已写入报告残留说明，待后端 owner 手动禁用。
- 报告：docs/manage-frontend/M2-后端E2E验证-运行报告-20260831.md。

## 前端契约一致性核查（用户要求：回头查前端是否也有脚本同类契约误读 BUG）
- 范围：src/api/*.ts(9 实现) + src/lib/request(core/helpers 信封解包) + hooks + 关键页面，逐端点对照冻结契约 openapi.v1.yaml v1.11.0。
- 结论：**未发现同类 BUG**。脚本踩的 8 坑（PUT/PATCH 写反、路径错 /admin/users、历史 h.articleId、公开 /articles 查 draft、/stats 串味、回调漏 r）前端在对应维度全部正确：① API 层逐端点对照契约写且注释标清"计划↔契约差异"；② request/core.ts:181-183 信封 code===0 即 return envelope.data，页面永远拿真实形状，杜绝 data.items/.data.data 层级误读（grep `.data.data` 零命中）；③ 6 个契约守卫测试反向钉死 method/path/shape（items undefined、/admin/users not toContain、/me/likes 裸数组、/stats 路径）。
- 唯一"看似相似但正确"：ArticleFormPage.onSubmit 空串转 null 发 summary/coverImage/slug，正是上一轮 BUG 的前端忠实契约行为，后端修好后即正确，不动。
- 未实现点（非 BUG）：前端无历史页，不调 /me/history，故 h.article.id 误读无从发生；若将来做历史页须注意 ReadingHistoryItem.article.id 嵌套。
- 门禁：typecheck 0 / test 62 passed（含 6 守卫）/ lint+build 绿。未改任何前端源码，仅产出报告。
- 报告：docs/manage-frontend/M2-前端契约一致性核查.md。

## 本地后端联调（2026-08-31 晚）
- 用户反馈线上 api-befull.kao9.com 慢，改指本地 http://localhost:11000。本地 seed 凭证 = admin/admin123456（node-backend/scripts/seed-users.ts:48 `SEED_ADMIN_PASSWORD ?? 'admin123456'`，与线上 Admin.123 不同；首跑 1001 后改之）。
- 重跑 v3 套件 **32/32 PASS**，与线上版零偏离，证明本地 seed 库已含 R4 修复 + 文章 null 校验修复。
- 报告：docs/manage-frontend/M2-后端E2E验证-运行报告-20260831-local.md（脚本按环境命名 API_BASE/ADMIN_PASS 走 env，不覆盖线上版，可复跑）。

## M2 管理后台 · 页面调优轮（T1~T9 + T6，2026-08-31 晚）
- 目标：本地代理打通后（`vite.config.ts` `API_TARGET` 默认 `http://localhost:11000`，方案 B 同源代理绕 CORS）进入真机浏览器体验打磨。owner 勾选全量范围 P0+P1+P2。
- 走查纠偏（关键，避免重复劳动/回归）：T5 路由 Suspense、T7 暗色模式经核实**早已完整实现**（main.tsx ThemeProvider+懒加载+大页 Suspense；Topbar 已有 ThemeToggle，index.css `.dark` 变量块 + Tailwind v4 `@custom-variant dark` 已配），故仅补文档、不改动。
- 落点：
  - T1 列表错误态：新建 `QueryErrorState`；`DataTable` 接 `error/onRetry`；7 列表页接 `isError`+重试（错误优先于空态）。
  - T2 表单实时校验：11 表单 `useForm({ mode: 'onTouched' })`。
  - T3 搜索防抖：Article/User 列表 300ms（`kw` 独立态，不污染 URL 筛选）。
  - T4 图标按钮 a11y：7 列表页操作图标补 `aria-label`（Topbar 早有）。
  - T6 批量操作：`DataTable` 加可选 `selectable`（默认关，7 页零回归）；`BatchActionBar` 工具条；文章=批量发布+批量删除、评论=批量通过+批量删除；`Promise.allSettled` 容忍部分失败 + toast 汇总（契约无批量端点，走循环单行 mutation）。
  - T8 空态插画：DataTable 空态加 `Inbox` 图标。
  - T9 图片懒加载：`ImageUploadField` 预览 `<img loading="lazy">`。
- 门禁全绿：`typecheck 0` / `lint 0 error`（14 infos 非阻断）/ `test 62 passed`（12 files）/ `build 1.17s`（md-editor 563kB 告警 owner 已接受）。
- 本地闭环实测（证据，经 `localhost:12000` 代理）：`POST /auth/login {username:'admin',password:'admin123456'}` → token(139)；`GET /admin/articles` 带 Bearer → 200，信封 `{code:0,data:{list,pagination}}` 与前端取数契约一致。
- 文档：`docs/manage-frontend/M2-页面调优计划.md` 补 §4 范围确认 + §5 交付验收（含各任务落点表、门禁、本地联调证据、已知边界）。
- 边界/待办：T6 批量每页选择独立、跨页全选未做（服务端分页，非本轮范围）；组件级单测（错误态/onTouched/批量）按计划留待单独一轮，owner 确认后再装 RTL。
- 未提交：按项目铁律自管 commit，AI 不自动提交。

## M2 界面体验审计 + 优化规划（2026-08-31 晚，切换 Frontend 专家）
- 用户决策：① 跨页全选不做；② 组件级单测暂不做。目标：接口/架构已稳，只审计「页面实现细节 + 组件体验」，不动后端/契约。
- 审计方法：逐文件读码 + grep 取证（不凭记忆）。框架层（响应式侧栏/移动 Drawer/暗色/Suspense/骨架/错误态/空态/批量/防抖/校验 T1-T9）确认扎实，不在本轮范围。
- 三处 P0/P1 硬伤（均有证据）：
  - P0-1 下拉/弹层背景透明 = **系统性主题 bug**：`index.css` 的 `:root`/`@custom-variant dark` 块未定义 `--popover`/`--popover-foreground`，而 `select/dropdown-menu/popover.tsx` 均用 `bg-popover` → 全透明；暗色下 Topbar 用户菜单文字压深底几乎不可读（严重 a11y 失败）。修复：补 `--popover: var(--card)` + `--popover-foreground: var(--card-foreground)`。
  - P0-2 封面图用裸 URL `TextField`（ArticleFormPage:159-164），项目已有 `ImageUploadField`（R2 上传，头像/站点 Logo 在用）却没接文章封面 → 新手不会用。改为用 `ImageUploadField`。
  - P1-2 新建文章页单列 `space-y-4` 长列（Markdown 分屏 + 一堆元数据堆一列）→ 桌面双栏（主列标题+正文，侧栏元信息+操作）。
- P1/P2：P1-1 四处原生 `<select>`（文章/评论/用户列表筛选）迁 shadcn `SelectField`（前置 P0-1）；P1-3 取消无未保存守卫；P2 列表封面+查看外链 / 分类搜索 Combobox / 角色约束可视化 / Dashboard 文章 KPI / 标签联想 / 对比度走查。
- 交付文档：`docs/manage-frontend/M2-界面体验审计与优化规划.md`（现状盘点 + P0/P1/P2 问题表 + Phase A/B 分期 + 待确认 + 证据索引）。本轮只出分析报告+规划，未实施（等 owner 确认 Phase A 范围与 Phase B 取舍）。
- **owner 回应审计（16:24）**：① 跨页全选不做、组件单测暂不做（上轮已定）；② 文章表单双栏**不同意**；③ 范围收窄——**先只优化「发布文章页」(ArticleFormPage)，其余页面靠后**。并问 A(保存弹窗) vs B(标签页) 的专家意见。
- 专家结论：**选 B+（标签页）**：A 的 6 项元数据含文件上传塞弹窗体验差、取消时内容去向尴尬；B 的 Tab1 专注度等同 A 且元数据可见有组织。B+ = Tabs + sticky 双动词操作栏(保存草稿/发布, 移除 status 下拉) + 跨 Tab 校验(发布缺分类跳 Tab2 高亮) + 取消守卫。
- 实施前置已核实：无 `tabs.tsx`、未装 `@radix-ui/react-tabs` → 自建轻量受控 Tabs(零新依赖)；`ImageUploadField`/`ConfirmDialog`/`useImageUpload` 均就绪。
- 交付方案：`docs/manage-frontend/M2-发布文章页优化方案.md`；待 owner 确认 Q1(用 B+ 还是改 A) 与 Q2(双按钮还是保留 status 下拉) 后实施。
- **Phase 1 实施完成（B+ 标签页式，已落地）**：owner 确认 Q1=B+、Q2=双按钮。改动：① `index.css` 补 `--popover`/`--popover-foreground`（明暗两套，映射 `--color-popover*`）→ 修好分类下拉/弹层透明（系统性，产物 CSS 已验证含两变量）；② 新建轻量受控 `ui/tabs.tsx`（零新依赖，含 ARIA+方向键）；③ `ArticleFormPage` 封面改 `ImageUploadField`、拆 Tab1 内容/Tab2 设置、sticky 双动词操作栏(保存草稿/发布)、移除 status 下拉、发布缺分类跳 Tab2 高亮、取消守卫(isDirty+ConfirmDialog)。四门门禁全绿（typecheck0/lint0error/test62/build1.19s）。dev server localhost:12000 存活；浏览器走查（含暗色）由 owner 执行。未提交。

## 组件透明背景专项排查与修正（18:xx）
- owner 反馈：input 背景透明，要求亮色白底、暗色对应底；并排查其他组件是否有类似问题。
- 排查方式：全量 grep `bg-transparent`（6 处）+ 扫描 ui/form 组件全部颜色 token 对照 `index.css` 已定义变量。
- 结论：组件层**仅 `input.tsx`/`textarea.tsx` 是裸露字段透明**（直接置于卡片/页面、无包裹层兜底）→ 改 `bg-transparent`→`bg-background`（`bg-background` 亮=oklch(.985)近白、暗=oklch(.2046)深底，自动切换）；保留 `file:bg-transparent` 文件控件标准写法。
- 其余 `bg-transparent` 均为正确写法：calendar 上下月图标按钮（本该透明）；TagsField 输入框（嵌 `border border-input` 包裹层）；LoginForm 用户名/密码（外层 `border bg-background` 包裹层）。
- 弹层类（dialog/drawer/popover/dropdown-menu/sonner）背景均指向已定义 token（card/popover/background/muted），无未定义变量透明问题（`--popover` 上轮已修）。
- 链路：`TextField`→`Input`、`TextAreaField`→`Textarea` 自动继承修正；`SelectField` 触发器本就 `bg-background`。
- 四门门禁全绿（typecheck0/lint0error[14 infos 可选]/test62/build1.23s）。未提交。

## 新建文章页「写作优先」改造（17:09）
- owner 反馈：上一轮 B+ 标签页仍像普通 CRUD 后台——编辑器太小（MarkdownEditor 默认 height=420px），不像文章系统后台。
- 核心诉求：进来第一眼是**尽量大、尽量高的编辑器**；元数据小而次要；不必要的字段删掉。
- 改造：① 新建 `hooks/useViewportHeight.ts`（视口高度 + resize 同步）；② `MarkdownEditor` 加 `height` 入参（回退 minHeight），Tab1 传 `editorHeight=max(540, vh-280)` → 编辑器占满可视高度、可拖拽；③ 标题 `TextField` 加 `className` 透传，标题放大 `text-base font-medium`；④ 删掉 `slug`（URL 别名）字段——新建留空交后端派生、编辑保留原 `article.slug` 不破坏链接；⑤ Tab2 元数据 `max-w-2xl space-y-3` 收紧、保持次要。
- 四门门禁全绿（typecheck0/lint0error[14 infos]/test62/build 通过）。dev server localhost:12000 存活。未提交。

### 发布文章页 8 条具体指令落地（owner 逐项点名，17:2x）
- ① 删 PageHeader(新建/编辑文章标题) + 删「标题*」标签 + 删「正文」标签；② 输入框/文本域浅色改纯白：`input.tsx`/`textarea.tsx` `bg-background`→`bg-white dark:bg-background`（暗色仍深底）；③ Tab 切换移到标题正下方；④ 标题输入框左侧加返回按钮(ArrowLeft)，`isDirty` 时弹 ConfirmDialog「返回会导致已编辑内容消失」；⑤ 标题 placeholder「请输入文章标题，不可为空」；⑥ 编辑器高度动态占满剩余可视高度且页面不滚动：页面根 `h-[calc(100dvh-6.5rem)]` flex 列 + 编辑器容器 callback ref + ResizeObserver 测高（替代 vh 估算，规避 useExhaustiveDependencies）；⑦ 底部操作栏移除「取消」，重做容器(`border-t bg-background/80 backdrop-blur py-3`)；⑧ `index.css` 放大 MarkdownEditor 工具栏图标(svg 20px、`!important` 覆盖库样式)。
- 附：`tabs.tsx` TabsContent 默认 `mt-4` 移除（让内容区 flex-1 填满）；`MarkdownEditor` 移除 `visibleDragbar`（高度改由容器动态接管，防溢出）。
- 四门门禁全绿（typecheck0/lint0error/test62/build 通过，仅 md-editor>500kB 告警 owner 已接受）。产物 CSS 已验证含工具栏放大规则与 bg-white。未提交。

## 18:32 发布文章页 5 项细节微调（owner 追加指令）
- ① 返回按钮：`variant=ghost`→`outline`（加边框）；行间距 `gap-2`→`gap-3`。
- ② 标题框：去定制下划线透明样式（`border-0 border-b bg-transparent…`），改普通 Input 样式 + `text-lg font-medium` 放大文字。
- ③ 编辑器工具栏图标过大：index.css svg `20px→16px`、按钮 `34px→30px`、font `15px→14px`。
- ④ 编辑器矮窗压缩：根容器 `overflow-hidden`→`overflow-y-auto`（矮窗可滚）；编辑器容器 `min-h-0`→`min-h-[360px]` 设下限，ResizeObserver 实测不低于 360。
- ⑤ Tab 区域背景不通栏：`tabs.tsx` TabsList 去 `bg-muted` 灰块、改下划线式（`border-b` + 激活 `border-primary`，仅覆盖标签宽度）；「设置」面板去 `bg-card` 整片卡片背景（改 `max-w-2xl` 直接落页面）。
- 四门门禁全绿（lint 0 error；5 warnings+14 infos 均为仓库既有非阻断）。未提交。

## 20:1x 发布文章页三处走查缺陷修复（owner 浏览器实测反馈）
1. **返回按钮与标题框未居中**：原 `items-start` + 按钮 `mt-1.5` 手动偏移。改为行内 `items-center` 并去掉 `mt-1.5`；标题框 `h-9`→`h-11`（text-lg 行高 28px 在 36px 框里会被裁），错误提示移出弹性行、用 `pl-12`（按钮36+间距12=48）与输入框左对齐。
2. **标题框上边/右边被裁**：根因是页面根 `overflow-y-auto`（overflow-y 非 visible 会让 overflow-x 也变 auto，横向裁掉 focus ring；标题行是首个子元素，ring 上边也贴着裁剪边）。改 `h-[calc(100dvh-6.5rem)] overflow-y-auto` → `min-h-[calc(100dvh-6.5rem)]`（不锁高、不自滚，外层 main `overflow-y-auto` + p-6 负责滚动与留白）。
3. **矮窗底部按钮挡住编辑器（关键根因）**：`MDEditor` 有默认 `minHeight=420` 下限，页面按 ResizeObserver 实测传入的 `height` 小于 420 时，库仍按 420 渲染 → 编辑器溢出容器、压在操作栏下。修 `MarkdownEditor`：`minHeight={height ?? minHeight}`（显式 height 优先，同值覆盖下限）。另给编辑器容器加 `overflow-hidden` 兜底。
- 四门门禁全绿（typecheck 0 / lint 0 error / test 62 / build 1.18s，md-editor 563kB 告警 owner 已接受）。未提交。
- 经验：给第三方组件传动态高度时，务必同步它内部的 min-height，否则动态高度等于失效。

## 20:4x 表单横排布局 + 编辑器高度可缩（owner 第 6 轮反馈）
1. **设置页控件激活时左边被裁**：`TabsContent` 上的 `overflow-y-auto` 会连带把 overflow-x 变成 auto，裁掉焦点环左侧。修：`-mx-2 px-2`（左右各留 8px 内边距再反向外扩，视觉不位移）。
2. **表单改横排（标签左 / 内容右）**：统一改 `components/form/FormField.tsx`——`grid sm:grid-cols-[6rem_minmax(0,1fr)] sm:gap-4`，标签 `sm:mt-2.5 sm:text-right`，无标签时内容 `sm:col-start-2` 保持左对齐；<640px（小弹窗）自动回退上下堆叠。TextField/TextAreaField/SelectField/TagsField/ImageUploadField 全部走 FormField，改一处全覆盖（SwitchField 本就是左右布局）。
3. **返回按钮改正方形同高**：`size="icon"`(h-9 w-9) → `className="size-11"` 与 h-11 标题框等高；错误提示缩进 `pl-12`→`pl-14`（44+12=56）。
4. **编辑器只涨不缩（根因在 flex 基准）**：上一轮把页面根从 `h-[calc(100dvh-6.5rem)]` 改成 `min-h-[...]`，容器主尺寸变成 indefinite，子元素 `flex-1`(basis 0%) 的百分比基准会退化成 `content` → 高度由编辑器固定像素值撑开，于是只会随视口变高、不会变矮。
   修法：根恢复确定高度 `h-[calc(100dvh-6.5rem)]` + 兜底下限 `min-h-[560px]`（两者都是确定值，basis 0 正常生效，可涨可缩；视口过矮时停在 560px 不再收缩，超出部分由外层 main 的 overflow-y-auto 滚动）。
- 四门门禁全绿（typecheck 0 / lint 0 error / test 62 / build 1.17s）。未提交。
- 经验：flex 列容器做"撑满剩余高度"时，容器必须有**确定高度**（h- 而非 min-h-），否则 flex-basis:0 会退化成 content，动态高度失效。

## 21:1x 摘要顶部裁切 + 表单组件浅色统一白底（owner 第 7 轮）
1. **摘要 textarea 顶部激活态被裁**：上一轮给设置面板 TabsContent 只加了横向留白（`-mx-2 px-2`），纵向没留 → 首个字段的焦点环仍被滚动容器裁掉。修：内层 `max-w-2xl space-y-4` 加 `py-2`，首/末字段上下各留 8px。
   - 教训：滚动容器要防焦点环裁切，必须**四个方向都留内边距**（overflow-y:auto 会连带 overflow-x，反之纵向同样受 padding box 裁剪）。
2. **表单组件浅色统一纯白底**（owner 明确：所有表单类组件浅色模式背景都要白）：
   - `components/ui/select.tsx` SelectTrigger：`bg-background` → `bg-white dark:bg-background`。
   - `components/form/TagsField.tsx` 标签输入外壳：此前**无任何背景**（透明），补 `bg-white dark:bg-background`，并 `rounded-md`→`rounded-lg`、加 `min-h-9` 与 Input/Textarea/Select 视觉统一。
   - 列表页筛选控件（此前裸 `border border-input` 无背景，透明）：`ArticleListPage`(搜索框+状态下拉)、`UserListPage`(角色/状态下拉+搜索框)、`CommentListPage`(状态下拉) 全部补 `bg-white dark:bg-background`。
   - 未动：Switch（`data-[state=unchecked]:bg-input`，是开关不是输入控件）、ImageUploadField 预览框（`bg-muted` 占位语义）。
- 四门门禁全绿（typecheck 0 / lint 0 error / test 62 / build 1.29s）。未提交。

## 21:2x 列表筛选原生下拉 → 组件化（owner 第 8 轮）
- 全站 4 处原生 `<select>`（文章列表状态 / 用户列表角色+状态 / 评论列表状态）全部替换。
- 新建 `components/form/FilterSelect.tsx`：基于 shadcn Select 的筛选栏专用封装（Trigger 宽 9rem、h-9、白底），弹层走 Portal 不受列表容器 overflow 影响，明暗跟随主题。
- **关键坑**：Radix Select 的 Item **不允许 value 为空串**（"" 是保留的清除语义），原生写法的 `value=""` 表示"全部"会报错。约定哨兵常量 `FILTER_ALL = 'all'`，各页在 onChange 里映射回 `undefined`（即不加该筛选条件）。
- 用户页角色选项改用 `ROLE_LABELS`、评论页改用 `STATUS_LABEL`，避免与徽标文案两处硬编码不一致。
- biome 自动整理 import 顺序（3 个列表页），已确认 diff 正常。四门门禁全绿（typecheck 0 / lint 0 error / test 62 / build 1.22s）。未提交。
