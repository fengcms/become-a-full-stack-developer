# 2026-09-08 发布维护日志

## 本轮任务
owner 指令：15/16/17/18 已同步 → 镜像 + 回填；更新 19/20 内链（今日发布目标）。
（16/17 上轮已镜像，本轮实际新增 15/18。）

## 新增 URL
- M1-15 文章 CRUD 与投稿状态机 → 164453121
- M1-18 文件上传：R2 / 本地磁盘双实现与签名直传 → 164453365

发布顺序回归连续：至此 M1-01~18 全部已发布，M1-19~31 未发布。

## 索引检索坑（新增）
M1-15 用关键词「文章CRUD」搜不到——索引标题是「文章 CRUD 与投稿状态机」（**CRUD 两侧带空格**）。
规则：用 `Node 后端篇` 拉整段清单比按标题关键词逐个搜更可靠，避免漏判"是否已发布"。

## 回填（3 处，目标均已发布）
| 文件 | 行 | 占位 | 注入 |
|---|---|---|---|
| M1-17 | 128 | M1-18 | 文件上传：R2 / 本地磁盘双实现与签名直传 |
| M1-14 | 174 | M1-15 | 文章 CRUD 与投稿状态机 |
| M1-09 | 113 | M1-15 | 文章 CRUD 与投稿状态机 |

## 关于 19/20 的结论
- **M1-19**：唯一占位 `{{LINK:M1-20}}` → M1-20 未发布 → 保留。**无注入项。**
- **M1-20**：唯一占位 `{{LINK:M1-21}}` → M1-21 未发布 → 保留。**无注入项。**

连续两轮出现"今日发布目标无注入项"（上轮 M1-18 亦然）。原因：19/20 之后的文章正文对已发文章的引用多为**纯文本**（如"M1-15 讲过…"），而非 `{{LINK:}}` 占位符。属作者写作风格，非漏注。

## 证据
- grep `{{LINK:M1-(1[5-8]|0[1-9])}}`（覆盖全部已发布编号）→ **0 残留**，零漏注。
- ARTICLES.md 第 37~42 行（M1-13~18）均为 🟢 已发布；19/20 保持 🟡。

## 待办（CSDN 副作用）
本轮编辑了已发布文章：M1-09、M1-14、M1-17。需 blog AI 重新发布（更新）到 CSDN。

## 经验（沿用并强化）
- 开工先拉**全量已发布清单**×**全量占位清单**做交叉比对；按标题关键词逐个搜易漏（本轮 M1-15 空格坑）。
- 同文件多 Edit 串行；不同文件并行。
