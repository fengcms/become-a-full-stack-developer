# 2026-08-29 工作日志

## M0-06 配图提示词重写（tag 时机变更导致）
- 用户准备发 M0-06，发现之前按提示词生成的图不对——根因：tag 约定已改为里程碑式（2026-08-27），但 `docs/M0-配图中文提示词包.md` 的 `06-发布流程` 仍写「写作 → 打 git tag → 发布到 CSDN → 回填」，把"打 tag"画进发布闭环，与"M0 不打 tag、tag 在代码定稿时打"矛盾。
- 修 `06-发布流程` 提示词：闭环改为「写作 → 发布到 CSDN → 回填」三节点；加 `>` 注释强调本图不含打 tag 环节、切忌出现「打 tag / article/Mx-yy」字样。
- 顺手修 M0-06 文章第 91 行漏改的旧闭环描述「写作 → 打 tag → 发 CSDN → 回填」→「写作 → 发 CSDN → 回填」（上一轮修 tag 矛盾时漏了这处）。
- 说明：`06-monorepo结构图`（"一个 tag 锁全局"）、`06-tag流程`（用 `node-backend-v1.0` 里程碑 tag）两图提示词**已对齐里程碑式，无需重做**；仅 `06-发布流程` 必须重出图。
- 经验：配图提示词包与文章同受"tag 约定"约束，约定变更时需同步扫提示词包，避免图文再次打架。

## M0 八篇内链全量穿插完成（收官动作）
- 用户确认 M0-01~08 全部发完、blog AI 已同步本地。触发此前裁定的"全发完一次性回填 `{{LINK}}`"。
- 用受管 Python 脚本批量替换（每处断言恰好 1 次命中）：共 **30 处成功、0 失败**。
  - 19 处 `{{LINK:M0-xx}}` 占位注入真实 URL（分布：M0-01×4 / M0-02×7 / M0-03×1 / M0-04×1 / M0-05×1 / M0-06×1 / M0-07×5）。
  - 顺手修用户发布时手动穿插留下的 7 处瑕疵：5 处乱码链接文字 `[《xxx]`（M0-04 L151、M0-05 L5/L112/L133、M0-08 L124）→ 去 `《`；2 处"相关资源"列表整句重复（M0-03 L119、M0-08 L158）→ 去重。
  - `ARTICLES.md` M0-04~08 五行由 🟡 草稿 置 🟢 已发布 + 回填 CSDN 链接（164139553/164140515/164166250/164167103/164167281）。
- 验证：全仓 `{{LINK:M0}}` 残留=0、`[《` 乱码链接=0；八篇真实内链数 4/9/4/3/5/3/7/6。
- ⚠️ **CSDN 线上与本地不一致风险**：上述 7 处瑕疵是用户发布时带入线上版的，本地已修干净，但**已发布的 CSDN 文章仍含这些瑕疵**（除非用户去 CSDN 编辑修正或重发）。需提醒用户：本地 articles/ 是干净源，CSDN 线上版若需一致需手动改 7 处。
- M0 产品篇正式收官：8 篇全发 + 内链全穿插 + 镜像表全 🟢。下一步进入 M1 后端文章写作（M1-01~30，以 node-backend-v1.0 冻结代码为素材）。
