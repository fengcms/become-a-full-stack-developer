# 2026-08-29

## M2 React 管理后台 · 基础情况 orientation（作为 FrontendDeveloper expert 接入）

- 定位确认：本系列「文章是产品、代码是素材」，monorepo 单仓多目录；M2 实现目录 `manage-frontend/` 尚未创建，其他前端目录（web/app）亦未建。
- 硬地基：API 契约冻结版 `docs/api/openapi.v1.yaml` v1.11.0（OpenAPI 3.1），53 路径 / 67 操作 / 45 schema，双门全绿（结构门 + 语义门 33 OK）。**前端只消费契约、绝不改契约**；类型/请求函数应由 OpenAPI 代码生成（M2-04）。
- 联调后端：M1 `node-backend/` 已冻结（tag `node-backend-v1.0`，133 测试 + 双门全绿）。本地起法 `pnpm dev`（PORT=3000）+ `pnpm migrate` + `pnpm seed`（建首个 admin，避免「无首 admin」死锁）。API base = `http://localhost:3000/api/v1`。
- 统一信封：`{code,message,data,requestId,timestamp}`，`code=0` 成功，业务错误按 `code` 分支（不依赖 HTTP 状态码）。错误码分段：1xxx 认证 / 2xxx 授权 / 3xxx 资源 / 4xxx 参数 / 5xxx 服务。
- 鉴权要点：access token 存**内存**（绝不 localStorage）；refresh 双载体——浏览器走 HttpOnly+SameSite=None Cookie、移动端走请求体。401 无感刷新须处理并发竞态；`/auth/login`、`/auth/register`、`/auth/refresh` 返回 401 不能触发跳登录拦截（白名单）。登出清 Cookie 闭环。
- 角色三角：`member`/`editor`/`admin`。editor 管全站内容（文章/评论/分类/标签），admin 另管用户/角色/站点配置；前端权限仅体验层（M2-12）。
- M2 范围：17 篇（路线图 v1.15）。实际要落地的后台模块：工程搭建(Vite+React+TS)、布局/路由/动态菜单、请求层封装(拦截器/401/重试)、OpenAPI 代码生成、TanStack Query 服务端状态、鉴权(无感刷新+路由守卫)、列表页范式(查询↔URL 同步)、表单页范式、Markdown 编辑器(粘贴上传)、文章管理全流程(草稿/发布/下架状态机)、按钮级权限、站点配置页(M2-16)、统计看板(M2-17)。
- 下一步：待 owner 确认从 M2-01 起正式动工；动工前先 `git checkout`/确认契约基线，并以 node-backend 本地实例为联调目标。

## node-backend R2 驱动补齐 + Cloudflare 部署前置（2026-08-29，SeniorDeveloper expert）
- 背景：用户要把已冻结的 M1 后端部署到 Cloudflare（Workers+D1+R2），前端以线上接口联调。
- 调研：wrangler.toml 雏形在；D1 适配层完备；**R2 驱动是 deferred 空壳（createStorage 的 r2 分支 throw）**——上传在 CF 不可用。属冻结后增量维护。
- 决策（用户拍板）：① 涉及上传→先补 R2 驱动；② 用户本机 wrangler login、我给命令他跑；③ 绑定自定义域名；④ 附件 URL 策略 A（/files 后端中转，前端无感）。
- 改动：`src/shared/storage.ts` 补 R2Storage（minimal R2BucketLike 接口，规避 workers-types）；`src/routes/files.ts` 统一走 createStorage（local/r2 都直出）；新增 2 测试文件（storage.r2 / files.r2）；`wrangler.toml` 升级生产骨架（D1/R2 绑定激活、STORAGE_DRIVER=r2、JWT_SECRET 走 secret）。
- 门禁：tsc0 / biome0 / vitest 140 passed / 契约双门 33 OK + yaml 零变更。未 git commit（用户自管）。
- 下一步：用户本机按我给的部署命令清单跑（login→create D1/R2→回填 id→drizzle-kit generate→wrangler d1 execute 建表→seed SQL 建 admin→wrangler deploy）。部署前建议 commit 增量 + bump patch tag（如 node-backend-v1.0.1）。

## BackendArchitect · R2 部署补充代码审阅（commit 2ad49dd）
- 被审：开发 AI 提交「补齐 R2 存储驱动并升级 Cloudflare 部署骨架」(storage.ts R2Storage + files.ts 统一直出 + wrangler.toml 生产骨架 + 2 个 R2 测试)。
- 裁定：代码通过（功能正确/测试充分/契约零变更/五门全绿：tsc0、biome 117 文件 0、vitest 140 passed、契约双门 OK、yaml 0 diff）；但 **1 P1 部署阻断**——wrangler.toml 缺 `compatibility_flags = ["nodejs_compat"]`，worker 引 node:crypto/fs/path 在 CF 默认不解析，wrangler deploy 模块加载即失败。报告 `docs/node-backend/review/B-R2部署-后端代码审阅报告.md`。
- 流程：交开发 AI 按报告修 P1（补 flag，可选做 P2 收 fs 进 LocalStorage）→ 复跑五门 → 架构师复批 → owner 实际 wrangler deploy 验证启动 → R2 部署补充收口。

## 开发 AI 处理 R2 审阅报告（P1 必修 + P2 选做，2026-08-29）
- P1 已修：`node-backend/wrangler.toml` 补 `compatibility_flags = ["nodejs_compat"]`（根因：storage.ts 顶层 import node:crypto 两驱动共用，CF 默认不解析 node:*，无 flag 模块求值即崩；即便 r2 模式 local 驱动也顶层 import 了 node:fs，故不可省）。
- P2 已做：`src/shared/storage.ts` 把 node:fs/promises、node:path 由顶层静态 import 改为 `LocalStorage` 内按需动态 `import()`（模块级缓存 fsCache）+ 字符串拼接 `joinPath` 替代 `node:path.join`；移除死变量 SAFE_KEY（key 校验已统一在 files.ts 路由层）。R2 生产 bundle 不再静态引 fs。顶层仅留 node:crypto（两驱动共用 + nodejs_compat 已必需）。
- P3 四项（策略 A 中转 / 每请求重建实例 / 不存 contentType / 去重 TOCTOU）确认接受，记录为后续优化，本期不动。

## M2-01 骨架落地 + 线上联调实测（FrontendDeveloper expert，2026-08-29 下午）
- 完成 M2 React 管理后台骨架（`manage-frontend/`）：Vite+React19+TS+Tailwind4+shadcn-ui + react-router7 + Zustand5 + TanStack Query5 + RHF7+zod4 + Biome2.5.11；OpenAPI 代码生成（`src/types/api.gen.ts` 4886 行，openapi-typescript 7.13）；请求层（`src/lib/request.ts` 信封解包+401 分流+旋转令牌并发去重）、错误码（`lib/errorCodes.ts`）、三角色权限（`config/roles.ts`+`lib/permission.ts`）、菜单/守卫同源（`config/menu.ts`+`router/guards.tsx`）、auth/ui store、登录页、仪表盘探针、三状态页、占位页、布局、路由表。
- 清理模板垃圾：oxlint、icons.svg、App.css、vite.svg、hero.png；`tsconfig.app.json` 去 baseUrl（TS6 弃用）；`biome.json` 适配 2.5.11（files.includes + preset + tailwindDirectives）。
- 门禁全绿：tsc -b 0 / biome 34 文件 0 / vite build 2074 模块 0 / dev 服务器 12000 转模块 200。
- **线上联调实测（代理 `https://api-befull.kao9.com`，方案 B 同源代理）逐项验证**：
  - 信封 `{code,message,data,requestId,timestamp}` 一致；分页 `{list,pagination:{page,pageSize,total,totalPages}}` 与 `Page<T>` 完全一致。
  - 公开 `/stats`、`/categories/stats` code:0；**`/site/settings` 返回 5000**（线上库未种子 site_settings 行 → 后端数据问题，非前端路径错）。前端 `usePublicSiteSettings` 已加 `retry:false` + 侧栏回落默认品牌名，不崩溃。
  - 登录 AuthResult = accessToken(JWT,role 在载荷,expiresIn 3600) + refreshToken(体) + HttpOnly Secure Cookie + user(role:member 等)。
  - 401 分流证据：无令牌→HTTP 401 + code 1004；伪令牌→HTTP 401 + code 1002（可刷新）；会员撞 /users、/admin/articles→code 2001（服务端角色闸）；refresh 经**体 refreshToken**→code:0 换新令牌（主路径，HTTP 可用）；刷新令牌旋转后旧值→code 1003（强制登出，正确）。
  - 登出 POST /auth/logout 须带 Bearer（x-authz minRole member）+ 体 refreshToken → code:0（前端 http.post 自动带 Bearer，正确）。
- **关键实测陷阱（已规避，非代码 bug）**：① 后端 Set-Cookie 带 `Secure`，curl/浏览器在 `http://localhost:12000` 不回传该 Cookie → 基于 Cookie 的静默恢复仅 HTTPS(生产) 生效；开发期靠**内存 refreshToken(体)** 保活，刷新页面需重登（dev 已知限制，待 owner 定是否上 HTTPS 或后端按环境去 Secure）。
- 路径核对：所有 `api/*.ts` 端点路径与契约逐项一致（/auth/me、/me/profile、/me/change-password、/site/settings、/stats、/categories/stats、/admin/articles/* 等）。
- 副作用：联调时注册了探针会员 `probe_member_x`（最低权限）于线上库，待 owner 用 admin 删或确认保留。
- 下一步：M2-02 起逐模块落地真实表格/表单（文章/评论/分类/标签/用户/站点设置/资料/改密）；admin 路径需 owner 提供 admin 凭据做浏览器点测。
- 门禁复绿：tsc0 / biome 117 文件 0 / vitest 140 passed / 契约双门 33 OK / yaml 0 diff。回复文档 `docs/node-backend/review/B-R2部署-后端代码-开发AI回复.md`。
- 未 git commit（用户自管）。待 owner 本机 `wrangler login` → 填 D1/R2 id + CORS/域名 → `wrangler deploy` 真实 CF 运行时终验 P1 修复。

## BackendArchitect · R2 部署补充 复批（提交 c900ea3）
- 开发 AI 回复 `B-R2部署-后端代码-开发AI回复.md` + 提交 `c900ea3`「补全CF部署兼容性配置并优化本地存储导入」。
- **复批通过（代码层面）**：P1 落实（wrangler.toml:7 `compatibility_flags = ["nodejs_compat"]`）；P2 落实（storage.ts 顶层移除 node:fs/node:path，改 LocalStorage 内动态 `import('node:fs/promises')`，R2 模式永不触发）；P3 接受。五门独立复验全绿：tsc0 / biome 117文件0 / vitest 140 / 结构门OK / 语义门33OK；yaml 0 diff；变更仅 storage.ts+wrangler.toml。
- 新观察（P3 非阻断）：P2 移除 storage.ts `SAFE_KEY`/`resolveKey` 第二层路径穿越防御；穿透核验调用链（files.ts:35 先校验、attachment.ts:151 传 DB 内容寻址 key、put 内部生成）当前无穿越可利用，但 storage 层不再自保，建议冻结后补回或记录信任不变量。
- 复审批复 `docs/node-backend/review/B-R2部署-复审批复.md`；权威终验仍需 owner 本机 `wrangler deploy` 真实 CF 运行时验 Worker 启动。

## node-backend 部署操作文档落地（2026-08-29，SeniorDeveloper expert）
- 动因：R2 部署的分散决策/命令散落多轮对话，owner 要可自操作手册。
- 交付：`docs/node-backend/部署到Cloudflare指南.md`——阶段0-5 复制即跑命令（wrangler login / secret put JWT_SECRET / d1+r2 create 回填 id / drizzle-kit generate + wrangler d1 execute 建表 / D1 等价 SQL 建首 admin / wrangler deploy + 自定义域名 DNS）、验证、回滚、常见问题、附录 toml 模板 + D1 seed SQL、§9 决策记录。
- 已对齐冻结后真实状态：nodejs_compat 已配、R2 驱动已补、drizzle dialect=sqlite、seed-users.ts 底部备 D1 SQL；明确无 deploy npm script（直接 wrangler deploy CLI）、JWT_SECRET 走 secret 不进 git。
- 下一步：owner 本机按文档自部署；部署前建议 commit 增量 + bump patch tag。

## node-backend 部署实跑验证 + R2 绑定名修正（2026-08-29 续）
- 用户本机 `wrangler deploy` 成功（api-befull.kao9.com 自定义域名、D1/R2 绑定挂载、34ms 启动）。冒烟 `GET /api/v1/articles` 返回 200 + `total:0` 空列表 → 证明 Worker 在线、D1 表已建且可查（非 500 说明表存在）、CORS 生效、响应信封中间件完整跑通。
- 隐患：活的 `wrangler.toml` 的 `[[r2_buckets]]` binding 误写为 `node_backend`，与 `src/config/env.ts` 的 `R2_BUCKET` 不一致 → 线上 `STORAGE_DRIVER=r2` 时 `createStorage` 抛「R2_BUCKET 未绑定」(storage.ts:129)，上传/附件接口全 500。已修正 `binding = "R2_BUCKET"`（部署指南附录 8.1 模板本就正确，活的 toml 曾漂移）；grep 全仓确认无其它 `node_backend` 引用；FAQ 补该坑。
- 用户确认 admin 已 seed 到 D1。本次为纯配置变更，冻结代码未动，六门门禁不受影响，无需重跑。
- 下一步：owner 本机 `cd node-backend && wrangler deploy` 重部署；部署后 R2 冒烟：①`POST /auth/login`{username,password} 换 token ②`POST /upload` 带 Bearer 上传 ③`GET /files/<key>` 验证读路径。

## admin 密码重置成功（2026-08-29 续）
- owner 用重算的 `bcryptjs(rounds=12)` 哈希（`Admin.123`）经 D1 UPDATE 重置后，`POST /auth/login` 返回 `code:0` + accessToken，user.role=admin/status=active。**确认根因是旧 seed 哈希与线上 hashPassword 不同源**，现已同源修复。
- 登录响应字段是 `data.accessToken`（非 `token`），后续抽 token 用 `['data']['accessToken']`。
- 当前仅登录通；R2 绑定名修正（wrangler.toml:35 → R2_BUCKET）**尚未重部署**，故 `POST /upload` 线上仍会 500。下一步：owner 重部署 + R2 冒烟。

## node-backend 部署 + 联调全链路 GREEN（2026-08-29 收官）
- owner 已重部署（R2 绑定名修正生效）。`POST /api/v1/upload`（admin token）返回 `code:0` + `data.storage:"r2"` → **R2 写链路线上通**，证明 wrangler.toml 的 `binding="R2_BUCKET"` 已生效、env.ts 读取对齐。
- 上传返回 key=`a4dd28db6e6d3fc0d43cdbef1e8ef161b353ce67d27e81d400f796bc77045ae6.png`（内容寻址 sha256+ext），`GET /files/<key>` 为读回验证（策略 A 中转）。
- 至此：Worker 启动 / D1 查询 / CORS / 登录(admin, bcryptjs12 同源) / R2 读写 全部线上验证通过。后端联调地基齐活，可正式交给前端以 `https://api-befull.kao9.com/api/v1` 联调。
- 用户侧踩坑记录（已写入部署指南 FAQ）：① wrangler.toml R2 binding 名须对齐 env.ts 的 R2_BUCKET；② D1 改密码的哈希须 bcryptjs(12) 同源且含 `$` 须用 heredoc 文件执行；③ curl `-F file=@~/x` 的 `~` 不被 curl 展开、须绝对路径且文件须在本机。

## R2 读回 404 根因（2026-08-29，非存储 bug）
- 现象：`POST /upload` 200 + `storage:"r2"`（写通），但 `GET /api/v1/files/<key>` 返回**纯文本 `404 Not Found`**（非 JSON 信封）。
- 根因：文件路由 `app.ts:69` 刻意挂在根 `app.route('/files', filesRoute)`（策略 A：附件直出不进 /api/v1 前缀），而其余路由都在 `/api/v1` 下。用户测的 `/api/v1/files/<key>` 未注册 → Hono 默认 404。**不是 R2 读不出，是 URL 前缀错**。正确取法是 `https://<domain>/files/<key>`（域名根 + /files，不带 /api/v1）。
- 契约 `Attachment.url` 仅定义为 `string(format:uri)`（openapi.v1.yaml:447），未限定前缀；代码策略 A 返回 `/files/<key>`（根相对）。设计自洽，但**前端须用 `ORIGIN + url` 而非 `API_BASE + url` 拼接**，否则拼出 /api/v1/files 会 404（M2 前端联调关键坑，已写 FAQ）。
- **已复测 GREEN**：`GET https://api-befull.kao9.com/files/a4dd28db...ae6.png` 返回 `200` + `content-type: image/png` + `content-length: 70` + `x-content-type-options: nosniff`，二进制体正确透出。**R2 读路径线上彻底打通**，404 根因确为 URL 前缀误用（非存储 bug）。
- 结论：node-backend 部署到 Cloudflare **全链路验收完成**——Worker 启动 / D1 查询 / CORS / 登录(admin,bcryptjs12 同源) / R2 读写 全部线上实测通过。后端联调地基齐活，正式交接前端以 `https://api-befull.kao9.com/api/v1` 联调（附件用 `ORIGIN + /files/<key>`，避开 /api/v1 前缀坑）。
- 验收报告已沉淀：`docs/node-backend/M1-后端部署到Cloudflare-验收报告.md`（含六门门禁表 tsc0/biome117文件0/vitest140/契约双门33 OK/yaml0diff、六条线上实测证据、四坑 FAQ、后续 commit+bump patch tag 建议、相关文档索引）。

## 吸收 telemarketing-saas-manage 经验资产 + M2 联调准备（2026-08-29，FrontendDeveloper expert）
- 吸收对象：`/Users/fungleo/Sites/ShangHaiYingZhou/telemarketing-saas-manage/docs-extract/` 12 篇经验文档 + 其 `src/` 源项目（Vite6+React19+TS+Tailwind4+shadcn 电销 SaaS 后台，已上线）。产出 `docs/prd/M2-前端开发准备简报.md`。
- 直接复用：技术栈/依赖、视觉令牌(`src/index.css`)、目录结构、配置驱动权限范式、状态分层、RHF+Zod 表单/两阶段弹窗/抽屉、shadcn 组件清单、pnpm11 bug/双key 漏刷/Page<T>/登出豁免等踩坑纪律。
- **契约差异（请求层须改写）**：① 信封 `code:0` 成功（参考用 `success` 布尔）；② base `/api/v1`；③ 错误码数字分段；④ 刷新走 **HttpOnly Cookie**（`credentials:'include'`，accessToken 内存不 localStorage）；⑤ 附件 `ORIGIN+/files/<key>` 不带 /api/v1。
- **实测线上靶**：`GET /api/v1/health`→`{code:0,...}`；登录 401→`{code:1001,...}`；CORS 返 `allow-credentials:true`+`vary:Origin` 但**不回显 `access-control-allow-origin`**（example.com/localhost:5173/12000 三源同现）。
- 🔴 CORS 阻塞：凭据模式缺 allow-origin 浏览器会拦截 → 开工前需 owner 拍板：方案A 把 `http://localhost:12000` 加入部署后端 `CORS_ORIGINS`（推荐，贴合契约 Cookie 刷新）；方案B Vite 代理+内存 refreshToken 走 body（dev 零后端改动，偏离契约）。已写入简报 §6 + 待拍板 3 项（含 UI 库 shadcn 确认、类型手写 vs OpenAPI 代码生成）。
- dev 端口固定 12000；联调靶 `https://api-befull.kao9.com/api/v1`（已验收 GREEN）。下一步：owner 拍板联调架构后起 M2-01 脚手架落 `manage-frontend/`。

## M2 确立前端开发规范 + 第一波代码回炉（2026-08-29 晚，FrontendDeveloper expert）
- 用户补 5 条规范：① 文档全部归 `docs/manage-frontend/`；② 函数一律箭头（禁 `function`）；③ 文件头注释 + 函数 TSDoc；④ 单文件 ≤200 行（特例文件头注明）+ 页面组件单文件单职责；⑤ 如上。
- 规范落盘：`docs/manage-frontend/开发规范.md`（含豁免清单：`components/ui/*`、`api.gen.ts`、`vite-env.d.ts` 豁免箭头/TSDoc 但留来源头）。
- 错放文档 `M2-骨架联调报告.md` 从 `.workbuddy/` 迁至 `docs/manage-frontend/`。
- 回炉改造（41 个手写文件全箭头化 + 标准文件头 + 函数 TSDoc）：
  - `src/lib/request.ts`(350) 拆 `request/{errors,session,core,helpers,index}.ts`；`core.ts` 258 行高内聚，头注明 §4 例外。
  - `AdminLayout`(265) 拆 `Sidebar.tsx`+`Topbar.tsx`；`LoginPage`(205) 拆 `LoginForm.tsx`；`StatePages`(98) 拆 3 错误页+`StateShell`+`index.ts` barrel；`DashboardPage` 抽 `StatCard` 到 `components/dashboard/`。
  - 其余 `api/*`、`config/*`、`hooks/*`、`lib/*`、`store/*`、`router/*`、`pages/*`、`types/common`、`vite-env` 全部转箭头/补注释。
- 门禁复绿：tsc -b 0 / biome 46 文件 0 / vite build 2086 模块 0 / dev 12000 转译 200。合规核对：手写文件 `function` 声明 0；超 200 仅 `core.ts`(258) 与 `router/index.tsx`(201，路由目录已注明)。
- 报告：`docs/manage-frontend/M2-代码规范改造报告.md`。未 git commit（用户自管）。

## site_settings 种子缺口确认（2026-08-29 后端收官后补充，SeniorDeveloper expert）
- 用户问 `/site/settings` 是否需种子数据。查证：`GET /site/settings` 公开接口在 `site_settings` 表无 `id=1` 行时直接 500（`src/services/site.ts:42` 空表抛 INTERNAL）。默认种子仅在应用层 `pnpm migrate`（`src/db/migrate.ts:188` onConflictDoNothing 插 id=1），但线上走 `drizzle-kit` 纯 DDL 建表路径（`0000_windy_songbird.sql` 经 grep 确认只 CREATE TABLE 无 INSERT）→ 线上缺此行会 500。
- 结论：**需要种子数据**。已落实两份产物：① 新建 `node-backend/scripts/seed-site-settings.sql`（幂等 `INSERT INTO site_settings (id, site_name, site_description, updated_at) VALUES (1,'成为全栈开发工程师','全栈开发工程师的成长笔记与实战专栏', unixepoch()*1000) ON CONFLICT(id) DO NOTHING;`）；② 部署指南新增「阶段 4.5 种子站点配置」+ FAQ 一条。
- owner 须本机执行 `cd node-backend && wrangler d1 execute node-backend --remote --file=./scripts/seed-site-settings.sql` 补种子（沙箱无 CF 凭证，按惯例 owner 跑）。

## M2 业务模块开发计划（2026-08-29 晚，FrontendDeveloper expert）
- 用户指令：联调遗留（`probe_member_x` 清理 + `/site/settings` 5000）不碰，交后端 AI；前端先出「可行的开发计划文档」。
- 产出：`docs/manage-frontend/M2-开发计划.md`。基线：骨架 M2-01~09 已冻结（门禁全绿、线上联调 GREEN），路线图 M2 共 17 篇，待建 M2-10~17 + 评论/分类/标签/用户/个人中心真实页。
- 结构：§0 现状基线 / §1 技术约束(引用开发规范.md+契约v1.11.0) / §2 模块↔文章映射 / §3 Phase0 基础件(DataTable/FormField/ConfirmDialog/useToast/Markdown编辑器<Can>) / §4~10 Phase1~7 业务模块(文章/评论/分类标签/用户/仪表盘/站点配置/个人中心,逐端点列出) / §11 Phase8 跨切面(构建/部署/复盘) / §12 节奏 / §13 风险(R1 编辑器选型待拍板/R2 CORS/R3 recharts 是否装/R4 联调遗留交后端/R5 GET /me/likes 契约矛盾) / §14 总结。
- 已建 9 张阶段任务卡(#12 Phase0 ~ #20 Phase8)供后续逐批施工；均 pending，未动工。
- 关键约束重申：文档只归 `docs/manage-frontend/`；每阶段门禁全绿再进下一阶段；新端点先 curl 实测后编码，差异入差异表交后端 AI。

## M2 F0.5 Markdown 编辑器选型定案 + Phase1 文章管理模块（2026-08-29 深夜，FrontendDeveloper expert）
- **F0.5 编辑器选型变更（重要）**：R1 用户原话「要找一个好用的 markdown 编辑器来实现这个功能，这是这个管理系统的核心功能」→ 计划原拟 TipTap，但 **TipTap v3 已移除 `@tiptap/extension-markdown`**（markdown 序列化脆弱），且契约 Article.content 是纯 string（max 65535）。故改采 **@uiw/react-md-editor v4.1.2**（CodeMirror6：左写右预览 + 工具栏 + 暗色跟随 next-themes `useTheme()`）；粘贴/拖拽图片走 `POST /upload` 自动上传并插入 `![alt](url)`（用 `fileUrl()` 修正 `/files` 根路径，避开 /api/v1 前缀坑）。预览 CSS 需 `@uiw/react-markdown-preview/markdown.css`，因 pnpm 严格解析将其加为直接依赖（`@uiw/react-markdown-preview` 5.2.1）。
- F0.5 新增：`src/api/attachments.ts`(uploadFile/listMyAttachments/deleteAttachment) + `src/hooks/useImageUpload.ts` + `src/components/editor/MarkdownEditor.tsx`（受控组件 value/onChange，textaareaProps 挂 onPaste/onDrop 以满足 a11y 静态元素规则）。Phase0(#12) 收口完成。
- **Phase1 文章管理模块(#15) 完成**，端点严格对齐契约 v1.11.0：`GET /admin/articles`(list,editor+) / `POST /articles`(create,member+) / `PUT /articles/{id}`(update,editor或作者) / `DELETE /articles/{id}`(软删) / `POST /admin/articles/{id}/approve`(pending→published) / `POST /admin/articles/{id}/status`(admin 强改,hook 已建，表单 status 选择+保存即可覆盖)。
- Phase1 新增：`src/api/categories.ts`(listCategoryTree GET /categories/tree) + `src/hooks/useArticles.ts`(useAdminArticles/useArticle/useCreateArticle/useUpdateArticle/useDeleteArticle/useApproveArticle/useSetArticleStatus) + `src/hooks/useCategories.ts` + `src/components/form/TagsField.tsx`(chip 输入) + `src/pages/articles/ArticleListPage.tsx`(DataTable+筛选+分页+过审/删除确认) + `src/pages/articles/ArticleFormPage.tsx`(RHF+zod+MarkdownEditor，编辑态 GET /articles/{id} 预填)。router 注册 /articles、/articles/new、/articles/:id/edit。
- 契约校正：既有 `src/api/articles.ts` 骨架路径已与 v1.11.0 一致（`PUT /articles/{id}` 非 PATCH、/admin/articles 正确），未改。
- 门禁全绿：typecheck 0 / lint 0 / build 通（ArticleFormPage chunk 1MB 因 md-editor 引 CodeMirror+highlight.js，懒加载，仅 advisory 体积警告，非错误）。
- 未 git commit（用户自管）。下一步：Phase2 评论审核(#14) → Phase3 分类/标签(#13)。

## M2 前端简报「items」笔误纠正（2026-08-29 收官后，SeniorDeveloper expert）
- 用户指出文章列表接口返回 `data.list`「是否与契约不一致」。查证三处一致：① 02 领域模型 `docs/prd/02-领域模型与API契约.md:418`「列表统一返回 `{ list, pagination }`」；② openapi `ArticlePage.list` 为 `array<ArticleSummary>`（190-200）；③ 代码 `src/services/article.ts:149` `ArticlePage { list: toArticleSummary[]; pagination }`。**三者都是 `list`** → 接口返回 list 正确符合契约，非 bug。
- 不一致感根因：我写的 `docs/prd/M2-前端开发准备简报.md:100` 误抄 telemarketing-saas-manage 参考项目的 `items` 约定（原句「取 data.items」）。已纠正为「取 `data.list`」并标注本契约字段名是 list 非 items、`Page<T>={list, pagination:{page,pageSize,total,totalPages}}`。
- 契约/代码不动（冻结铁律）。🔴 待办：须核查 manage-frontend 前端若已按 `items` 取数（`types/common.ts` 的 Page 定义 + 请求层取数 / DataTable 泛型）需改 `list`，否则联调取不到数组——M2 Phase1 文章管理已施工，应重点查 `useArticles`/`ArticleListPage` 的取数。

## M2 前端第一轮架构审阅（2026-08-29，eno 前端架构分析专家）
- 审阅对象：`manage-frontend/`（提交 `f0f4172`「Phase1 文章管理模块 + Markdown 编辑器」）。对照基准：M2 简报、冻结契约 v1.11.0、后端运行时 `node-backend/src/shared/response.ts`。
- 报告：`docs/manage-frontend/review/M2-前端代码第一轮审阅报告.md`。方法：不采信自陈，结论全部基于读源码 + 独立跑门禁 + 对照冻结契约/后端运行时取证。
- 总评：**综合 80/100 ⭐⭐⭐⭐，首期可继续推进（非阻塞）**。架构骨架、契约对齐、权限模型达生产级；有两处偏离约定 + 工程化空白需在规模化前清零。
- 🔴 **P2-1（待 owner 拍板）CORS 方案 B 被擅自定为开发默认**：`vite.config.ts:8` 注释「走同源代理，绕开 CORS（方案 B）」——但简报 §6/§7 将此标为「待 owner 拍板」硬阻塞项（方案A 改后端 `CORS_ORIGINS`+Cookie 刷新 / 方案B Vite 代理+内存 refreshToken 走 body）。代码实现方案 B 自洽正确，但决策权属 owner。副作用：dev 代理同源使 `core.ts`「空体→HttpOnly Cookie」分支永不触发，该契约分支 dev 无实测覆盖。
- 🔴 **P2-2** `tsconfig.app.json` 未开 `strict:true`，偏离简报 §4（阶段1 要求 strict+noUnused）。当前仅开 noUnusedLocals/Parameters，strictNullChecks/noImplicitAny 核心严格族未生效；趁模块少应早补。
- 🟡 **P3-1** `@tanstack/react-table@9` 死依赖（DataTable 自建列模型，零引用）；**P3-2** `ArticleFormPage` 1.06MB chunk（md-editor 未拆）；**P3-3** `ArticleListPage` 状态色硬编码 `bg-slate-200` 等（偏离令牌纪律）；**P3-4** 仓库根 3 个 0 字节 `_tmp_*` 已被 git 跟踪（删+`.gitignore _tmp_*`）；**P3-5** 零测试/零 CI/零 hook。
- 门禁独立复验全绿：`tsc -b` 0 错 / `biome check` 66 文件 0 issue / `vite build` 成功（仅 ArticleFormPage 体积告警）。
- 契约一致性专项 47/50（⭐⭐⭐⭐⭐）：信封 `code:0`/错误码数字分段/`fileUrl` 根路径/分页 `list`/`Page<T>`/`data?.list`、角色三角、ownerOverride 全部现场核验对齐冻结契约 + 后端运行时 `paginate({list,pagination})`——**其中 `data?.list` 取数已确认正确**（同步回应上条「items」笔误：前端代码用的是 `list` 非 `items`，简报 doc 笔误不影响代码）。「七端一致」最高价值维度确认前端请求层适配正确。
- ℹ️ `core.ts` ~210 行超 200 软上限，文件头已自陈论证（拆碎致环依赖），属合理例外不扣分。
- 待 owner 决策：① P2-1 CORS 方案 A/B；② P2-2 是否开 strict。后续可交开发 AI 整改后由我（BackendArchitect）做第二轮复批（不采信自陈）。

### 第一轮审阅修订（2026-08-29 晚，owner 裁决后）
- owner 两裁决：① **方案 B 代理模式是 owner 的暂定决策，非 AI 自主拍板** → 初版把 P2-1 判「🔴 擅自决定」系误判，已更正为 ✅ 已确认决策（保留「Cookie 分支 dev 无实测」已知权衡 🟡）；② **strict 要开启** → P2-2 由「🔴 偏离简报」降为 🟡 已确认整改项（A-P2-2）。
- 报告已同步修订：评分联动上调 技术栈 40→45 / 契约一致性 47→50 / 综合 80→83（⭐⭐⭐⭐）；新增 §10 修订记录。首期无阻塞项，两处 🔴 解除。
- 下一步：开发 AI 落实 A-P2-2（开 strict 修类型错）+ 视节奏清 P3；完成后交我（BackendArchitect）第二轮复批（不采信自陈）。

## M2 分页「items」纠正报告（可转交前端 AI 自检）
- 用户要一份转交前端 AI 的纠正报告。已落实 `docs/prd/M2-分页字段名纠正报告.md`：澄清「后端返回 list 正确、与契约一致；YAML 里 `items` 是 OpenAPI 数组元素保留字非字段名；误读来自参考项目约定 + 简报已纠正笔误」；附当前前端代码核查表（Page<T> 用 list、ArticleListPage 用 data?.list、request 层透出 envelope.data、Pagination 字段一致 → 文章模块无需改）+ 后续列表模块自检清单（禁 data.items、分页用 data.pagination.{page,pageSize,total,totalPages}、统一用 common.ts 的 Page<T>、重生成 api.gen.ts 后复查 list 非 items）。

## M2 第一轮审阅整改全量收口（2026-08-29 深夜，FrontendDeveloper expert）
- 输入两份文档：《M2-分页字段名纠正报告》（后端）+《M2-前端代码第一轮审阅报告》（审阅专员 eno，综合 83/100）。整改后产出回复 `docs/manage-frontend/review/M2-第一轮审阅回复.md`。
- **分页报告：无需改代码**（文章模块本就用 `data.list`，报告判断正确）。但把其 §5 自检清单固化为测试 `src/api/articles.test.ts`——含**反向断言** `(page as unknown).total/.items` 均为 undefined，一旦有人抄参考项目的 `{items,total}` 习惯，测试立刻红而非页面静默空白。
- **A-P2-2 开启 strict**：`tsconfig.app.json` 加 `"strict": true`，**0 类型错误**。用哨兵文件反向验证门禁真在工作（TS18047 's' is possibly null / TS7006 implicit any 均被拦），并用 `tsc --showConfig` 确认生效值。现有代码零错误是因为此前已习惯 `??` + `isApiError` 守卫。
- **P3-1** 移除 `@tanstack/react-table`（src 内仅注释提及、零引用）；**P3-4** `git rm` 三个 `_tmp_*` + `.gitignore` 加 `_tmp_*`（`git check-ignore` 验证生效）。
- **P3-3** 状态色抽语义令牌：`index.css` 加 `--status-{draft,pending,published}-{bg,fg}`（`.dark` 改深底亮字）+ `@theme inline` 映射，组件改用 `bg-status-draft text-status-draft-fg`。
- **P3-2（重点，体积 -47%）**：两步。① `manualChunks` 把编辑器生态归入独立 chunk → `ArticleFormPage` 1.06MB → 8.79 kB（报告核心诉求达成）。② 继续归因发现真凶：`rehype-prism-plus` 默认入口**同时** import `refractor`(36 语言) 与 `refractor/all`(**297 全量**)，default 导出用全量版 → 新增 `build/refractor-languages.ts`，vite alias **只顶替 `refractor/all`**（common 36 + 补 jsx/tsx/nginx/docker/http = 41 种）。md-editor chunk **1,059.87 → 563.94 kB**（gzip 363.46 → 180.24，-50%）；模块数 3156→2849。

## 分类 PUT 省略 parentId 静默挪根 坑（前端 AI 预警，SeniorDeveloper 归因）
- 前端 AI 报：`CategoryNode` 无 `parentId`，且 `PUT /categories/{id}` 全量替换，编辑子分类漏传 parentId 会被置空、静默挪到根。
- 归因结论（三者混合）：① **前端对 `CategoryNode` 的理解有误**——`CategoryNode`(openapi:355-367)是递归树节点，刻意用 `children` 嵌套表达父子，**本就不该有 parentId**；`parentId` 存在于 `Category` 实体(352)与 PUT body(1556)，字段未缺失。② **真正的坑是后端实现 bug**：`src/services/category.ts:312` `const newParent = input.parentId ?? null` + `:335` `parentId: newParent` → 省略即写 null；而同函数 `:336` `sortOrder: input.sortOrder ?? existing.sortOrder` 却保留原值 → **同一函数内语义不一致**，证明不是有意的"全量替换"设计。③ **契约欠规范（次要）**：PUT body `required:[name,slug]`(1558)、parentId 可选，但从未写明"省略字段保留原值"还是"省略即置空"；且用 PUT（REST 暗示全量替换）却只 require name/slug，语义自相矛盾。
- 推荐修复：`category.ts:312` 改 `input.parentId ?? existing.parentId`（与 sortOrder 对齐，局部更新语义），`description` 同理；同步契约 description 注明"省略字段保留原值"。触及冻结契约行为变更，须走 增量维护：先改 YAML → 改代码 → 复跑双门（tsc/biome/vitest + check_contract.py 33 OK + yaml diff）。待 owner 确认是否开工。
  - 关键发现：**官方 common 集（36 种）没有 jsx/tsx** —— React 技术栈后台文章里 jsx/tsx 高频，直接用 common 会让高亮静默失效，故必须补。`html` 无需补（markup 自带 html 别名）。
  - 两个 alias 踩坑：① 不能写 `refractor/lib/common.js`（不在包 exports 里，解析失败）；② 不能 alias 裸 `refractor`（该文件自身 import 它 → 自循环）。
  - 产物 grep 验证：jsx/tsx/nginx/docker 在，abap/agda/apl 为 0。
- **P3-5 测试+CI**：装 vitest 4.1.11（`environment:'node'`，复用 vite alias），**28 个测试 / 5 文件**覆盖审阅点名四条路径：permission(10)、信封解析(7)、**401 并发刷新去重**(3，刷新接口加 20ms 延迟撑开并发窗口防 flaky)、分页 list 消费(4)、fileUrl(4)。CI `.github/workflows/manage-frontend-ci.yml`（仅前端子目录变更触发，串 lint/typecheck/test/build）。
  - CI 用 `biome check .`（只读）而非本地 `--write`，当场抓到 2 处 import 排序问题——本地 --write 会自动改、永远"看着绿"。
- 门禁四门全绿：typecheck 0（strict 开）/ biome 73 文件 0 issue（只读）/ vitest 28 passed / build 通。
- 依赖变化：➖`@tanstack/react-table`、➕`vitest`(dev)、➕`refractor@5.0.0`(dev，构建期语言集)。

## M2 前端第一轮整改第二轮复批（2026-08-29 深夜，eno 前端架构分析专家）
- 输入：开发 AI 回复 `docs/manage-frontend/review/M2-第一轮审阅回复.md` + 实际代码（基线 `f0f4172` → 整改 `3b82a15`）。
- 方法：不采信自陈——独立跑四门禁 + git 取证 + 读源码 + 对照契约/后端运行时。报告 `docs/manage-frontend/review/M2-前端代码第二轮审阅报告.md`。
- **复验结论：第一轮全部待整改项（A-P2-2 + P3-1~P3-5）真实闭环，无阻塞项。**
  - A-P2-2：`tsconfig.app.json:25` 有 `"strict":true`；`tsc --showConfig` 实测输出 `"strict": true`（确为生效值，非仅改文件）；`pnpm typecheck` 退出码 0。
  - P3-1：`package.json` 全量无 `@tanstack/react-table`。
  - P3-2：`vite build` 实测 md-editor **563.94 kB / gzip 180.24**、ArticleFormPage **8.79 kB**（首轮 1.06MB）→ 与回复数字吻合。
  - P3-3：`ArticleListPage` 用 `bg-status-*`；`index.css` 定义双主题 `--status-*-bg/-fg` + `@theme inline` 映射（暗色改深底亮字保对比度）。
  - P3-4：`git ls-files | grep _tmp_` = **NONE_TRACKED**（初轮我对 diffstat 显示 0 字节的疑虑已排除，确已真删）。
  - P3-5：`vitest run` 5 文件 28 测试全过；CI workflow 存在且用只读 `biome check`。
- **最值得肯定**：开发 AI 把两条「会静默失败」的风险固化成 CI 守卫——① 分页 `articles.test.ts` 反向断言 `data.items`/`data.total` 为 undefined（防参考项目 `{items,total}` 习惯串味致静默空白）；② `refresh.test.ts` 401 并发刷新去重（刷新接口 20ms 延迟撑开并发窗口，断言只打 1 次 refresh + 新令牌落内存、无 localStorage）。测试质量高于一般冒烟。
- **评分联动上调：综合 83 → 88/100（⭐⭐⭐⭐⭐）**。技术栈 45→48、工程化 30→42、性能 37→40；架构 45/契约 50 维持。未给 90+：仅 Phase1 模块少，工程化随规模仍有爬坡空间。
- **owner 提问「测试是否该放 tests 目录」→ 专业建议（不盲从）**：co-location（源文件同目录 `*.test.ts`）是 Vitest+Vite+React+RTL+shadcn 生态的**事实标准/主流做法**；集中 `tests/` 反而是次主流（多见于后端/旧 Jest）。不建议改。建议：维持 co-location，仅把测试约定（位置/命名/环境）补进 `docs/manage-frontend/开发规范.md` 防后续混乱。若 owner 仍坚持集中式，需改 vitest include + 重配 alias，非标准且收益小，待 owner 拍板再动。
- 遗留两项均 owner 已裁决、不阻塞：① 方案 B Cookie 分支 dev 无实测（转上线前 checklist）；② `openapi-typescript@7` 要求 ts@5.x 实际 ts@6（既存，仅影响类型生成工具）。
- **待 owner 拍板 3 项**：① md-editor 仍 563.94 kB 超 500 kB 告警线，若要压到线下须放弃代码高亮（换 nohighlight 变体可再省 ~200 kB），我建议保留高亮（懒加载+gzip 180kB 仅编辑页一次）；② 方案 B 的 Cookie 刷新分支 dev 无实测，上线前需明确验证方式；③ 语言集 297→41，请目视复核 jsx/tsx/bash/sql 预览高亮。
- ℹ️ 既存 peer 警告（非本轮引入）：`openapi-typescript@7.13` 要求 ts ^5，项目 6.0.3。
- 未 git commit（owner 自管），建议本轮整改单独提交。下一步：Phase 2 评论审核（#14）。

## 分类 PUT 静默挪根 修复落地（冻结后首个行为 bug 增量维护）
- owner 确认开工。按增量维护纪律：**先改契约 YAML 注明省略字段语义 → 改实现 → 补回归测试 → 复跑双门**。
- 改动三文件：`docs/api/openapi.v1.yaml`(+2 行说明 PUT 为局部更新、可选字段省略保留原值)；`src/services/category.ts:312` `input.parentId ?? existing.parentId`（原 `?? null`）、`:334` `input.description ?? existing.description`（原 `?? null`），与 `:336` sortOrder 对齐成统一局部更新语义；`test/routes/categories.test.ts`(+26 行)新增「更新时省略 parentId/description 保留原值（不静默挪根）」断言。
- 六门门禁全绿：tsc 0 / biome 0（117 文件，格式自动修复 1 处）/ vitest 141 passed（原 140 + 新增 1，categories 单文件 11 passed）/ openapi_spec_validator OK / check_contract.py 语义门全过（33 OK 维持）/ yaml diff 仅 +2 行说明（有意增量，非违约）。
- 🔴 **关键交接**：改的是部署到 Cloudflare 的 Worker 代码，**必须 `wrangler deploy` 重新部署线上才生效**；当前线上仍是旧 `?? null` 逻辑。契约 YAML 改动仅文档，无需部署。建议 owner：① git commit 增量；② bump patch tag（如 node-backend-v1.0.1）；③ `cd node-backend && wrangler deploy`。
- 前端 AI 无需改代码（前端本就该回传 parentId；现后端即使漏传也不挪根，更安全）。

## M2 前端 Phase2+Phase3 第三轮审阅（2026-08-29 深夜，eno 前端架构分析专家）
- 输入：开发节点文档 `M2-Phase2-NOTES.md` + `M2-Phase3-NOTES.md` + 实际代码（基线 `3b82a15` → `f98ed5e` + 文档修正 `d56868c`）。21 文件 +2126 行；新增评论审核 / 分类树 / 标签三模块 + 17 测试（总数 28→45）。
- 方法：不采信自陈——独立跑四门禁 + git 取证 + grep 冻结契约 openapi.v1.yaml + 读源码 + 对照后端运行时。报告 `docs/manage-frontend/review/M2-前端代码第三轮审阅报告.md`。
- **门禁全绿**：typecheck 0（strict 生效）/ biome 89 文件 0（只读）/ vitest 45 passed（8 文件）/ build 通（md-editor 563.94 kB 未退化）。与节点文档自报吻合。
- **契约一致性（端点全对齐 v1.11.0，grep 验证存在）**：`GET /admin/comments`(2119)、`PATCH /comments/{id}/status`(2056)、`POST /articles/{idOrSlug}/comments`(1897)、`GET /categories/tree`(1670)、`GET/POST/PUT/DELETE /tags`(1765)、`/categories/{id}`。
- **最值得肯定（架构纪律成熟）**：开发 AI 主动识别并以 openapi 为唯一真相源纠正**计划文档与冻结契约两处冲突**——① 计划 `M2-开发计划.md` §5 写 `POST/DELETE /admin/comments`，契约里 `/admin/comments` 仅 GET → 改走 `PATCH /comments/{id}/status` + `DELETE /comments/{id}`，写文件头 + 测试钉死防后人改 404；② 计划 §6 提标签「合并」，契约无 merge 端点 → 不实现，记「先改契约再改实现」。正确践行契约/代码冻结铁律（计划是意图、契约是唯一真相源、冲突回流）。
- **架构亮点**：① 权限守卫同源（`RequireCan` 引用 `lib/permission` 的 canModerateComments/canManageCategories/canManageTags 均 roleAtLeast editor，与菜单一致；editor 管内容、admin 管用户/站点配置边界守住）；② 契约硬约束前置 UI（CATEGORY_MAX_DEPTH=4 满级禁新建子、有子/有文章禁删除、articleCount>0 禁删标签，避免点了才吃 409）；③ 纯函数 `categoryTree.ts`（buildParentMap/collectSubtreeIds/nodeDepth/canAddChild/flattenTreeOptions）环防护 + `depth<=CATEGORY_MAX_DEPTH+1` 死循环兜底 + 8 测试守卫；④ 评论代回复不乐观插入（契约返回可能 rejected）、reviewing 唯一进出路径 PATCH、三态语义令牌、CommentListPage 经 grep 确认不传 sort（契约无 sort）。
- **零回归**：localStorage 仍仅 auth.ts(不落凭证)+ui.ts(侧栏态,非凭证)；本轮零新依赖（package.json 未动）；chunk 体积未退化；useComments 写后 invalidateQueries(['comments']) 前缀匹配失效子键正确。
- **评分 88 → 89/100（⭐⭐⭐⭐⭐）**。架构 45→47、工程化 42→44；技术栈48/性能40/契约50 维持。未给 90+：组件级 jsdom 测试/E2E/覆盖率门禁仍缺。
- **非阻塞关注点(R1-R4)**：① `CategoryNode` 契约无 parentId 字段（树靠 children 嵌套），前端 buildParentMap 反推双保险正确，根因在契约层，不阻塞；② 后端 `PUT /categories/{id}` 局部更新（node-backend-v1.0.1 增量）前端声称已修，请 owner 确认已纳入冻结/部署记录；③ 评论审核 reason 可选（与契约一致）；④ 组件级测试缺口（工程化爬坡项）。
- 结论：Phase2+Phase3 无阻塞，可推进 Phase4 用户管理。

### owner 三项裁决（2026-08-29 深夜，回复文档已同步改写）
1. **md-editor 563.94 kB 超 500 kB 告警线 → 接受，不改**。owner 原话：「作为文章系统，包含一个优质的编辑器是必要项」。定性为**主动取舍非缺陷**（能省的 297→41 语言已省，余下 CodeMirror+Prism+解析链全是功能本体；路由级懒加载、仅编辑页一次、gzip 180kB，首屏/列表页无影响）。
   - 可选未执行：若想消除构建告警噪音，可调 `build.chunkSizeWarningLimit` 至 700 **并附注释说明原因**。我未擅自改——不加说明地抬高阈值等于藏问题，与本轮原则相悖。等 owner 点头发话再补。
2. **高亮语言集 297→41 → 目视复核通过**（jsx/tsx/bash/sql 均正常）。
3. **方案 B Cookie 刷新分支 dev 无实测 → 上线前测试，当前暂忽略**。转为上线前 checklist（HTTPS 环境或 staging 切方案 A 时补验），不阻塞开发。
- 结论：**无遗留阻塞项**，回复文档 §0/§0.1/§2/§5/§7 已全部改写为「已裁决」口径（新增 §0.1 裁决一览表）。下一步 Phase 2 评论审核。

## M2 前端 Phase4+5+6 第四轮审阅（2026-08-29 深夜，eno 前端架构分析专家）
- 输入：节点文档 `M2-Phase4-NOTES.md` + `M2-Phase5-NOTES.md` + `M2-Phase6-NOTES.md` + 实际代码（基线 `f98ed5e` → `8eb7205`）。16 文件 +1352/−57；新增用户管理 / 仪表盘收尾 / 站点配置三模块 + 8 测试（总数 45→53）。
- 方法：不采信自陈——独立跑四门禁 + git 取证 + grep 冻结契约 openapi.v1.yaml v1.11.0 + 读源码。报告 `docs/manage-frontend/review/M2-前端代码第四轮审阅报告.md`。
- **门禁全绿**：typecheck 0（strict 生效）/ biome 99 文件 0 / vitest 53 passed（10 文件）/ build 通（DashboardPage 336.47 kB、md-editor 563.94 kB owner 已接受）。与自报吻合。
- **契约一致性（端点全对齐，grep 验证存在）**：`GET /users`(2849,minRole admin)、`GET /users/{id}`(2899)、`PATCH /users/{id}`、`POST /admin/users/{id}/reset-password`(3019)、`GET /stats`(3527)、`GET /categories/stats`(3505)、`GET /admin/articles?sort=-createdAt`(2365 引用 Sort)、`GET /admin/comments`(2133/2139 仅 status/articleId，**无 sort**，前端 localeCompare 兜底)、`GET/PATCH /admin/site/settings`(3124)。
- **计划与契约偏差，开发 AI 仍正确以契约为真相源并钉死（防后人照计划改 404）**：① `/admin/users/{id}` 实际在 `/users` 下（仅 reset-password 带 /admin）→ `users.test.ts` 反向断言 not.toContain('/admin/users')；② 重置密码契约要求 admin 主动填 newPassword(minLength 8)、响应不返凭证 → ResetPasswordDialog 是输入新密码非展示凭证；③ 评论接口无 sort → 前端兜底；④ F0.2 ImageUploadField 基座未落地 → 自建最小 LogoUploadField。
- **架构亮点**：① 🔒 自锁保护——`UserEditDialog` 编辑自身时禁用 disabled 选项（禁用自己=锁死后台，真实 footgun 显式拦掉），本轮最有价值安全判断；② 契约守卫测试继续加厚（users 4 + site 4，钉死路径 + {list,pagination} 无 items/total + /stats 与 /categories/stats 不串味 + admin 端点带 /admin/ 段）；③ 语义令牌纪律延伸到 role-*/user-status-*（index.css 双主题 + @theme inline 映射）；④ useUsers invalidate(['users']) 前缀匹配失效键正确；⑤ 零新依赖（recharts ^3.10.1 计划已规划）、零 localStorage 回归、chunk 未膨胀。
- **评分 89 → 90/100（⭐⭐⭐⭐⭐）**：自锁护栏为真实安全增益。未给更高：组件/E2E 测试仍缺（工程化爬坡项，与第三轮 R4-工一致，非本轮 regression）。
- **非阻塞关注点(R4 续 / F0.2 / 站点字段子集 / recharts 拆包可选)**：① R4（续）——公开 `GET /site/settings` 旧后端可能 5000，前端本端只碰 admin 端点未触碰公开版，但前台页头/页脚品牌区依赖公开版 → 上线前确认后端已修复（本端无需改）；② F0.2 LogoUploadField 是站点专用最小实现，等文章封面/头像也要上传时再泛化为通用 ImageUploadField(accept/articleId) 并补通用测试，避免过早抽象；③ 站点设置仅暴露 6 高频字段（icp/contactEmail 等待运营需要再扩）；④ recharts 随 DashboardPage 路由 lazy 已隔离，dynamic import 非必要。
- 结论：Phase4+5+6 无阻塞，可推进 Phase7 个人中心 / Phase8 跨切面。唯一跨端遗留 R4（公开 site/settings 5000）转上线前 checklist。

## M2 Phase 2 评论审核模块（2026-08-29 深夜，FrontendDeveloper expert）
- **发现计划与契约偏差（重要，未停下请示、按契约实现）**：`M2-开发计划.md` §5 写的 `POST /admin/comments`（代回复）与 `DELETE /admin/comments/{id}` **契约里不存在**——`/api/v1/admin/comments` 只有 GET。真实端点：
  - 列表 `GET /admin/comments`（editor+，query: page/pageSize/status/articleId，**无 sort 参数**）
  - 审核 `PATCH /comments/{id}/status`（editor+，body {status, reason?}；**reviewing 的唯一进出路径**；置 approved 时后端清空 rejectedReason）
  - 代回复 `POST /articles/{idOrSlug}/comments`（member+，传 parentId 即回复某楼；返回值**可能 rejected**——契约要求就地提示且**不要插入列表**）
  - 删除 `DELETE /comments/{id}`（editor + ownerOverride；`x-cascade: children` **级联删子回复**）
  - 处理：契约唯一真相源，按契约实现；偏差写进 `api/comments.ts` 文件头 + 用测试钉死路径（防后人照计划文档"修正"成 404）。
- 新增：`src/api/comments.ts`、`src/hooks/useComments.ts`（审核/回复/删除三 mutation，写后失效 `['comments']`）、`src/pages/comments/{CommentListPage,CommentReviewDialog,CommentReplyDialog}.tsx`、`src/api/comments.test.ts`（5 用例）。
- 改：`lib/queryClient.ts` 加 `qk.comments`；`router/index.tsx` 把 /comments 占位换真实页；`index.css` 加评论三态语义令牌 `status-approved/rejected/reviewing`（明暗各一套，沿用 P3-3 纪律）。
- 三个设计取舍：① 契约不支持 sort → 列表**刻意不给 DataTable 传 sort**（传了也是被忽略的无效参数）；② `Comment` 无 articleTitle 字段 → 「所属文章」列显示 `#articleId` 并跳文章编辑页；③ 代回复不做乐观插入（可能返回 rejected）。
- 门禁四门全绿：typecheck 0 / biome 79 文件 0 issue / **test 33 passed**（新增 5）/ build 通（CommentListPage 独立 chunk 8.64 kB）。
- 未 git commit。下一步：Phase 3 分类（树）+ 标签（#13）。

## M2 Phase 3 分类树 + 标签模块（2026-08-29 深夜，FrontendDeveloper expert）
- **发现计划与契约偏差 #2**：计划 §6 提标签「新建/**合并**/删除」，但契约**没有 merge 端点**（全仓 grep merge/合并 无结果）→ 合并不做，已在 `api/tags.ts` 文件头 + 页面注释写明「须走先改契约再改实现」。
- **契约三条硬约束已前置到 UI**（不是点了才吃 409）：
  - `Category.x-max-depth: 4` → 深度达 4 的节点，「新建子分类」按钮禁用。
  - `DELETE /categories/{id}` 须**无子分类且无文章**、**不级联** → 有子节点的行删除按钮禁用并提示先迁移。
  - `DELETE /tags/{id}` 须无文章引用 → `articleCount > 0` 的标签删除按钮禁用。
- **关键坑（契约缺口，前端自行兜底）**：`CategoryNode` schema **没有 parentId 字段**（只有 id/name/slug/description/sortOrder/children），父子关系靠 children 嵌套表达；而 `PUT /categories/{id}` 是**全量替换**语义——编辑子分类时若漏传 parentId，很可能被后端置空、静默挪到根。解法：抽纯函数 `buildParentMap(tree)` 从树反推 id→parentId，编辑时回填。已在 `categoryTree.ts` 头注释写明。
  - 成环防护：`collectSubtreeIds(node)` 收集自身+子孙，编辑时从父级候选里排除（后端 PUT 会校验环返 409/3002，但不该让用户先提交再吃瘪）。
- 新增文件：`src/api/tags.ts`、`src/hooks/useTags.ts`、`src/pages/categories/{CategoryTreePage,CategoryNodeRow,CategoryFormDialog,categoryTree}.tsx|ts`、`src/pages/tags/{TagListPage,TagFormDialog}.tsx`、测试 `categoryTree.test.ts`(8) + `tags.test.ts`(4)。扩展 `api/categories.ts`（加 CRUD + 导出 `CATEGORY_MAX_DEPTH`）、`hooks/useCategories.ts`（三 mutation）。
- 改：`qk` 加 `tags`；router 把 /categories、/tags 占位换真实页。
- 门禁四门全绿：typecheck 0 / biome 89 文件 0 issue / **test 45 passed**（新增 12）/ build 通（CategoryTreePage 6.64 kB、TagListPage 4.37 kB，均独立懒加载 chunk）。
- 未 git commit。下一步：Phase 4 用户管理（#16）→ Phase 5 仪表盘（#18）→ Phase 6 站点配置（#17）→ Phase 7 个人中心（#20）→ Phase 8 跨切面（#19）。

## M2 准备简报纠错（2026-08-29 末，FrontendDeveloper expert）
- 用户指出 `docs/prd/M2-前端开发准备简报.md`（我自写、动工前）含过时/错误声明会误导后续。据 `package.json`/`vite.config.ts`/线上实测/owner 裁决逐项订正：① 技术栈 Vite6→Vite8（rolldown）；② 补 Markdown 编辑器选型 `@uiw/react-md-editor` v4（勿回评 TipTap）；③ 刷新令牌 Cookie 假设→实为方案 B 内存 refreshToken+body；④ 门禁三件套→四件套（加 vitest）；⑤ pnpm 11→pnpm@9.4.0；⑥ CORS「硬阻塞待拍板」→已选 B（`vite.config.ts` 双代理已落地）。新增 §8 纠错记录表 + 顶部状态标注 + 各处「🔧 已纠正」标记。未 git commit（用户自管）。
- 原则重申：纠错只动文档、不动代码（冻结铁律）；契约核心约束（list 非 items、/files 根路径、code:0、数字错误码、accessToken 不 localStorage）仍有效。

## M2 阶段交付 NOTES + DEV-LOG 机制落地（2026-08-29 末，FrontendDeveloper expert）
- 用户要求：每开发一个阶段把结果落实到一个文档（仿后端 `docs/node-backend/B0-NOTES.md` 五段式）+ 一份开发流水账日志（踩坑/思考，供复盘学习）。
- 已建 `docs/manage-frontend/` 下：① `DEV-LOG.md`（时序流水账，基座→Phase3，含 CORS/编辑器/refractor/评论端点偏差/分类 parentId 坑等）；② `M2-基座-NOTES.md`（M2-01~09 脚手架）；③ `M2-Phase0-NOTES.md`（基础件+第一轮审阅整改）；④ `M2-Phase1-NOTES.md`（文章）；⑤ `M2-Phase2-NOTES.md`（评论，含计划端点偏差）；⑥ `M2-Phase3-NOTES.md`（分类树+标签，含 parentId 坑）。均仿 B0-NOTES：交付物表/选型理由/关键设计/运行/待留意。
- 机制约定：后续每阶段收口即补一篇 `M2-PhaseN-NOTES.md` + 往 `DEV-LOG.md` 追加条目；文档归 `docs/manage-frontend/`。未 git commit（用户自管），建议按阶段单独提交。

## 前端 AI 校验清单交付（2026-08-29 末，SeniorDeveloper）
- 用户要一份可转交前端 AI 的校验文档，照后端真实行为做契约一致性自检。已落 `docs/prd/M2-后端契约校验清单-前端自检.md`。
- 内容结构：§0 基础约定（线上基址/信封/accessToken/⚠️ R1 修复待重部署）；R1 分类 PUT 省略 parentId 不再静默挪根（契约事实+后端修复 `category.ts:312,334`+curl 验证步骤+前端自查：编辑表单仍应显式回传 parentId、`CategoryNode.children` 递归渲染）；R2 列表分页 `list` 非 `items`（指 `M2-分页字段名纠正报告.md`）；R3 附件 `/files` 根路径（指部署指南 FAQ）；R4-R5 登录/站点配置/CORS 快速过 + 交付确认输出格式。
- 兼容性：与 M2 Phase 3 前端已有的 `buildParentMap(tree)` 兜底回填（DEV-LOG:222）一致——后端修后是局部更新语义，前端显式回传更稳；文档提醒前端 AI 更新「PUT 是全量替换」的旧认知。

## git tag 里程碑（2026-08-29 末，owner 已 commit&push 后由 AI 打 tag）
- owner 已 `git commit & push`（HEAD `148421c` "fix(category): 修复分类更新时省略字段静默置空的bug"，已同步 origin/master）。
- 按「冻结后增量维护 bump patch tag」约定，打注解 tag **`node-backend-v1.0.1`**（父基线 `node-backend-v1.0`）并 `git push origin node-backend-v1.0.1`。本地 `git tag -l` + 远端 `git ls-remote --tags` 双验证均命中（commit `1180907a`）。
- tag 覆盖：R2 驱动+策略 A 直出+wrangler.toml 骨架 / seed-users.ts + seed-site-settings.sql / 部署指南+验收报告 / 分类 PUT 局部更新修复 / 注释同步+分页纠正报告+前端校验清单。六门门禁全绿。

## M2 前端 R1/R2/R3 自检（收尾，2026-08-29 深夜）
- 核对 `docs/prd/M2-后端契约校验清单-前端自检.md`：R2(list 非 items)/R3(/files 根路径) 前端已正确，grep 实测 0 误用；R1(分类 PUT 局部更新) 前端编辑表单本就显式回传 parentId，无需改功能。
- 纠错：CategoryFormDialog.tsx + categories.ts 注释把「PUT 全量替换会静默挪根」改为「后端已修局部更新+前端双保险」；M2-Phase3-NOTES.md §三/§五 同步纠正；DEV-LOG 追加本条目。
- 门禁复验全绿（test 45）。提醒：R1 修复需后端已 deploy node-backend-v1.0.1 才线上生效。

## M2 Phase 4 用户管理（2026-08-29 深夜，通过审阅后续做）
- 交付：src/api/users.ts + src/hooks/useUsers.ts + pages/users/{UserListPage,UserEditDialog,ResetPasswordDialog}.tsx + users.test.ts(4) + queryClient 加 users.list + index.css 加 role-*/user-status-* 令牌 + router 接 UserListPage。门禁全绿（test 49）。
- 偏差：计划 §7 写 `/admin/users`，契约实为 `/users`（仅 reset-password 在 `/admin/users/{id}/reset-password`）；计划「一次性凭证」实为 admin 输入 newPassword。偏差钉进 users.test.ts。
- 坑：z.coerce.number / 字段级 .refine 都会破坏 zodResolver 泛型 → 用 z.string() 提交转 number + .regex 校验（同 CategoryFormDialog）。自锁保护：编辑自身禁用 disabled。

## M2 Phase 5 仪表盘（2026-08-29 深夜）
- 交付：StatsChart.tsx(recharts 环形图) + DashboardPage.tsx 改写(分类图替换 CSS 条形 + 近期文章/评论 Top N + 待审纳网格) + site.test.ts(2)。门禁全绿（test 51）。
- 认知：仪表盘基座(M2-09)已提前接真实 /stats、/categories/stats，Phase 5 为增强非替换。recharts 已装 R3 风险解除。
- 偏差：计划 §5 写评论 `?sort=-createdAt`，契约 admin/comments 无 sort → 取默认前 5 + 前端 createdAt 兜底。近期文章用 sort=-createdAt（admin/articles 支持）。

## M2 Phase 6 站点配置（2026-08-29 深夜）
- 交付：SiteSettingsPage.tsx + LogoUploadField.tsx(替代未实现 F0.2) + 路由接入真实页 + site.test.ts(+2 admin 守卫)。门禁全绿（test 53）。
- 坑：① canXxx 是函数式(actor)=>bool，enabled 须 useAuthStore 取 actor 再调 canManageSiteSettings(user) 得布尔；② F0.2 ImageUploadField 基座未落地→自建 LogoUploadField；③ 契约测试 not.toContain('/site/settings') 误判（/admin/site/settings 含该子串）。
- 偏差：公开 GET /site/settings 返回 5000 属后端 R4 风险，本阶段只碰 admin 端点不碰公开版；上线前需确认后端已修。

## M2 Phase 7 个人中心（2026-08-29 深夜，FrontendDeveloper expert — 第四轮审阅后下一轮）
- 输入：第四轮审阅 `docs/manage-frontend/review/M2-前端代码第四轮审阅报告.md`（综合 90/100、无阻塞、明确「可推进 Phase7 个人中心 / Phase8 跨切面」）；按 `docs/manage-frontend/M2-开发计划.md` §10 落地。
- **落实 F0.2 泛化（审阅建议）**：「头像」是第二个图片上传场景，触发 `LogoUploadField`→**`ImageUploadField`** 泛化：新增 `accept='image/*'` + `shape='square'|'circle'`；`SiteSettingsPage` 改用 `ImageUploadField`（label/description 不变）；旧 `LogoUploadField.tsx` 已 `rm`。站点设置与个人中心共用，避免过早抽象后又返工。
- 端点严格对齐冻结契约 v1.11.0：`src/api/me.ts`(getMyProfile/PATCH /me/profile / POST /me/change-password / GET /me/likes 裸数组 / GET /me/favorites 分页 / add|removeFavorite) + `src/api/notify.ts`(GET /me/notifications / unread-count / POST /me/notifications/read-all / PATCH /me/notifications/{id})。
- hooks：`useMe.ts`(profile/update/changePassword/likes/favorites/toggleFavorite，写后失效 `qk.me.favorites({})`，统一 toast 避免页面层双 toast) + `useNotifications.ts`(unreadCount 含 `refetchInterval:60_000` 轻轮询替代 WebSocket，契约无推送通道)。
- 页面群 `src/pages/profile/`：`ProfileLayout`(左导航 NavLink + Outlet) / `ProfilePage`(头像 circle + nickname/email zod，提交只传 nickname/email/avatar，无本地 toast) / `ChangePasswordPage`(old+new min8+confirm.refine) / `NotificationsPage`(列表+全部已读+单条已读跳转,未读行 bg-primary/5+红点) / `LikesPage`(裸数组 ArticleSummary[],createdAt 空判断) / `FavoritesPage`(分页+取消收藏幂等)。
- `Topbar` 加 `NotificationBell`(count>0 红点,跳 /profile/notifications)；`router` 把 /profile + /profile/password 两个占位替换为嵌套路由(5 子页)；`queryClient` 加 me/notifications 键。
- 关键坑：① **R5 契约矛盾坐实并钉死**——`GET /me/likes` 契约返回**裸数组** `ArticleSummary[]`（非 `{list,pagination}`），计划/路线图写 page 是错的；`me.test.ts` 反向断言 `(data as any).list/.pagination` 为 undefined，防后人照计划「修正」成 data.list 静默空白。已登记后端「契约维护批次」。② 用户名只读展示初版误用 TextAreaField 与 RHF nickname 冲突→改 Input + htmlFor 静态展示（满足 noLabelWithoutControl lint）。③ 双 toast→删除页面层 useToast 仅留 hook。④ `createdAt` optional→空判断 format。
- 门禁四门全绿（实测复验）：typecheck 0(strict) / biome 111 文件 0 issue / **test 62 passed**(12 文件,较 Phase6 的 53 +9：me 5 + notify 4) / build 通(DashboardPage 336.44kB、md-editor 563.94kB owner 已裁决)。
- 文档：`docs/manage-frontend/M2-Phase7-NOTES.md`(五段式：交付物/选型理由/F0.2 泛化/R5 矛盾坑/运行+待留意) + `DEV-LOG.md` 追加 Phase 7 段。
- 待留意（非阻塞）：① R4 续——公开 `GET /site/settings` 5000 仍属后端上线前确认项（本端未碰公开版）；② 组件级/E2E 测试仍缺（工程化爬坡项，与第三/四轮 R4 一致）；③ Phase 8 跨切面(构建/部署/复盘) 为计划最后一轮。未 git commit（用户自管）。

## M2 Phase 8 跨切面（2026-08-29 深夜，FrontendDeveloper expert — 计划最后一轮）
- 输入：第四轮审阅「可推进 Phase 7 个人中心 / Phase 8 跨切面」；按 `M2-开发计划.md` §11 落地 M2-13/14/15。
- **M2-13 构建优化**：路由懒加载 + `manualChunks` 隔离编辑器早已就位（Phase1 整改 P3-2）。本阶段补真实体积证据：装 `rollup-plugin-visualizer@7.1.1`(dev)，挂 `vite.config.ts` 但**仅 `ANALYZE=1` 生效**（gated，平时/CI 零开销）；`pnpm analyze` 生成 `dist/stats.html` treemap。`package.json` 加 `analyze` 脚本。实测产物(gzip)：index 127.77kB(首屏基线)/md-editor 180.24kB(唯一>500kB,owner 已接受)/DashboardPage 99.05kB(recharts,仅/dashboard 按需)/业务页 2~3.4kB。首屏≈127kB gzip+小页面 chunk，体积策略到位。未擅自抬高 chunkSizeWarningLimit（真实信号不藏）。
- **M2-14 部署指南**：`docs/manage-frontend/M2-14-部署指南.md`——CF Pages + Nginx 双方案，SPA 回退(`_routes.json`/`try_files`)+`/api/v1`与`/files`反代(dev 代理≠生产)+缓存策略+验证清单 6 条+FAQ(`/files` 根路径坑)。
- **M2-15 复盘**：`docs/manage-frontend/M2-15-复盘文档.md`——四门门禁+62 测试+依赖增量；4 处计划↔契约偏差全部以契约为真相源回流+测试钉死；选型对错逐条证据；七端契约一致性；组件/E2E 测试缺口列工程化爬坡项(非阻塞)。
- 坑：归档 treemap 到 `docs/.../build-report/` 被 biome 扫出 1 warning(扫 html 生成物)→改只留 `dist/stats.html`(gitignore)+`biome.json` 加 `!docs/manage-frontend/build-report`。lint 复绿 111 文件 0 issue。
- 交付 `docs/manage-frontend/M2-Phase8-NOTES.md`(五段式)。门禁四门全绿(实测)：typecheck 0 / lint 111 文件 0 / test 62 passed / build 通。**M2 前端全系列收官**（骨架+Phase0~8）。唯一跨端遗留 R4(公开 site/settings 5000) 转后端上线前确认。未 git commit（用户自管）。
