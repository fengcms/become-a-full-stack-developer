# 2026-08-29 工作日志

## M0-06 配图提示词重写（tag 时机变更导致）
- 用户准备发 M0-06，发现之前按提示词生成的图不对——根因：tag 约定已改为里程碑式（2026-08-27），但 `docs/M0-配图中文提示词包.md` 的 `06-发布流程` 仍写「写作 → 打 git tag → 发布到 CSDN → 回填」，把"打 tag"画进发布闭环，与"M0 不打 tag、tag 在代码定稿时打"矛盾。
- 修 `06-发布流程` 提示词：闭环改为「写作 → 发布到 CSDN → 回填」三节点；加 `>` 注释强调本图不含打 tag 环节、切忌出现「打 tag / article/Mx-yy」字样。
- 顺手修 M0-06 文章第 91 行漏改的旧闭环描述「写作 → 打 tag → 发 CSDN → 回填」→「写作 → 发 CSDN → 回填」（上一轮修 tag 矛盾时漏了这处）。
- 说明：`06-monorepo结构图`（"一个 tag 锁全局"）、`06-tag流程`（用 `node-backend-v1.0` 里程碑 tag）两图提示词**已对齐里程碑式，无需重做**；仅 `06-发布流程` 必须重出图。
- 经验：配图提示词包与文章同受"tag 约定"约束，约定变更时需同步扫提示词包，避免图文再次打架。

## M0 八篇内链全量穿插完成（收官动作）
- 用户确认 M0-01~08 全部发完、blog AI 已同步本地。触发此前裁定的"全发完一次性回填 `{{LINK}}`"。
- 用受管 Python 脚本批量替换（每处断言恰好 1 次命中）：共 **30 处成功、0 失败**。
  - 19 处 `{{LINK:M0-xx}}` 占位注入真实 URL（分布：M0-01×4 / M0-02×7 / M0-03×1 / M0-04×1 / M0-05×1 / M0-06×1 / M0-07×5）。
  - 顺手修用户发布时手动穿插留下的 7 处瑕疵：5 处乱码链接文字 `[《xxx]`（M0-04 L151、M0-05 L5/L112/L133、M0-08 L124）→ 去 `《`；2 处"相关资源"列表整句重复（M0-03 L119、M0-08 L158）→ 去重。
  - `ARTICLES.md` M0-04~08 五行由 🟡 草稿 置 🟢 已发布 + 回填 CSDN 链接（164139553/164140515/164166250/164167103/164167281）。
- 验证：全仓 `{{LINK:M0}}` 残留=0、`[《` 乱码链接=0；八篇真实内链数 4/9/4/3/5/3/7/6。
- ⚠️ **CSDN 线上与本地不一致风险**：上述 7 处瑕疵是用户发布时带入线上版的，本地已修干净，但**已发布的 CSDN 文章仍含这些瑕疵**（除非用户去 CSDN 编辑修正或重发）。需提醒用户：本地 articles/ 是干净源，CSDN 线上版若需一致需手动改 7 处。
- M0 产品篇正式收官：8 篇全发 + 内链全穿插 + 镜像表全 🟢。下一步进入 M1 后端文章写作（**M1-01~31 共 31 篇**，以 node-backend-v1.0 冻结代码为素材）。

## M1 写作规划裁定（owner 确认）
- owner 裁定：按路线图原 31 篇计划写 M1（M1-01~31），不另起炉灶；开发中踩的坑/感悟穿插进对应正文，插不进但有价值的，31 篇写完后以增补文补（清晰编号，不破坏路线图绑定）。
- 统筹 AI（我）负责写；CSDN 线上瑕疵由 owner 自行重编，我只保证本地 articles/ 干净正确。
- PM 加固要点：① 写前先出「31 篇逐篇写作纲要 + 踩坑点池」（从 docs/node-backend/B0~B7-NOTES、DEV-LOG、review 报告挖真实素材），把"插哪"从随机变 deliberate；② 踩坑点必须锚定冻结代码 node-backend-v1.0 真实行为，不引用冻结后不存在的实现；③ 每篇只服务一个核心命题（呼应 R21 防重复）；④ 增补文用 M1-番外 或 M1-32+ 编号。
- 延续 M0 全套规范：标题前缀 `成为全栈·Node 后端篇·`、占位 token、CTA、相关资源、≥2200 字。
- 发布流沿用：我写→owner 发 CSDN→blog AI 同步→按 SOP 回填（M1 起可交独立发布维护 Agent）。

## M1-01 写作完成（31 篇开篇）
- 文件：`articles/M1-01-后端工程从零搭建-TypeScript目录与热更新.md`。标题前缀 `成为全栈·Node 后端篇·`，CJK 正文 2313 字（≥2200 达标）。
- 命题：工程初始化 / 目录约定（src 八目录 config/db/lib/middleware/routes/services/shared/types）/ 热更新（tsx watch）/ 门禁雏形（tsc+biome+vitest）。
- 穿插真实踩坑（来自 DEV-LOG，锚定 node-backend-v1.0）：P-01（`@/` 别名 TS7 删 baseUrl 否则 TS5102）、P-02（类型纪律 verbatimModuleSyntax+noUncheckedIndexedAccess+zero any）、P-05（`@/`+`./`同目录是重命名保险丝）。
- 占位 token：回链 `{{LINK:M0-01}}`/`{{LINK:M0-03}}`、预留 `{{LINK:M1-02/03/20/21}}`、配图 `{{IMG:01-目录结构图}}`/`{{IMG:CTA-订阅专栏}}`。CTA+相关资源齐备。
- 素材地基已核：tsconfig.json（paths/verbatimModuleSyntax/noUncheckedIndexedAccess）、package.json scripts（dev=tsx watch、build=tsc）、src 真实八目录树、src/index.ts 双部署入口注释。
- 后续：M1-02 框架选型（Hono）；按编号顺序续写，每篇锚定冻结代码 + 穿插踩坑点池。

## M1-01 目录结构修正（owner 抓出真实 bug）
- owner 指出：M1-01 的目录树写了 `lib/` 并描述其作用，但 `src/lib` 在结构调优时已被拆进 `services/`(业务逻辑) 与 `shared/`(基础设施)，**现已彻底移除、磁盘上不存在**。
- 已核真实 `src/`：`config(1)/db(3)/middleware(4)/routes(21)/services(19)/shared(10)/types(2)`，**7 目录无 lib**。`lib` 内容归位：信封构造+错误→`shared/response.ts`+`shared/errors.ts`(+codes/db-error)；文章/评论逻辑→`services/*`。`guard`→`src/middleware/auth.ts`；校验 schema 不再是单文件，改成各路由内联 zod（如 `src/routes/auth.ts` 的 `registerSchema`）。
- M1-01 改正：目录树改为 7 目录（含 db 补 Drizzle schema、shared 补"响应信封/错误与错误码"）；"八个目录"4 处表述→"七个"；P-05 保留"把 lib/ 拆进 services/ 和 shared/"作为真实重构历史叙述（恰解释 lib 为何消失）。
- **系统性修复（防复发）**：`docs/M1-文章创作纲要.md` 14 处陈旧 `src/lib/*` 引用全部重映射为真实路径（response→shared/response、error→shared/errors、article→services/article、guard→middleware/auth、schemas→各路由内联 zod 等）；铁律④强化"目录结构严格以 node-backend-v1.0 真实 src/（无 lib）为准"。
- **铁律升级（owner 裁定）**：M1 每篇文章写作前，必须核对 `node-backend-v1.0` 真实代码（尤其 src/ 目录树、文件是否存在），不得依赖纲要/记忆里的旧结构。"文章以真实代码为基础"是硬约束。
- 附带提醒：M1-01 的 `{{IMG:01-目录结构图}}` 生成时须画 7 目录（无 lib），与修正后文字一致。

## M1 文末/内链约定修正（owner 本轮指令，已固化进 M1 创作纲要）
- owner 指出三点，M1-01 已按此改好：
  1. **订阅专栏图用可复用真实 URL 直填**：专栏宣传图在 M0 产品篇成品里已存在，直接复用 `![订阅专栏](https://i-blog.csdnimg.cn/direct/64327c7510ad45dcb8b997df3a151525.png)`，不留 `{{IMG:CTA-订阅专栏}}` 占位。
  2. **专栏/仓库链接用 `[url](url)` 标准 markdown**：CSDN 不渲染裸 URL。文末统一为「## 订阅这个专栏」标准块（对齐 M0-01 成品：订阅图 + 两条 `[url](url)` 链接），**不单列「相关资源」块**。
  3. **已发布的 M0 链接直接填真实 URL**：M0 全 8 篇已发，M1 回链 M0 不保留 `{{LINK:M0-xx}}` 占位（如 M1-01 前置知识已注入 M0-01 `164119914`、M0-03 `164121738`）。
- 未发布的 M1 篇内链仍保留 `{{LINK:M1-xx}}` 占位（发布后回填）。
- 上述规则已写入 `docs/M1-文章创作纲要.md` 铁律③、§三 回链 M0、§四 写作纪律清单；后续 30 篇 M1 文章一律照此执行。
- M1-01 当前尾态：前置知识含 M0-01/M0-03 真实链接；文末「## 订阅这个专栏」含真实订阅图 + `[url](url)` 两链接；正文 M1-02/03/20/21 占位保留。
