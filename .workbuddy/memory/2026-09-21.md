# 2026-09-21 发布维护

## 本轮：M2-10/11 发布 → 镜像 + 回填；M2-12/13 目标

### SOP-A · URL 镜像（ARTICLES.md）
| 文章 | CSDN 链接 | 状态 |
|---|---|---|
| M2-10 Markdown 编辑器：预览、暗色主题与连续图片粘贴 | 166107729 | 🟢 |
| M2-11 文章管理工作流：保存修改、投稿、发布与下架 | 166110378 | 🟢 |

M2-01~11 全部已发布。ARTICLES.md 第 70/71 行 🟢，72/73 行保持 🟡。

### 注入 · 6 处
- M2-10 被引用 3 处：`M2-19` L87、`M2-15` L136、`M2-09` L244
- M2-11 被引用 3 处：`M2-22` L76、`M2-09` L245、`M2-12` L162

### M2-12/13 状态
- `M2-12`：唯一占位 `{{LINK:M2-11}}` 已注入 → **就绪**
- `M2-13`：唯一占位 `{{LINK:M2-15}}`（未发布）→ 保留，无注入项

### CSDN 副作用
被编辑文件中**只有 `M2-09` 是已发布文章** → 需 blog AI 重新发布才生效。
其余（M2-12/15/19/22）均草稿，无副作用。

### 🔴 累积待重发清单（优先级高，均为坏链修复）
`M1-27`、`M1-28`、`M1-29`、`M1-30`、`M1-31` —— 09-19 修复的 9 处坏链，未重发前线上仍是坏链。

### 顺手体检
对 M2-10/11 做了链接 ID 体检：引用 3 个 ID（164453365 / 164453121 / 165986069）全部与索引一致，**无坏链**。

## 操作要点（延续）
- 同文件多占位必须**串行** Edit；不同文件可并行。
- 按目标编号**逐个精确 grep** 查回填目标（不通读全量清单），核验覆盖全部已发布编号。
- M2 系列占位形态为 `[标题]({{LINK:Mx-yy}})`，**只填 URL**，不套完整 markdown 链接。
- 查具体文章前先 Glob 拿真实文件名，不猜。

---

## 第二轮：owner 定下两条新规则 + 首次执行全量链接审计

### owner 指令
1. **「CSDN 链接 ID 全量比对索引」定为每轮标准动作**。
2. **中途不再重发**：owner 会在全系列发布完成后统一打磨并全量更新；期间不更新已发布文章。故不再逐轮提示"必须重发"，只累积清单。

### 首次全量审计（本轮执行）
方法：抓取全仓 `articles/*.md` 里所有 `article/details/<id>`，与 `materials/csdn-已发布链接.md` 比对。Grep 工具的 content 模式会把超长行显示为 `[Omitted long matching line]`——本轮有 **19 处**这样的行，逐一 Read 补出 ID，否则审计不完整。

**结果：修掉 2 处「错链」（新缺陷类型）**

| 文件 | 行 | 文案 | 错误 URL | 正确 URL |
|---|---|---|---|---|
| M1-19 全文搜索 | L56 | 统一响应结构：HTTP 状态码与业务码如何分工 | `164363025`（=结构化日志） | `164289071` |
| M1-21 后端测试策略 | L43 | 同上 | `164363025` | `164289071` |

**新认识**：审计要查两类，不止一类。
- **幽灵链**：ID 不在索引里（09-19 修过 4 个 ID / 9 处）
- **错链**：ID 在索引里，但**文案与目标文章不符**（本轮 2 处）。原审计只查前者，会漏掉这类。

### 其他观察（未改，仅记录）
- `M0-04` L5、`M0-06` L5 的链接主机写作 `blog.csdn.net/FungLeo/...`（大写 F）；`M0-07` L26 写作 `fungleo.blog.csdn.net/...`。可访问但形式不统一，属正文措辞，未动。
- 全仓其余链接的「文案↔目标」本轮逐条核对一致（M0/M1/M2 全覆盖）。

### 已写入长期记忆
`MEMORY.md` 新增「发布维护 SOP」小节（三轮动作 + 中途不重发 + 占位形态 + 检索纪律），并补「发布进度」与「待最终全量更新清单」（25 篇）。

