# 2026-09-23

## 发布维护 Agent · M2-14/15 周期

**输入**：owner 告知 m2-14/15 已由 blog AI 同步，要求镜像 + 回填，并更新 m2-16/17 内链。

**开场异常（已查明）**：读 `materials/csdn-已发布链接.md` 时索引仍停在 09-21（最新 ID `166234769`=M2-13），`old-blogs/` 亦无 14/15 备份，git 最新提交仍为「完成2026-09-21的CSDN博客同步」。经只读抓取 CSDN 作者列表页确认 **M2-14/15 实际已于 2026-09-22 发布**（`166353905` / `166354291`）——即「已发布、blog AI 同步仍在途」。11:27 索引与备份落地（blog AI 完成 run+links），遂按标准 SOP 执行。

**执行**：
1. **SOP-A 镜像**：`ARTICLES.md` 第 74/75 行 M2-14/15 由 🟡 草稿中 → 🟢 已发布 + URL（一次 Edit 替换相邻两行，避免同文件写竞争）。
2. **SOP-B 回填**：全库 `{{LINK:M2-15}}` 共 3 处 → 填真实 URL：`M2-13` L141、`M2-10` L248、`M2-22` L77。`{{LINK:M2-14}}` 全库 0 处（无需回填）。
3. **m2-16/17 内链核查**：`M2-16` 零占位（延伸阅读 3 条 M2-05/07/12 均已解析）；`M2-17` 仅剩 `{{LINK:M2-18}}`（M2-18 未发布，按约定保留）。→ **本轮无注入项**，与既有写作风格一致。
4. **SOP-C 全量审计**：正文唯一 ID 53 / 索引 451，**幽灵链 0、错链 0**。
5. 核验：`{{LINK:M2-1[45]}}` 零残留；ARTICLES.md 74/75 行 🟢。

**方法沉淀（写入 MEMORY.md）**：
- 「索引滞后 ≠ 阻断」：`docs/链接与发布协作约定.md` §四 已授权自跑 `csdn_backup.py run --refresh-ids` + `links` 兜底；脚本依赖 `requests`+`markdownify`，**只有托管 venv 有**（系统 python3 无）。
- **错链必须用严格口径**：正文链接文案多为改写式（例：「阅读量：时间桶去重与原子计数」→ 目标《阅读量防刷：去重、冷却与计数写分离》），字面比对会误报 12 条；改为「与目标标题相似度 vs 与其余 450 条标题最高分」比较（差值 >0.28 且 >0.55 才算错链）→ 实测 0 条。

**累积待全量更新清单新增**：`M2-10`、`M2-13`（本地源回填了 `{{LINK:M2-15}}`）。

**遗留**：`M2-07` 文案「认证与授权：JWT、刷新令牌与权限模型」多写「与权限模型」，目标（M1-12 认证方案）正确，存疑未改，待 owner 定夺。
