# 2026-08-26 工作日志

## M1 Node 后端交付 · 总把控独立复验与验收冻结

- 开发 AI 交付 `docs/node-backend/M1-后端交付文档.md`（B0→B7 八批完成，声称 53 路径/67 操作、126 测试、双门 33 OK）。
- **总把控未采信自陈，独立复跑全部门禁**（托管 Node 22.22.2 + venv default）：
  - `tsc --noEmit` ✅ 0 error；`biome check .` ✅ 96 文件 0 问题；`vitest run`（串行+超时 30s）✅ **126 passed**（17 文件）
  - 结构门 `openapi-spec-validator` ✅ OK；语义门 `check_contract.py` ✅ 33 OK；`git status` ✅ 契约字节级未改
- **抽样穿透高价值裁定点（零假修复确认）**：
  - `refresh_tokens` 有状态表真在 `src/db/schema.ts:45`（落实 02-总把控裁决 Q3-A 推翻计划无状态）
  - 扁平信封真在 `src/lib/response.ts:55`（落实 Q1，错误复用同一 envelope、无 error 包裹）
  - 专用 401 保真（1001/1003）由 vitest 断言覆盖
- 产出 `docs/node-backend/03-统筹AI复审批复.md`：验收通过、即日冻结，并指派 §六 已知留口归属。
- **两项需统筹后续跟进**（非阻塞）：
  - §六#4 `GET /me/likes` 契约矛盾（裸数组 vs 声明 page/pageSize）= 冻结契约自身内部矛盾，非代码错 → 排「契约维护批次」统一整改，与既有 TODO「F2 计数复核」并轨
  - §六#7 nullable 唯一索引「假唯一」（email/slug 对 NULL 不冲突）→ 记入 Go/Python 跨端协调清单，勿误判为缺陷

## 下一步
- 统筹 AI 进入「写 M1 后端文章」（M1-01~M1-30），以验证冻结代码为唯一素材；`B0~B7-NOTES.md` + `DEV-LOG.md` 是直接素材。
- 本「契约→主计划→批次任务包」工作流将在 M2/M3/M4/M6/M7 五端原样复用（可沉淀 skill）。

## 关键判断
- 本轮协作模型验证成功：用户(owner)指定开发 AI 写码 + 我(总把控)写文档/验收。开发 AI 提的 8 问题中 Q1/Q2 是计划文档自身错（已就地修）、Q3/Q4 推翻计划保真 02 冻结文档——均按「契约/02 为事实源」裁定，开发 AI 未擅改契约（符合纪律）。
- 「信任但验证据」原则再次立功：交付文档漂亮，但门禁与穿透均由我亲自跑/查，确认无假修复后才验收。

## 补遗：冻结暂缓（用户裁决，2026-08-26 13:09）
- 用户指出：测试/契约全绿只证明「功能正确」，但**目录结构与组织风格由 AI 自主决策、未经其审阅**，与预期不符。
- 判定：功能完备 ≠ 可冻结。我此前单方面宣布「即日冻结」属 premature。已撤回冻结，M1 后端改为 **待调优（未冻结）**。
  - 动作：`03-统筹AI复审批复.md` 加第六节补遗；MEMORY.md 里程碑改为「门禁全绿但冻结暂缓 / 待调优」。
- 调优期门禁须全程保持绿（重构不破坏 126 测试）。待用户预期对齐 → 重构 → 复绿 → 用户确认 → 再冻结。
- **用户选择自写「目标结构补充文档」**（原话：一两句话说不清）。路径待定，用户写好后会告知。届时我读文档→出重构计划+批次任务包→开发 AI 执行→我验 126 测试+双门复绿→用户视觉确认→再冻结。当前代码保持待调优原样，不提前动手。

## 补遗2：用户产出目标结构 + 统筹出调优任务包（2026-08-26 13:39）
- 用户与独立架构师 AI 讨论后，写出 `docs/node-backend/04-目标目录结构.md`：**采用水平分层方案**（routes / services / shared / types / db / middleware / config），零行为变更重构规范；含 §2 分层职责铁律、§3 当前 lib→新结构逐文件映射表、§4 三批次（试点/主体/收尾）、§5 门禁、§6 风险回滚。
- **统筹先做两件事**：
  1. **修 `03-统筹AI复审批复.md` 的冻结结论**：此前仅加第六节补遗，但正文 〇/§四/§五/结尾仍写「即日冻结/冻结后/移交写作」自相矛盾。已统一改为「功能验收通过、冻结暂缓、待调优（未冻结）」口径（〇结论、§四纪律、§五下一步、结尾状态、§六改名「结论变更记录」）。
  2. **分析 04 架构合理性 + 起草开发任务**：结论「合理」，产出 `docs/node-backend/05-结构调优任务包.md`（含复制即用提示词、分层铁律、映射摘要、批次、门禁、禁止项、可选优化、交付物），事实源锁定 04、门禁锁定 126 测试+双门 33 OK。
- **架构分析要点（给用户的判断）**：分层清晰、routes 薄路由纪律治好 AI「routes 内嵌 DB/逻辑」原罪；`lib`→`shared` 命名比含糊 lib 好；迁移纯文件移动+import 调整，风险低。一点提示：services/ 仍保留 27 文件细粒度拆分（article 四件分立等）——用户原始抱怨含「组织风格不对」，若指文件过碎，可在确认规范后追加「合并同类」可选批；但 04 是用户+架构师定的事实源，先照执行不夹带。
- **当前状态**：M1 后端仍「待调优（未冻结）」；下一步开发 AI 持 05 任务包执行分层重构，每批交门禁证据，统筹验绿+用户视觉确认后再冻结。

## 补遗3：结构调优开发执行完成（本会话，2026-08-26 晚）

- 开发 AI 按 `04-目标目录结构.md` + `05-结构调优任务包.md` 完成全量分层重构（开发 AI 角色执行，统筹/架构师只审查不写码）：
  - 前置 Step A：`lib/`(24) 整体迁 `services/`(14)+`shared/`(10)（http-error→errors、jwt→auth 重命名）。
  - B-试点 → B-主体-1(articles/categories) → B-主体-2(users/me/auth) → B-主体-3(tags/site/upload/members/notifications) → B-收尾(types 上提)。
  - 新建 `types/auth.ts`(AuthVars/AuthUser)、`types/common.ts`(ErrorCode/BizErrorCode/Pagination/Envelope)、`services/site.ts`、`services/notification.ts`；扩 `user/tag/attachment` 等。
- **五门门禁最终全绿**：tsc 0 / biome 0(109 文件) / vitest **126 passed** / 契约结构门 OK / 契约语义门 **33 OK**；YAML 字节级未改。
- routes 全部 ≤200 行（最大 auth.ts 135）；services 超 200 者(article.ts≈252、user.ts≈250) 已注释标注「services 例外」；`shared/` grep 确认无 `@/services` 反向依赖。
- 交付物：`docs/node-backend/B-结构调优-NOTES.md`（批次证据/偏差说明/最终树/遗留可选项/铁律清单）。
- **偏差**：① lib 前置迁移独立于主体批；② types 层用 re-export 兼容（middleware/codes 透出 AuthVars/BizErrorCode），30+ 路由 import 与 3 测试零改动。
- **当前状态仍「待调优（未冻结）」**：功能+结构均达标，仍待 owner 视觉确认目录风格后冻结。下一步交统筹 AI 独立复验（git diff+门禁复跑+铁律逐条核对），通过后转「写 M1 后端文章」。

## 补遗4：后端架构师独立复审 B-结构调优（2026-08-26 晚）

- **裁定：审查通过（可放行，无 P2 阻塞）**，5 项 P3 非阻塞。报告：`docs/node-backend/review/B-结构调优-后端代码审阅报告.md`。
- **独立复验全绿**：tsc0 / biome 110 文件 0 / vitest 126 / 契约结构门 OK / 语义门 33 OK；`openapi.v1.yaml` git diff 0 行；4 测试文件改动仅 import 路径（无断言逻辑变更）。
- **铁律实证 key 点**：routes 无歧义 Drizzle token 全无（铁律②铁证，注意 grep `\.get\(` 会被 `c.get('user')`/路由方法名误报，需用 `getDb\|drizzle-orm\|\beq(\|sql\`\|\.run(\|\.all(` 复扫）；`git show d0e309b` 24 文件 0/0 纯移动、`15f516d` 为逻辑下沉；shared 无 `@/services` 反向依赖；middleware/codes 均 `export type` 透出 types；types 对 shared 仅 `import type`（无运行时环）。
- **P3 发现（须随批复修正文档）**：① `services/article.ts` 收 `c` 读 `c.req` 是**既有残留**（旧 lib/article.ts d0e309b~1:143/160/213/241 已有），非本次引入，归未来清理；② user(335)/category(373)/comment(226) 缺「services 例外」注释，仅 article 有；③ NOTES §四尺寸与"全部已标注"不实（article 实测 451 非≈252、user 335 非≈250）；④ types 非绝对最底层、措辞须精确为 type-only 引用 shared。
- **下一步**：开发 AI 补 3 份 service 注释 + 修正 NOTES §四 → owner 确认目录风格 → 冻结 → 写 M1 文章。

## 补遗5：P3 修订 + categories/comments 分拆合并（本会话）

- 开发 AI 落实 B-结构调优审阅 P3 收尾（owner 提议合并分拆文件，已 AskUserQuestion 确认「合并两者」）：
  - **P3-2**：`services/user.ts`(335)/`category.ts`(373)/`comment.ts`(226) 头部补「services 例外」注释，与 `article.ts` 对齐。
  - **P3-3/P3-4**：`B-结构调优-NOTES.md` §四 以真实 `wc -l`（article 451 / category 373 / user 335 / comment 226 / article-mutation 185）替换原≈252/≈250 误述；types 措辞精确为「仅含类型、对 shared 为 import type 引用、无运行时依赖与环」。
  - **文件合并**：`categories-read/write`(37+67) → `categories.ts`(89)、`comments-read/write`(46+70) → `comments.ts`(102)；删 4 分拆文件、`app.ts` 挂载改单实例单挂。纯机械重构、零行为变更。
- **五门门禁复跑全绿**：tsc 0 / biome 0(109 文件) / vitest **126 passed** / 契约结构门 OK / 契约语义门 **33 OK**；routes 由 23 降至 **21** 文件、均 ≤200（最大 auth.ts 135）。
- NOTES §三 加第 4 项偏差说明。状态仍「待调优（未冻结）」，待 owner 视觉确认目录风格后冻结 → 写 M1 文章。

## 补遗6：categories/comments 合并文件崩溃重做（本会话末）

- owner 指出合并当次 agent 崩溃、两合并文件可能未正确处理。开发 AI 以 `services/category.ts`/`services/comment.ts` 导出 + `openapi.v1.yaml` 路径为事实源**重建** `categories.ts`/`comments.ts`，逐端点核对契约 5 条 category 路径 + 4 条 comment 路径全部覆盖、service 调用签名逐一对应。
- **五门门禁复跑全绿**：tsc 0 / biome 0(110 文件) / vitest **126 passed** / 契约结构门 OK / 契约语义门 **33 OK**；`openapi.v1.yaml` 字节级未改。
- NOTES §三 第 4 项追加「崩溃重做核对」说明。结论：两合并文件经重建验证完整正确，崩溃未造成端点/逻辑缺失。

## 补遗7：上传附件去重优化方案 A+（本会话，架构师设计）

- owner 提出上传无去重（同图多传存多份文件）。架构师实证根因：`storage.ts:43` 用 `randomUUID()` 生成 key（与内容无关）、`attachments` 表无 hash 列、`createAttachment` 无条件 insert。
- **方案裁定**：A+ = 内容寻址物理去重（key=sha256(buffer)+ext；`storage.get` 命中复用不写盘）+ 删除事务内引用计数（防误删共享文件）。契约**零变更**（请求体/响应/端点/错误码均未变；content_hash 不进 Attachment 响应；DB 列属内部非契约）。C(客户端预检) 因新增端点会动契约，冻结下禁止。
- **关键设计点**：① 采用 `hash+ext` 而非纯 hash——`files.ts:41` 反解 ext、`SAFE_KEY` 兼容，serve 路由零改动；② **核心去重无需任何 schema 变更**（仅 storage+service 改动）；`content_hash` 列仅作可选增强（backfill/scheme-B 就绪），且须双源迁移（schema.ts + migrate.ts STATEMENTS + drizzle-kit，migrate.ts:7-8 警告防漂移）；③ 删除并发必须事务包裹（删行+计数+条件删文件），否则会与并发上传交错误删文件；④ `storage.put` 仅被 `services/attachment.ts` 调用，改动面隔离。
- 交付：`docs/node-backend/06-上传去重优化方案.md`（含逐方案选型、契约影响正式裁定、竞态分析、测试策略、风险回滚、P0-P5 实施批次、4 项待 owner 裁决开放问题）。待 owner 裁决后交开发 AI 执行、架构师独立复验。

## 补遗8：去重 4 项开放问题裁决 + 批次任务包（本会话末）

- owner 裁决：① **全局**去重（跨用户共享，公开博客无碍）；② **不做** `content_hash` 列（核心功能不需要，P0/P2 取消 = 零 DB 迁移）；③ **不做** 存量 backfill（P5 取消，历史 randomUUID 文件不折叠）；④ **接受** 同字节异 ext 轻微残留重复（维持 hash+ext，不升级纯 hash）。
- 据此收敛 `06`：§11 标注裁决、§10 收敛为 P1/P3/P4（P0/P2/P5 取消）、§3.2 标注 content_hash 步骤跳过（put 返回维持 `{key,url}`、不落库）。
- 产出 `docs/node-backend/07-上传去重-批次任务包.md`（交开发 AI 执行，架构师复验）：
  - **改动面极窄**：仅 `storage.ts` 的 `LocalStorage.put`（randomUUID→sha256(buffer)+ext，get 命中复用不写盘）+ `attachment.ts` 的 `deleteAttachment`（getDb().transaction 内：读行→删行→count 同 key 兄弟→仅 0 才删物理文件）。
  - 含**越界清单**（禁改 yaml/Attachment 响应/加列/改 files.ts/upload.ts/加端点）、接受准则（grep 自验 + 事务原子性）、P4 五门门禁（含 126 + 契约双门 33 OK + yaml 字节级 0 diff）、交付复验流程。
  - 明确 R2 分支 deferred（无凭证不实测，沿用 B10）。
- 已确认代码现状（回读实证）：`storage.put` 现状 `:42-47` 用 randomUUID；`get(key)` 已存在返回 Buffer|null；`attachment.deleteAttachment:118-137` 无引用计数；`sql` 已在 attachment.ts:6 导入。
- 状态：方案+任务包已就绪，待开发 AI 实施 → 架构师独立复验（git diff 仅两文件 + 五门门禁 + 契约字节级零变更）→ owner 视觉确认 → 冻结。

## 补遗9：上传去重开发实施完成（本会话末，开发 AI 角色）

- 开发 AI 按 `07` 任务包实施 A+ 去重（owner 已裁定合理 → 直接开干）：
  - **P1**：`src/shared/storage.ts` `LocalStorage.put` 改 `sha256(buffer)+ext` 内容寻址；`get(key)` 命中即复用、跳过写盘；移除无用 `randomUUID` import。接口返回仍 `{key,url}`（决策 #2 不落 hash）。
  - **P3**：`src/services/attachment.ts` `deleteAttachment` 删行+兄弟计数+仅 `remaining===0` 才删物理文件。
  - **P4**：新增 `test/services/attachment.test.ts`（6 用例：存储去重/writeFile 仅 1 次、同字节异 ext、全局服务去重、删除无孤儿、末引用删除、幂等）；beforeEach 注 users FK 行 + 清 attachments。
- **与原方案代码的一处必要偏差（已记录于交付文档）**：Drizzle better-sqlite3 的 `transaction` 回调**必须同步**，异步回调会抛 "Transaction function cannot return a promise"。故 DB「删行+计数」放同步事务（保留防并发交错的原子性），**异步文件删除移到事务外** best-effort（catch 吞掉，与原方案同款边界）。文件删除本就是 best-effort，核心 DB 原子性已由同步事务覆盖。
- **五门门禁全绿**：tsc 0 / biome 0(113 文件) / vitest **132 passed**(126+6) / 契约结构门 OK / 契约语义门 **33 OK**；`openapi.v1.yaml` `git diff` 为空（字节级零变更）。越界核验：schema/migrate/files/upload/yaml 均无 diff、未新增 content_hash。
- 交付物：`docs/node-backend/08-上传去重-交付文档.md`（改动清单/grep 自验/五门门禁/契约零变更/6 测试/回滚/偏差说明）。
- **状态仍「待调优（未冻结）」**：去重作为已冻结主线的收口优化已完成；下一步交 BackendArchitect 独立复验（git diff 仅 storage.ts+attachment.ts + 五门门禁 + 契约双门字节级零变更）→ owner 视觉确认 → 冻结 M1 后端主线。

## 补遗10：上传去重 独立复审裁定（本会话末，BackendArchitect）

- 架构师独立复审开发 AI 交付（提交 `5252c3d` + `08` 文档）：报告 `docs/node-backend/review/B-上传去重-后端代码审阅报告.md`。
- **五门门禁复跑全绿**（独立）：tsc0 / biome 113 文件 0 / vitest **132 passed**(126+6) / 契约结构门 OK / 语义门 **33 OK**；`openapi.v1.yaml` 字节级 0 行 diff；contentHash 未进响应（决策#2 守住）；randomUUID 已移除；仅 2 源文件+1 测试改动。
- **P2（冻结前置，须 owner 裁决）**：`DELETE /attachments/{id}` editor/admin 删「不存在 id」由原 404 变 200，违反冻结契约 `:2297-2303`（404 附件不存在）。根因链：`guard('editor',...)` 的 `roleOk` 满足即 `return next()`(auth.ts:53-54)→editor 直达服务；新 `deleteAttachment` 对缺失 id `if(!row) return`(resolve) 而非原 `changes===0→throw 404`。开发 AI 写入「幂等」测试却**未标注为契约偏差**。语义门是静态 YAML 检查抓不到运行时行为差异。修复=复原 `attachment.ts:131` 为 `throw AppError(NOT_FOUND,404)`（方案A 严守契约，推荐）或 owner 明示接受幂等 200 留档（方案B）。**未决 P2 不得冻结**。
- **P3-1（已知局限）**：better-sqlite3 事务须同步 → 文件删除被迫移事务外 → 极窄孤儿竞态（并发同字节上传在 count=0 提交后/FS 删前插新行→孤儿）。`06` 原「文件删置 async 事务内」在 better-sqlite3 本就跑不起来，系唯一可行形态；属去重+引用计数+非事务 FS 固有代价，拒 ref_count 列无迁移路径，文档化接受。
- **P3-2（既有）**：守卫缺失 id 返 403(auth.ts:60) 非契约 404，本次前已存在；另立 issue 不计入责任。
- 下一步：开发 AI 完成 #1（选 A 或 B 留档）→ 申请复批（复跑门禁+双门+确认缺失 id 回归 404/owner 留档）→ owner 视觉确认 → 冻结 M1 后端主线。

## 补遗11：上传去重复审裁决 + review 文档重写（本会话末，owner 裁定）

- owner 对复审 P2/P3-1/P3-2 逐项裁决：
  - **P2 → 方案 A**：恢复缺失 id 的 404（`attachment.ts` 事务内缺失即 `throw AppError(NOT_FOUND,404)`，去掉恒 true 的 `found` 字段；测试改断言 `rejects` 404）。严守冻结契约。
  - **P3-1 → 接受**：孤儿竞态（better-sqlite3 同步事务迫使文件删置事务外）文档化接受，非阻塞；要求开发 AI 在 `deleteAttachment` 物理删处补注释说明窗口+未来 ref_count 缓解方向。
  - **P3-2 → 要求开发 AI 修复**：守卫 `null → 404`（全局纠偏）。根因 `guard()` 工厂未区分 `null`(资源不存在) 与"非归属者"；`null` 在 attachments/comments/articles 三类 `resolveOwner` 语义恒为"不存在"，契约既有一致（评论/文章测试已断言 404），零回归。
- **关键判断**：P3-2 修复点为共享守卫工厂 `guard()`（影响三类带 ownerOverride 资源，不传 resolveOwner 的 `guard('editor')` 等不进入该分支不受影响）；`guard.test.ts` ④(b) 用"非归属者"(`()=>'u1'` 配 `u2`) 仍 403，不误伤。修复是"实现错、契约对"的纠偏，非改契约。
- 已将 `docs/node-backend/review/B-上传去重-后端代码审阅报告.md` **重写为「裁定 + 开发 AI 修正指令」形态**：含 owner 裁决表、问题根因、三任务精确代码 diff（P2-A / P3-2 守卫 / P3-1 注释）+ 测试配套、复批门禁 5 条。owner 直接拿该文档交开发 AI 执行 → 架构师复批（确认缺失 id 回归 404 + 守卫 null→404 + yaml 字节级 0 diff）→ owner 视觉确认 → 冻结 M1 后端主线。

## 补遗12：上传去重 review 修正实施完成（开发 AI 角色）

- 按 `review/B-上传去重-后端代码审阅报告.md` 三任务执行修正：
  - **任务1(P2-A)**：`src/services/attachment.ts` `deleteAttachment` 事务内 `if(!row) throw AppError(NOT_FOUND,404)`（去 `found` 字段，类型 `{storageKey,remaining}`）；测试 `attachment.test.ts` 缺失 id 用例由 `resolves` 幂等改为 `rejects` 带 `code:NOT_FOUND/httpStatus:404`，新增 `ErrCode` import。
  - **任务2(P3-2)**：`src/middleware/auth.ts` `guard` 工厂区分 `null`(资源不存在→404) 与"非归属者"(→403)；`guard.test.ts` 新增 `GET /missing`(`()=>null`) + 断言 404 用例。
  - **任务3(P3-1)**：`deleteAttachment` 物理删处补注释说明事务外删文件的孤儿竞态窗口 + 未来 ref_count 缓解方向（owner 接受，非阻塞）。
- **五门门禁全绿（本轮复跑）**：tsc0 / biome 113 文件 0 / vitest **133 passed**(126+6 去重+1 guard404) / 契约结构门 OK / 语义门 **33 OK**；`openapi.v1.yaml` `git diff` 为空（字节级零变更）。
- **改动面收敛（git diff --name-only src/ test/）**：仅 `middleware/auth.ts` + `services/attachment.ts` + `test/middleware/guard.test.ts` + `test/services/attachment.test.ts` 四个文件；`storage.ts` 是 review 基线(5252c3d)内既有改动，本轮未碰。越界核验：schema/migrate/files/upload/yaml 均无 diff。
- 交付文档 `08-上传去重-交付文档.md` 同步更新：§一 文件清单增 auth.ts/guard.test.ts；§二 P3 代码同步新 deleteAttachment + 补 P3-1 孤儿竞态说明；§三/§五/§七 门禁数字 132→133、测试表「幂等→404」、结论改写三源文件。
- **状态仍「待调优（未冻结）」**：下一步交 BackendArchitect 复批（复跑门禁 + 缺失 id 回归 404 + 守卫 null→404 + yaml 字节级 0 diff）→ owner 视觉确认 → 冻结 M1 后端主线。

## 补遗13：DEV-LOG.md 断更补写（本会话）

- owner 指出 `docs/node-backend/DEV-LOG.md` 自 B6（2026-08-25）后断更，遗漏了 B7、结构调优、上传去重的思考过程。
- 已补写：B7 简述 + 结构调优水平分层重构（T-R1~T-R6）+ 结构调优 review（T-R7/T-R8 分拆合并）+ 崩溃重做（T-R9/T-R10）+ 上传去重批次（U-R1~U-R5，含 better-sqlite3 transaction 必须同步回调血泪坑）+ 上传去重 review 修正（U-R6~U-R9）+ 当前状态收尾。
- 重点保留"why"：分层禁令、机械迁移前提、分拆回收、以 services+契约为事实源重建、内容寻址、引用计数护栏、同步事务、孤儿竞态文档化、git diff 越界核验等可复用判断。
- 价值：DEV-LOG 是"后面接手的 AI / 未来的我"学判断的素材，不止 changelog；本次断更是"重代码轻记录"的滑坡，已纠正。

## 补遗14：上传去重 review 修正 · 复批通过（BackendArchitect 角色）

- 开发 AI 按 `review/B-上传去重-后端代码审阅报告.md`（修正指令稿）完成三任务并更新 `08`，架构师独立复批：报告 `docs/node-backend/review/B-上传去重-复审批复.md`。
- **复验证据（不采信自陈）**：tsc0 / biome 113 文件 0 / vitest **133 passed**(126+6+1) / 契约结构门 OK / 语义门 **33 OK**；`openapi.v1.yaml` 字节级 **0 行 diff**；randomUUID 已从 storage.ts 移除；contentHash 未进 Attachment 响应（决策#2 守住）；越界 5 文件（yaml/schema/migrate/files/upload）git diff 全 0。
- **三任务逐条核验通过**：① P2-A `attachment.ts:130` 缺失 `throw AppError(NOT_FOUND,404)`、`found` 字段已移除、测试 `:135` 断言 rejects 404；② P3-1 `attachment.ts:146-149` 孤儿竞态注释化接受（owner 接受，非阻塞）；③ P3-2 `auth.ts:58` `null→404`、`:61` 非归属者→403、全局零回归（三类 resolveOwner 的 null 语义恒为不存在）、`guard.test.ts:60` 新增 null→404 用例。
- **2 项 P3 非阻塞建议**：(A) 路由级 e2e 缺「editor 经完整路由 `DELETE /attachments/{不存在}` → 404」锁（现由 service 单测 `:135` + guard 单测 `:60` 间接锁死，建议补约 10 行集成用例）；(B) `storage.ts:2` 注释仍 `src/lib/storage.ts`（lib→shared 重构遗留，顺手改）。
- **裁定：复批通过，无 P2 阻塞**。状态：待 owner 视觉确认 → 冻结 M1 后端主线。
