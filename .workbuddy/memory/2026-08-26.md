# 2026-08-26 工作日志

## M1 Node 后端交付 · 总把控独立复验与验收冻结

- 开发 AI 交付 `docs/node-backend/M1-后端交付文档.md`（B0→B7 八批完成，声称 53 路径/67 操作、126 测试、双门 33 OK）。
- **总把控未采信自陈，独立复跑全部门禁**（托管 Node 22.22.2 + venv default）：
  - `tsc --noEmit` ✅ 0 error；`biome check .` ✅ 96 文件 0 问题；`vitest run`（串行+超时 30s）✅ **126 passed**（17 文件）
  - 结构门 `openapi-spec-validator` ✅ OK；语义门 `check_contract.py` ✅ 33 OK；`git status` ✅ 契约字节级未改
- **抽样穿透高价值裁定点（零假修复确认）**：
  - `refresh_tokens` 有状态表真在 `src/db/schema.ts:45`（落实 02-总把控裁决 Q3-A 推翻计划无状态）
  - 扁平信封真在 `src/lib/response.ts:55`（落实 Q1，错误复用同一 envelope、无 error 包裹）
  - 专用 401 保真（1001/1003）由 vitest 断言覆盖
- 产出 `docs/node-backend/03-统筹AI复审批复.md`：验收通过、即日冻结，并指派 §六 已知留口归属。
- **两项需统筹后续跟进**（非阻塞）：
  - §六#4 `GET /me/likes` 契约矛盾（裸数组 vs 声明 page/pageSize）= 冻结契约自身内部矛盾，非代码错 → 排「契约维护批次」统一整改，与既有 TODO「F2 计数复核」并轨
  - §六#7 nullable 唯一索引「假唯一」（email/slug 对 NULL 不冲突）→ 记入 Go/Python 跨端协调清单，勿误判为缺陷

## 下一步
- 统筹 AI 进入「写 M1 后端文章」（M1-01~M1-30），以验证冻结代码为唯一素材；`B0~B7-NOTES.md` + `DEV-LOG.md` 是直接素材。
- 本「契约→主计划→批次任务包」工作流将在 M2/M3/M4/M6/M7 五端原样复用（可沉淀 skill）。

## 关键判断
- 本轮协作模型验证成功：用户(owner)指定开发 AI 写码 + 我(总把控)写文档/验收。开发 AI 提的 8 问题中 Q1/Q2 是计划文档自身错（已就地修）、Q3/Q4 推翻计划保真 02 冻结文档——均按「契约/02 为事实源」裁定，开发 AI 未擅改契约（符合纪律）。
- 「信任但验证据」原则再次立功：交付文档漂亮，但门禁与穿透均由我亲自跑/查，确认无假修复后才验收。

## 补遗：冻结暂缓（用户裁决，2026-08-26 13:09）
- 用户指出：测试/契约全绿只证明「功能正确」，但**目录结构与组织风格由 AI 自主决策、未经其审阅**，与预期不符。
- 判定：功能完备 ≠ 可冻结。我此前单方面宣布「即日冻结」属 premature。已撤回冻结，M1 后端改为 **待调优（未冻结）**。
  - 动作：`03-统筹AI复审批复.md` 加第六节补遗；MEMORY.md 里程碑改为「门禁全绿但冻结暂缓 / 待调优」。
- 调优期门禁须全程保持绿（重构不破坏 126 测试）。待用户预期对齐 → 重构 → 复绿 → 用户确认 → 再冻结。
- **用户选择自写「目标结构补充文档」**（原话：一两句话说不清）。路径待定，用户写好后会告知。届时我读文档→出重构计划+批次任务包→开发 AI 执行→我验 126 测试+双门复绿→用户视觉确认→再冻结。当前代码保持待调优原样，不提前动手。

## 补遗2：用户产出目标结构 + 统筹出调优任务包（2026-08-26 13:39）
- 用户与独立架构师 AI 讨论后，写出 `docs/node-backend/04-目标目录结构.md`：**采用水平分层方案**（routes / services / shared / types / db / middleware / config），零行为变更重构规范；含 §2 分层职责铁律、§3 当前 lib→新结构逐文件映射表、§4 三批次（试点/主体/收尾）、§5 门禁、§6 风险回滚。
- **统筹先做两件事**：
  1. **修 `03-统筹AI复审批复.md` 的冻结结论**：此前仅加第六节补遗，但正文 〇/§四/§五/结尾仍写「即日冻结/冻结后/移交写作」自相矛盾。已统一改为「功能验收通过、冻结暂缓、待调优（未冻结）」口径（〇结论、§四纪律、§五下一步、结尾状态、§六改名「结论变更记录」）。
  2. **分析 04 架构合理性 + 起草开发任务**：结论「合理」，产出 `docs/node-backend/05-结构调优任务包.md`（含复制即用提示词、分层铁律、映射摘要、批次、门禁、禁止项、可选优化、交付物），事实源锁定 04、门禁锁定 126 测试+双门 33 OK。
- **架构分析要点（给用户的判断）**：分层清晰、routes 薄路由纪律治好 AI「routes 内嵌 DB/逻辑」原罪；`lib`→`shared` 命名比含糊 lib 好；迁移纯文件移动+import 调整，风险低。一点提示：services/ 仍保留 27 文件细粒度拆分（article 四件分立等）——用户原始抱怨含「组织风格不对」，若指文件过碎，可在确认规范后追加「合并同类」可选批；但 04 是用户+架构师定的事实源，先照执行不夹带。
- **当前状态**：M1 后端仍「待调优（未冻结）」；下一步开发 AI 持 05 任务包执行分层重构，每批交门禁证据，统筹验绿+用户视觉确认后再冻结。

## 补遗3：结构调优开发执行完成（本会话，2026-08-26 晚）

- 开发 AI 按 `04-目标目录结构.md` + `05-结构调优任务包.md` 完成全量分层重构（开发 AI 角色执行，统筹/架构师只审查不写码）：
  - 前置 Step A：`lib/`(24) 整体迁 `services/`(14)+`shared/`(10)（http-error→errors、jwt→auth 重命名）。
  - B-试点 → B-主体-1(articles/categories) → B-主体-2(users/me/auth) → B-主体-3(tags/site/upload/members/notifications) → B-收尾(types 上提)。
  - 新建 `types/auth.ts`(AuthVars/AuthUser)、`types/common.ts`(ErrorCode/BizErrorCode/Pagination/Envelope)、`services/site.ts`、`services/notification.ts`；扩 `user/tag/attachment` 等。
- **五门门禁最终全绿**：tsc 0 / biome 0(109 文件) / vitest **126 passed** / 契约结构门 OK / 契约语义门 **33 OK**；YAML 字节级未改。
- routes 全部 ≤200 行（最大 auth.ts 135）；services 超 200 者(article.ts≈252、user.ts≈250) 已注释标注「services 例外」；`shared/` grep 确认无 `@/services` 反向依赖。
- 交付物：`docs/node-backend/B-结构调优-NOTES.md`（批次证据/偏差说明/最终树/遗留可选项/铁律清单）。
- **偏差**：① lib 前置迁移独立于主体批；② types 层用 re-export 兼容（middleware/codes 透出 AuthVars/BizErrorCode），30+ 路由 import 与 3 测试零改动。
- **当前状态仍「待调优（未冻结）」**：功能+结构均达标，仍待 owner 视觉确认目录风格后冻结。下一步交统筹 AI 独立复验（git diff+门禁复跑+铁律逐条核对），通过后转「写 M1 后端文章」。
