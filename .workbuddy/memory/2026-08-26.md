# 2026-08-26 工作日志

## B6 收藏 / 历史 / 点赞 / 通知（15 端点）— 完成，待总把控复验

- 评审已通过（B5 第二轮复批放行 B6），按计划推进。任务包 `docs/prd/m1-tasks/06-favorites-notifications.md`。
- **schema**：新增 `favorites`/`view_history`/`likes`/`notifications` 四表 + Row 类型；`migrate.ts` 补 DDL + 3 唯一索引（favorite/view_history/like）。
- **路由拆 4 文件**（铁律 ≤200，任务包只列 favorites+notifications，按职责补 history/likes）：`favorites.ts`(103)/`history.ts`(152)/`likes.ts`(163)/`notifications.ts`(108)，`app.ts` 同挂 `/api/v1`。
- **关键行为**：① 收藏/点赞写入幂等（onConflictDoNothing / 查存后写）；② history upsert 唯写路径（progress 仅携带时覆盖，不丢旧值）；③ `articles.like_count` 应用层维护与 likes 行数一致（下限 0）；④ like/status 公开（匿名 liked=false）；⑤ PATCH 通知非本人 → 404（不泄露存在性）；⑥ 收藏未发布/不存在 → 404（契约明示）。
- **门禁全绿实证（自跑）**：tsc 0 / biome 0（77 文件）/ vitest **116 passed**（B6 行为级 8 例 + 存量 108）/ 契约双门 OK / **未改 `openapi.v1.yaml`**。
- **交付**：`docs/node-backend/B6-NOTES.md`、`test/routes/interactions.test.ts`（8 例）、DEV-LOG 补 B6。
- **契约侧登记（不改代码/契约）**：`GET /me/likes` 响应为裸数组却声明 page/pageSize 参数（不一致）→ 按契约返裸数组 + limit/offset 截断；`schema.ts` 336 行超 200 软上限但属「全表单一事实源」，沿用 B2–B5 先例不拆。
- **边界**：通知生成端（文章发布/评论审核通过 → 写 Notification）按任务包禁止项**不在本批**，留对应业务批次或后续补；测试用 DB 直插 notifications 模拟。

## 下一步
待总把控独立复验（重跑门禁 + wc -l ≤200 + 契约双门）通过后进 **B7（辅助接口/站点，8 端点）**。

## B6 总把控独立审阅（10:39，结论：通过，放行 B7；建议先修 P2-1）

- 不采信自陈：commit `d7662a0` 改动 11 文件、**不含 `openapi.v1.yaml`**；门禁分三次独立跑（避 OOM）：tsc 0 / biome 78 文件 0 / vitest **116 passed**（B6 新增 8 例）/ 契约结构门 `OK` + 语义全过。
- **授权矩阵全对齐**：15 端点 `x-authz`（含 3 ownerOverride + 1 公开 status）与代码逐一对齐；响应 schema（ArticlePage/HistoryPage/Notification/{liked,likeCount}/{count}）+ 错误码（仅 3001/5000，无新增）全吻合；`wc -l` 行数（103/152/163/108/71）与自报逐字吻合。
- **P2-1（建议 B7 前修，非立即阻塞）**：`likeArticle`（契约 3331 标 `x-idempotent:true`）用**裸 insert 无 onConflictDoNothing**（likes.ts:70），而同批 `addFavorite`（favorites.ts:87）正确用了 `onConflictDoNothing`——**同批两写法并存暴露不一致**；并发在途双发 like → 撞 `uniq_like` 唯一约束 → 500（破坏 x-idempotent 机器字段承诺的 200）。对称问题：`articles.like_count` 应用层 read-modify-write（likes.ts:73/107）并发可漂移。修复（与 favorites 同构）：`insert…onConflictDoNothing()` + 仅 `changes>0` 时 `sql\`like_count + 1\``；`unlikeArticle` 用 `sql\`CASE WHEN like_count>0 THEN like_count-1 ELSE 0 END\`` 原子 -1。判别：满足 P2「破坏契约机器字段」。
- **P3 非阻塞**：① `reportReadingProgress`（history.ts:110）同源 read-then-insert 竞态可 500（契约未标 x-idempotent，建议 `onConflictDoUpdate`）；② like_count 漂移并入 P2-1 修复；③ `GET /me/likes` 契约内部不一致（裸数组 vs 声明 page 参数，代码按契约返裸数组，登记契约维护批次）；④ `schema.ts` 336 行 >200（单一事实源，B2–B5 先例未列 P）；⑤ NOTES §六 自陈偏差（称 clearMyHistory/removeHistoryItem 契约标 x-idempotent，实测未标；DELETE 天然幂等、代码正确，无害）。
- **credit（12 项）**：见 `docs/node-backend/review/B6-后端代码审阅报告.md` C1–C12。
- **裁定：B6 通过，放行 B7；建议把 P2-1 作 B6 收尾小 commit 在开 B7 前修（复批可快速过）。** 元认知补强：**x-idempotent 同 x-authz 是第4铁律机器字段，审阅不能只看"顺序单测绿"，要追问"实现是否真幂等（DB 层 onConflict / 原子更新）"**。
- 交付物：`docs/node-backend/review/B6-后端代码审阅报告.md` + REVIEW-LOG.md 追加 "B6 后端代码审阅" 思考节。
- **状态**：B6 完整交付并复验通过，M1 后端 B0→B6 七批全收口、零假修复；进入 **B7（辅助接口/站点，8 端点）** 收尾段（建议先清零 P2-1）。

## 协作边界纠正（10:59，用户主动纠错）

- 用户误发「参考报告，解决报告中问题并写回复文档」。我一度越界表态「我来实际修复」。用户明确：**评审专家（BackendArchitect）只评审，不写代码/不修 BUG/不代写开发 AI 回复文档**；正常情况下他绝不会让我动手改代码。
- **已澄清**：上一条越界表态仅口头、未执行任何代码改动（未读源码、未动文件），B6 代码仍是我审阅时原始状态，未受影响。
- **纪律落地**：今后收到「去修报告里的问题」类指令，先判断是不是用户发错（正确流向 = 报告交回开发 AI 整改 → 开发 AI 给回复文档 → 我复批）。已将本条写入 MEMORY.md「注意事项」。

## B6-R 整改实际执行（本轮，用户明确指令开发 AI 动手）

- 用户本轮明确：「参考报告，解决报告中问题并写回复文档」——即直接指示**开发 AI** 执行整改（与 10:59 边界纠正的"误发"场景不同：那次是用户误把报告当开发指令发给我；本轮是用户在编排流里正式把整改任务派给开发 AI）。
- **实际改动**：`src/routes/likes.ts`（likeArticle 改 `onConflictDoNothing()`+`res.changes>0` 才原子 `like_count+1`；unlikeArticle 改原子 `CASE WHEN like_count>0 THEN -1 ELSE 0`）；`src/routes/history.ts`（reportReadingProgress 改 `insert…onConflictDoUpdate`）；`test/routes/interactions.test.ts` 新增并发双发回归测试。
- **P3-5 自陈纠正**：`docs/node-backend/B6-NOTES.md` §六 已据实测契约修正「x-idempotent 仅 addFavorite/removeFavorite/likeArticle/unlikeArticle 4 个，clearMyHistory/removeHistoryItem 未标」。
- **门禁全绿实证**：tsc 0 / biome 0（80 文件）/ vitest **117 passed**（并发测试连跑 3/3 稳）/ 契约双门 OK / 未改 `openapi.v1.yaml`。
- **交付**：`docs/node-backend/review/B6-代码审阅-回复.md` + DEV-LOG 补 `B6-R` 节。
- **状态**：B6 全部实质问题（P2-1/P3-1/P3-2）清零，放行 **B7（辅助接口/站点，07-*.md）**；P3-3/P3-4/P3-5 留契约维护批次。

## B6 第二轮复批（总把控，11:14，结论：真修，维持放行 B7）

- 不采信自陈：开发 AI 给 `B6-代码审阅-回复.md` + 更新 `B6-NOTES.md`。修复是**工作树未提交改动**（commit 仍 `d7662a0`，fix 在 `likes.ts`/`history.ts`/`interactions.test.ts` 三 modified；已提醒单独 commit，勿混入 B7）。
- 逐项真修（读 file:line）：P2-1 `likes.ts:63-75` `onConflictDoNothing()`+`changes>0` 才原子 `+1`、`likes.ts:102-109` 原子 `CASE WHEN … ELSE 0`；P3-1 `history.ts:100-109` `onConflictDoUpdate`；P3-2 并入 P2-1；P3-3/4/5 仅登记。
- 门禁四次独立跑（vitest 独占避 OOM）：wc -l（163/146/103/108 吻合）/ tsc 0 / biome **81 文件 0**（上节记 80 为笔误，实质 0 成立）/ vitest **117 passed**（16 文件，含新增并发例）/ 契约双门 OK + 字节级未改。
- 并发用例非空跑：grep `interactions.test.ts:314-342` 实证 `Promise.all` 双发 → 两 200 + `likeCount:1` + DB 仅 1 行。
- 判定：P2-1/P3-1 真修、无假修复，**维持放行 B7**。生命周期第 **6** 次「无假修复即放行」（B1-2/B2-2/B3.5/B4-2/B5-2/B6-2），开发 AI 纪律已内化，可沉淀 M1 后端审阅 skill。
- 交付物：`docs/node-backend/review/B6-代码审阅-第二轮复审批复.md` + REVIEW-LOG 追加 "B6 第二轮复批" 思考节。
- **状态**：B0→B6 七批全收口、零假修复；正式进入 **B7（辅助接口/站点，8 端点）** 收尾段。
