# 2026-09-17 工作日志

## 发布维护 · M2-02/03 同步 + 后续草稿内链

**SOP-A 镜像（2 篇）**
- M2-02 后台骨架 → `165589276` → 🟢
- M2-03 请求层封装 → `165590548` → 🟢

**注入（7 处，指向已发布的 M2-02/M2-03）**
- `M2-12` L161 → M2-02
- `M2-05` L20 → M2-02 + M2-03（同一行两处占位）
- `M2-04` L178 → M2-03
- `M2-07` L21/L260 → M2-03（replace_all）
- `M2-01` L181 → M2-02（**核验阶段才发现的漏注**）

**证据**：grep `{{LINK:(M0-*|M1-*|M2-01/02/03)}}` → 0 残留；ARTICLES.md 第 62/63 行 🟢，第 64 行 M2-04 保持 🟡。

## ⚠️ 本轮教训：规划阶段漏项，靠核验兜住

首轮通读全量占位清单时，**漏看了 M2-01 L181 的 `{{LINK:M2-02}}`**（清单输出很长，M2-01 的条目混在中间）。是在编辑完成后的核验 grep 里抓出来的。

**改进建议（下次执行）**：
- 不要依赖"通读一大坨 grep 输出"来列注入清单。改为**按目标编号逐个 grep**：确定本轮发布了哪几篇，就分别 grep `{{LINK:M2-02}}`、`{{LINK:M2-03}}`，逐个精确列出命中行，再动手。
- 核验 grep 必须覆盖**全部已发布编号**（M0 全量 + M1 全量 + M2 已发布），不能只查本轮新增的两个编号——否则漏注会被放过。

## M2 系列占位形态（与 M1 不同，需牢记）

| 系列 | 形态 | 替换方式 |
|---|---|---|
| M1 | 裸占位 `{{LINK:M1-29}}` | 替换为完整 markdown 链接 `[标题](URL)` |
| M2 | `[标题]({{LINK:M2-02}})` | 占位在 URL 位置，**只填 URL**，不动链接结构 |

M2 的链接文案已由作者写好，替换时不要重复加 `[标题](...)`。

## 用户指令的一处歧义（未追问，按规则执行）

用户写："更新 m2-03 04 等文章的内链"。M2-03 本轮刚发布，实际需处理的是 M2-04/05/07/12 等草稿。按既定规则（**注入所有指向已发布文章的占位符，与源文件状态无关**）执行，覆盖了全部 7 处。若用户本意只是 04/05，本轮覆盖更广但无副作用。

## 待办
- 上轮报告的 M1-31 疑似坏链（`164493891`）owner 未回复，仍未修
- ARTICLES.md 第 57 行 `## M2 · React 管理后台（待写作）` 标题仍未更新
