# 2026-09-10 工作日志

## 发布维护 · M1-21/22 同步 + 23/24 内链

**SOP-A 镜像**
- M1-21 后端测试策略 → `164720486` → 🟢
- M1-22 容器化 → `164721321` → 🟢
- 至此 M1-01~22 全部已发布

**回填（3 处）**
- `M1-20` L156 `{{LINK:M1-21}}` → 后端测试策略
- `M1-01` L106 `{{LINK:M1-21}}` → 后端测试策略（历史遗留伏笔，本轮补齐）
- `M1-21` L157 `{{LINK:M1-22}}` → 容器化

**23/24 无注入项**
- `M1-23` 唯一占位 `{{LINK:M1-24}}`（今天发，保留）
- `M1-24` 唯一占位 `{{LINK:M1-25}}`（未发，保留）
- 连续第四轮该情况，确认是写作风格（后续文章对已发文章多用纯文本引用），非漏注

**证据**
- grep `{{LINK:M1-01~22}}` → 0 残留，零漏注
- ARTICLES.md 第 45/46 行 🟢，47/48 行保持 🟡

**CSDN 副作用**：本轮改了 3 篇已发布文章 `M1-01`、`M1-20`、`M1-21`，需 blog AI 重新发布才生效。

## 踩坑记录（重要）

1. **文件名猜测导致假阴性**：用 `M1-23-部署上线-Cloudflare与Linux双轨适配.md` 猜测路径 grep 得到 "No matches found"，误以为无占位。实际文件名为 `M1-23-部署上线-从本地起服到真正对外服务.md`、`M1-24-一套后端双部署-适配层如何让一份代码跑在两套运行时.md`。**教训：查询具体文章前必须先 Glob 拿真实文件名，禁止凭编号猜文件名。**

2. **网关链路不稳定**：2026-09-09 至 09-10 期间，工具调用经 copilot.tencent.com 网关反复 502 / ETIMEDOUT / socket hang up，每轮被整体中断、上下文作废，导致同一任务重复重做多轮，用户感知为"卡死"。非用户本地网络问题。**教训：链路不稳时应压缩每轮工具调用数量、避免多次往返；连续中断后可向用户主动说明真实原因。**

3. **Edit 缩短 old_string 规避引号规范化**：中文正文里的引号经 Read 输出后可能被规范化，直接用整句匹配易失败。改用不含引号的短子串（如 `（{{LINK:M1-21}}）我们聊`）可稳定命中。
