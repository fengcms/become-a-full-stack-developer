# 2026-09-12 工作日志

## 发布维护 · M1-27/28 同步 + 29/30/31 内链（M1 末批）

**SOP-A 镜像**
- M1-27 评论内容安全 → `165110811` → 🟢
- M1-28 辅助接口 → `165111053` → 🟢
- 至此 M1-01~28 已发布；29/30/31 为本轮末批，发完即 M1 全 31 篇收官

**回填（2 处）**
- `M1-26` L194 `{{LINK:M1-27}}` → 评论内容安全
- `M1-27` L224 `{{LINK:M1-28}}` → 辅助接口

**29/30/31 无注入项**
- `M1-29` L153 唯一占位 `{{LINK:M1-30}}`（今天发，保留）
- `M1-30` L145 唯一占位 `{{LINK:M1-31}}`（今天发，保留）
- `M1-31` **无任何占位符**（收官篇，无"下一篇"，符合预期）
- 连续第七轮该情况，写作风格使然，非漏注

**证据**
- grep `{{LINK:M1-01~28}}` → 0 残留，零漏注
- ARTICLES.md 第 51/52 行 🟢，53/54/55 行保持 🟡

**CSDN 副作用**：改了 2 篇已发布文章 `M1-26`、`M1-27`，需 blog AI 重新发布才生效。

## 待办
- 29/30/31 发布后需再跑一轮 SOP-A：镜像 3 条 URL + 回填 M1-29 的 `{{LINK:M1-30}}` 与 M1-30 的 `{{LINK:M1-31}}`。届时 `{{LINK:}}` 应全仓清零。

## 踩坑
- **Glob 不支持 `(29|30|31)` 交替语法**：`articles/M1-(29|30|31)-*.md` 返回空。改用字符类 `M1-2[9]-*.md` 和通配 `M1-3*-*.md` 分两次查才拿到。**记住：Glob 只用字符类 `[...]` 和 `*`，不用 `(a|b)`。**
