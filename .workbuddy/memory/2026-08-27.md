# 2026-08-27 工作日志

## M0 八篇回炉改造清单（统筹 AI）
- 通读 `articles/M0-01~M0-08` 八篇草稿：已较扎实（M0-04 有 ASCII ER 图、M0-08 有 7 张双列对比表），但**不一致于已定调**：标题缺 `成为全栈·产品篇·` 前缀、延伸阅读用纯文本「（发布时回填 CSDN 链接）」而非 `{{LINK:Mx-yy}}` token、零配图（违背 CSDN 2–4 图/篇规则）。
- 产出《docs/M0-回炉丰富度改造清单.md》：八篇通用规则（标题改名 / `{{LINK}}` 内链 token 化 / `{{IMG}}` 配图占位 / 文末 CTA / 排版多样性 / 不注水）+ 现状体检表 + 逐篇改造清单 + 验收口径 + 附录 A 内链映射 + 附录 B 配图资产清单。
- 约定：仓库内文档（`00/01/02`、`openapi.v1.yaml`）相对链接**不** token 化；`M1-24` 为未来篇，token 先占位。
- CTA 订阅 Banner 图：用户拒绝本次生成（省 credits），清单 §0 规则 5 与附录 B 已改为「待生成」，并移除对不存在文件的引用。

## 配图中文提示词包（用户用豆包出图）
- 用户明确：图片由他用**豆包**在线生成（中文+图上文案支持好，已用于博客封面/插图），不走本机 ImageGen（省 credits）。
- 产出《docs/M0-配图中文提示词包.md》：附录 B 全部 17 个 `{{IMG:}}` token 的**完整中文提示词**（含建议比例），并附「出图→传 CSDN→替换 token」流程。清单 §0 规则 4 已挂引用。
- 工作流定调：统筹只产出中文提示词 + 占位 token；用户出图托管后，发布阶段把 `{{IMG:}}`/`{{LINK:}}` token 替换为真实地址。

## 待办（用户已锁定优先序）
1. 照清单逐篇回炉 M0 八篇（加图/码/表、标题改名、内链 token 化、文末 CTA）。
2. 写根目录 README.md + node-backend/README.md。
3. 按 `成为全栈·Node 后端篇·xx` 写 M1 三十篇。

## M0-01 首篇回炉试做（用户选 A：先动一篇看效果）
- 完整重写 `articles/M0-01-为什么前端工程师要走向全栈.md`：加标题前缀 `成为全栈·产品篇·`；内链 prose→`{{LINK:M0-08}}`/`{{LINK:M0-04}}`（往期前端文章保留手动 URL 占位）；新增「调接口的人」TS 码块（~12 行，中文注释）；按提示词包补 `{{IMG:01-链路全景图}}`+`{{IMG:01-能力地图剖面}}`；两处 引用块金句；文末 `{{IMG:CTA-订阅专栏}}`。
- 强化「设计主导+陪练、非经验复盘」叙事身份：第五节明写「这套专栏是我刻意设计出来的，不是随笔式经验复盘」。
- 字数 1854→2209（补三段不注水实质内容：黑盒内真实角色 / 跨层排错视角红利 / 七端复利具象例 + 市场供需动机 / 能力地图即专栏骨架 / 反焦虑收尾）。
- 同步 `ARTICLES.md` M0-01 标题列加前缀。复验：无「发布时回填」残留、token 写法一致、3 图 + 4 内链。
- 用户确认风格「可以，就这么写」后，批量回炉 M0-02~08 七篇（全按 M0-01 范式）。

## M0-02~08 批量回炉完成（全部过线）
- 八篇统一：标题前缀 `成为全栈·产品篇·`、内链 `{{LINK}}` token 化（仓库 doc 00/01/02 + openapi 保留真实相对链接）、按清单补 `{{IMG}}` 占位（M0-05 文件名「六个端」→「七个端」并删旧文件）、文末 `{{IMG:CTA-订阅专栏}}` + 弱化营销 CTA。
- 补字到达标线（≥2200 中文字符）：逐篇补不注水实质内容（失败模式/反例/对标清单/最小路径/读者 FAQ/公约信任论等），最终字数 2207~2237 全过线。
- 同步 `ARTICLES.md` M0-02~08 标题列加前缀（8 行全部带前缀）。
- 复验全绿：无「发布时回填 CSDN 链接」残留、8 篇标题前缀齐全、无重复段落（版本化/锦上添花/持续产出各 1 次）。
- **M0 八篇回炉全部完成**，待用户用豆包出图并发布后，由 blog AI 索引注入真实 `{{IMG}}`/`{{LINK}}` 地址。

## 根 README + 后端 README 提示词（本任务）
- 用户提供 git 仓库地址：`https://github.com/fengcms/become-a-full-stack-developer`。
- 写根目录 `README.md`：定位（文章是产品/代码是素材）+ monorepo 七端 + 仓库内容表 + M0–M8 七子项目表 + 契约硬地基 + 文章↔tag 机制 + 当前进度（M0 草稿就绪/M1 待冻结/M2–M8 未始）+ 阅读方式 + 作者（FungLeo / CSDN）。
- 核对事实：`src/lib` 已为空目录且无任何引用，证实 04 目标结构的 routes/services/shared/types 分层已落地；services 19 / shared 10 / routes 21。
- 给后端开发 AI 起草任务提示词《docs/node-backend/README-任务提示词.md》：指令其只为 `node-backend/` 写 README（不改码），必读 04/交付文档/package.json/openapi 头部/src 实际分层；须含定位/技术栈表/分层字符树+职责表/快速开始(pnpm)/migrate+seed 建首 admin（明令禁直插库绕过）/质量门(126 passed+双门33 OK)/契约只读/当前状态(53路径67操作)；文风第一人称+表格+不注水。用户可原样转发给后端 AI。

## 备注
- 当前 M1 后端状态：功能验收通过、待调优（结构调优已落地且用户认可「比我想的好」，待用户最终视觉确认后冻结）。
- 链接协作：用户发布首批后通知 blog AI 跑 `csdn_backup.py links`，统筹读 `materials/csdn-已发布链接.md` 镜像进 ARTICLES.md 并注入 `{{LINK}}` 内链。

## tag 缺失问题 + 后端 README 验证（用户问「tag 没打能否弥补」）
- git 实况：`master` 同步 origin，**tag 数=0**；工作区干净；M0 八篇已在 `9ba75db` 提交；根 README+提示词在 `ab46f6f` 提交。
- 结论：**可完全弥补**。约定是「发布时打 tag」(章程§七)，M0 还是草稿(待出图+发CSDN)，触发时机未到，非事故；git 历史完整，tag 只是指针可事后补打且不改历史。
- 关键纠正：**不要给当前草稿打 tag**（checkout 会出带 `{{IMG}}`/`{{LINK}}` 占位符的草稿，违背"发布时状态"语义）。正确时机=每篇发布后(图/链接注入完成并提交)打 `article/Mx-yy`。M0 不对应独立代码库→tag 标的是"该文发布时整个仓库状态"，8 个 tag 落相近区间，按发布顺序 commit+tag 成 M0-01→M0-08 序列。
- 修补动作：把「打 tag」补为《docs/链接与发布协作约定.md》标准流程第 5 步 + 新增第八节专门说 tag 约定(触发时机/M0特殊处理/禁止项/历史补打命令)。
- **发现后端 AI 已按提示词产出 `node-backend/README.md`(153行)+`DEV-LOG.md` 并已在 `ab46f6f` 提交**。验证(信任但要验证)：事实准确——**实测 `pnpm test` = 133 passed(非它自报的126)**；`.env.example` 存在；10 个 `B*-NOTES.md`+`DEV-LOG.md` 真实存在。但相对链接 bug：从 node-backend/ 看 `docs/...`/根 `README.md` 缺 `../` 打不开，已修。
- **暴露 126→133 过时**：04 规范/交付文档/记忆写死 126，实际 133(调优后增 7 测试)。已把 `04-目标目录结构.md` 质量门改 133(注明原126为调优期基线)；交付文档为交付时点记录(126 为当时值)，当前代码 133，是否改交付文档由 owner 定。

## tag 约定重订为里程碑式（推翻 per-article，2026-08-27 用户拍板）
- 用户认为 per-article tag 要求过高、会乱（M0 不改代码打 8 个 tag 无意义）。**原「每篇打 `article/Mx-yy`」方案废止**。
- 新方案：**里程碑式 tag**——只标记代码库跨阶段节点，命名 `<里程碑>-v<主版本>`：`contract-v1.11.0`(契约冻结)/`node-backend-v1.0`(M1后端冻结)/各端 `*-v1.0`/`final-v1.0`。M0 产品侧不打 tag。
- 已改写《docs/链接与发布协作约定.md》：第三章第5步 + 第八节均改为里程碑式；保留"已废止 per-article"说明文字（非残留）。
- **已打并推送 `contract-v1.11.0`**（指向 HEAD `ab46f6f`，含 a00377d 对 openapi 的描述性纠错 code1003→code1004，即契约最终准确冻结态）。`node-backend-v1.0` 待用户视觉确认冻结后打。
- M0 八篇文末统一追加「## 相关资源」模块：专栏地址(category_13204651.html) + 仓库根地址。复验八篇均✓。
- CSDN 专栏地址已记入项目 MEMORY.md（协作约定段）。根 README 经核查无 per-article tag 描述，无需改。

## M1 后端正式冻结（2026-08-27 owner 裁决）
- 用户原话：「后端冻结吧，如果后面发现有 BUG，到时候再修复更新，我暂时想不到后端还有什么需要调整的地方。」→ **即日冻结**。
- 冻结前独立复跑三道门禁全绿：tsc 0 error / biome 115 文件 0 问题 / vitest **133 passed**(18 文件) / 契约双门 33 OK。实测数字(133/115)已同步进 03 复审批复，覆盖旧记录 126/96。
- **已打并推送 `node-backend-v1.0`**：commit `5ea6d54`，push master + tag 均成功。现仓库两里程碑 tag：`contract-v1.11.0` + `node-backend-v1.0`。
- 文档同步（commit 5ea6d54，7 文件）：
  - `docs/node-backend/03-统筹AI复审批复.md` 〇结论/§一/§二/§四/§五/§七 全改为「已冻结(node-backend-v1.0)」，新增 §七 2026-08-27 冻结裁决。
  - 根 `README.md`：M1 状态→已冻结；文章-代码机制改为里程碑式 tag。
  - `node-backend/README.md`：当前状态行→已冻结（133 测试）。
  - `docs/prd/00-项目章程.md` §七：per-article→里程碑式（与 2026-08-27 已批准决策一致）。
  - `articles/M0-06` + `docs/M0-配图中文提示词包.md`：教学文与配图提示词对齐里程碑式 tag（避免教错读者）。
  - 项目 `MEMORY.md`：里程碑表 `node-backend-v1.0` ⏳→✅，进度段状态改「已冻结」。
- **刻意保留**：交付文档/审阅报告/NOTES/DEV-LOG/REVIEW-LOG 中的「126 passed / 待调优(未冻结)」为当时真实快照，属历史记录，未改动（避免伪造历史）。
- **增量维护约定**：冻结后主干只读；后续 BUG 走 fix→门禁复绿→commit→必要时 bump patch tag（如 node-backend-v1.0.1），不热改冻结主干。已写入 03 §四与 MEMORY。
- 下一步：进入 M1 后端文章写作(M1-01~M1-30)，以冻结代码 node-backend-v1.0 为唯一素材。

## M0-01/M0-02 发布与链接回填（统筹 AI）
- M0-01 已发 CSDN（`https://blog.csdn.net/fungleo/article/details/164119914`），用户把成品同步回本地 `articles/M0-01`；M0-02 也已发（`https://blog.csdn.net/fungleo/article/details/164120426`）并同步本地。
- 链接注入（发布后一次性回填 `{{LINK}}`→URL）：M0-02 内 M0-01 链接（2 处）已注入；M0-02 真实链接注入到引用它的 5 篇草稿——M0-03×3 / M0-04×1 / M0-05×1 / M0-06×2 / M0-08×1（共 8 处）。全仓 `{{LINK:M0-02}}` 残留归零。
- 图片：`{{IMG:CTA-订阅专栏}}` 复用 M0-01 真实 URL；M0-02 两张正文配图（七端架构图 `aafc257b`、波次串行图 `1f64a0e3`）发布时获 CSDN URL。修了一个本地 bug：M0-02 波次串行图占位符未删 + CSDN 另插一张，已合并为单行 `![{{IMG:02-波次串行图}}](url)`；并提醒用户核对 CSDN 线上是否也中招（可能显示字面 `{{IMG:02-波次串行图}}`）。
- `ARTICLES.md` 对齐里程碑式约定：表头旧 per-article tag 说明改里程碑式；M0 全 8 行 tag 列改 `—`；M0-01/M0-02 置 🟢 + 回填真实 URL。
- **遗留待确认**：M0-06 草稿 §4（约 80/92/117 行）仍写「每篇一个全局 tag(`article/Mx-yy`)」「`git checkout article/Mx-yy`」，与其自身 §2 里程碑式约定打架，发布前需改；已向用户提出，待确认是否顺手修。
- 后续：M0-03~08 仍 🟡 草稿，其引用的 M0-04/M0-05/M0-08 等未发文章 `{{LINK}}` 暂留占位，待对应篇发布后回填。

## M0-06 tag 矛盾修复 + 链接回填约定（统筹 AI）
- 用户确认 CSDN 线上 M0-02 波次串行图字面 bug 已手动修；并确认 M0-06 自相矛盾内容由 AI 改、M0-03~08 内部互链「全发完再统一回填」。
- 修 M0-06（5 处，已与 §2 里程碑式对齐）：导语「每篇一个 git tag」→「里程碑式 git tag」；§4「每篇只讲一件事，对应一个 tag」→「对应一个端/里程碑」；§4「git checkout article/Mx-yy」→「git checkout node-backend-v1.0（对应里程碑 tag）」；小结「每篇文章一个全局 tag(article/Mx-yy)」→「里程碑式 tag(node-backend-v1.0 之类)」；顺手修延伸阅读重复半句。核验：全篇 `article/Mx-yy` 仅剩第 50 行演进说明（合理保留），无矛盾残留。
- **链接回填约定（用户裁定）**：M0-03~08 内部互相引用的 `{{LINK}}` 占位（尤其已发 M0-01/M0-02 里指向未发篇的占位），统一等 M0 全部发完后再一次性回填，不逐篇反复编辑已发布文章。

## M0-03 发布（今日第三篇，共 M0-01~03）
- M0-03 已发 CSDN（`https://blog.csdn.net/fungleo/article/details/164121738`），blog AI 已同步本地。今日三篇（M0-01/02/03）达成。
- `ARTICLES.md` M0-03 行置 🟢 + 回填 URL（tag 列 `—`，M0 不打 tag）；发布状态镜像随每篇即时准确。
- 严守裁定：**未触碰任何文章正文 `{{LINK}}` 占位**（M0-03 内部及已发 M0-01/02 里指向 M0-03 的占位均留到 M0 全发完统一填）。剩余未发：M0-04/05/06/07/08。

## 写作/维护分工裁决（A 计划）+ 发布维护 Agent 岗位说明书
- owner 就「后端文章谁写 / 发布维护谁做」征询意见，统筹 AI 给三选项（A 统筹写+分阶段委派维护 / B 后端AI写实现文 / C 全委派），owner 裁定采用 **A 计划**。
- **A 计划要点**：① 文章（M1~M8）由统筹 AI 写（上下文+voice 连续，M0 规范无缝延续）；② 发布维护 M0 收尾由统筹 AI 顺手做（需全局视图、量小），M1 起委派独立「发布维护 Agent」；③ 正文 `{{LINK}}` 统一在某里程碑全发完后一次性回填，不逐篇反复改已发文章。
- 起草 `docs/发布维护-agent-岗位说明书.md`（给独立 AI 的交接文档）：含角色边界、双工作区路径、索引消费/兜底、SOP-A 单篇发布维护、SOP-B 统一回填内链、占位 token 规范、铁律禁忌、grep 证据模板、当前交接状态（M0 已发3/待发5）、与统筹 AI 分工界面。已记入 MEMORY.md 长期约定段。
- 今日工作结束。明日继续发布文章（M0-04~08），发布后由统筹 AI 按 SOP-A 更新 ARTICLES.md，正文 `{{LINK}}` 仍留待 M0 全发完统一回填。
