# 2026-09-22 · 发布维护 Agent

## 任务：把两条新裁定固化进岗位文档（owner 授权「好的，补进去」）

owner 指定：把「全量链接审计」与「中途不重发」补进 `docs/发布维护-agent-岗位说明书.md`。

## 改了什么

### 1. `docs/发布维护-agent-岗位说明书.md`（整篇重写，章节重编号）
- **新增 §6 SOP-C 全量链接审计**，定为**每轮标准动作**：给出两类缺陷的定义与实例——**幽灵链**（ID 不在索引，2026-09-19 修 4 个 ID / 9 处）、**错链**（文案与目标篇不符，2026-09-22 修 2 处）；含步骤、修复边界（只换 ID，不增删链接、不改措辞）、沙箱分片执行提示。
- **新 §8 铁律 3「回填照做，但中途不重发」**：回填必须做；owner 系列期间不更新 CSDN；本地改动累积，收官后一次性全量更新；**取消逐轮重发提醒**，改为报「本轮新增 N 篇受影响」。
- **修订 §4 SOP-A 步骤 3（关键纠偏）**：回填时机由原「里程碑全发完统一处理」→ **逐轮回填**（这才是 M1 的实际做法）。同时补入：M1 裸占位 / M2 `[标题]({{LINK}})` 两代形态差异、同文件串行 Edit、索引「搜不到 ≠ 未发布」。
- **§5 SOP-B 降级**为「收尾/兜底」，保留旧裁定原文并标注修订。
- **§7 Token 规范**补 M2 形态行；**§9 核验模板**补审计命令 + 超长行陷阱说明。
- **§10 交接状态刷新**：原内容已严重过期（写着「M1 31 篇全部未发布 / 全仓 65 处占位 / SOP-0 待执行」），改为 M0 8/8、M1 31/31、M2-01~11 已发布、M2-12~22 草稿。
- **§11 分工表**补「全量链接审计」「最终打磨/全量更新」两行。

### 2. `docs/发布维护-agent-启动提示词.md`（同步重写）
- 与说明书对齐；新增「四、常态任务（每轮三件事）」「五、操作规范（易错点）」两节；铁律节补「中途不重发」「SOP-C 每轮必跑」。
- 章节重编号后，两份文档的交叉引用已全量核验一致（§4/§5/§6/§8/§9）。

## 新发现（已记入说明书 §10，待 owner 定夺）

项目记忆里那份「待最终全量更新清单」（25 篇）**并不完整**：`M1-01`、`M1-04`、`M1-05` 等也在发布后被回填过，但未列入。若 owner 收官时执行「全量更新一切已发布篇」则清单无需精确；若要按需精选，需结合发布时点重建一次。

## 本轮未改正文

只动了 `docs/` 两份文档，**未触碰 `articles/` 与 `ARTICLES.md`**，故无新增待重发篇目。

---

## 追加：M2-12/13 维护周期（同日）

**SOP-A 镜像**：M2-12 按钮级权限 → `166233264`；M2-13 评论审核工作流 → `166234769`。**M2-01~13 全部已发布**。

**回填 4 处**（跨 4 个不同文件）：
- `{{LINK:M2-12}}`：`M2-16` L114、`M2-11` L167
- `{{LINK:M2-13}}`：`M2-06` L192、`M2-15` L138

**14/15 状态**：
- `M2-14`：**无占位符**（历史占位已在前几轮清完），无可注入项——不是漏做。
- `M2-15`：唯一占位就是 `{{LINK:M2-13}}`，本轮已注入 → **就绪**。

**SOP-C 全量链接审计（首次按新 SOP 正式执行）**：
- 方法：分片脚本扫全部 61 篇（20 / 21 / 20 三片），并导出 ≥150 字符的**超长行 97 行**逐行核对——避免 `[Omitted long matching line]` 陷阱（错链恰藏其中）。
- 结果：正文唯一 ID **52** 个 / 索引 **460** 个；**幽灵链 0**；逐条核对「文案 ↔ 目标标题」→ **错链 0**。
- 唯一存疑项（**未改，仅记录**）：`M2-07` L23/L261 文案「认证与授权：JWT、刷新令牌与权限模型」指向 `164363240`（M1-12 认证方案）。**目标正确**（M2-07 的真实前置就是 M1-12），只是文案多写了「与权限模型」。改文案属改写措辞，越界 → 留 owner 定夺。
- 另：`M0-07` L26 用 `fungleo.blog.csdn.net`、`M0-06` L5 用大写 `FungLeo`——主机写法不统一，早已存在、可访问，非缺陷。

**ARTICLES.md 编辑技巧**：M2-12/13 是**相邻两行**，改用「一次 Edit 替换两行整块」，彻底规避同文件写竞争（比串行两次更稳）。

**受影响已发布文章（累积，按「中途不重发」只记录）**：本轮编辑了 `M2-06`、`M2-11`（另两处 `M2-15`/`M2-16` 是草稿，无副作用）。

**临时产物已清理**：`/tmp/audit.py`、`/tmp/compare.py`、`/tmp/art_ids.txt`、`/tmp/long_lines.txt`。
